# BARAM 2026 — 세션 인수인계 (HANDOFF)

> **새 세션 시작법**: 이 파일 + `project_strategy.md`(결론 정본, ~1,700줄) + `_INDEX.md`(스크립트 색인)를
> 업로드하면 이어받을 수 있음. 이 파일이 "지금 어디까지 왔고 무엇이 합의됐는가", 정본이 "왜 그런가".
> 프로젝트 폴더가 Cowork 에 연결돼 있음 — **문서·코드 갱신은 폴더에 직접 커밋하는 방식으로 작업 중.**
> 작성: 2026-07-28 (KST). 직전 판(07-27 밤)은 v20 학습이 **돌아가는 중에** 쓰여서 결과가 빠져 있었음 — 이 판이 그것을 반영함.

---

## 1. 대회와 현재 위치

* 데이콘 제3회 풍력발전량 예측 AI 경진대회 (BARAM 2026). 2025년 1년치 시간별 발전량을
  KPX 3그룹에 대해 예측. **총점 = 0.5(1−NMAE) + 0.5·FICR** (오차 ≤6%cap → 4원, ≤8% → 3원,
  발전량 가중. 채점은 실제 ≥ 10%cap 행만).
* 일정: **대회 종료 08-14** / 산출물 제출 08-17 / 발표(2차, 50%) 08-28. 하루 제출 5회.
* 현재: **최고점 v13 = 0.641458 (public ~200위).** 1위 0.66912. #30(산출물 커트) 0.65032.
  100~200위가 0.002 안에 몰린 정체 구간 — 계단이 아니라 구조적 점프 한 번의 싸움.
* 목표는 1위 (사용자가 명시). 남은 제출 ~17일 × 5회.

## 2. 직전에 끝난 것 — 관측(ASOS) 축 종결. **다음 실험 미선정**

**v20 `[무효]` → v20b `[폐기]`. 두 개가 합쳐져야 축이 닫힘 (§3.32). 제출 0회 소모.**

* **v20** — 관측 컬럼 10개가 gain top-200 에서 **전량 탈락**(110칸 중 5칸, 운영 경로 joint 0/30).
  모델이 관측을 본 적이 없음. 그런데 v13 대비 top-200 겹침이 **74.4%**(폴드당 51개 교체) —
  풀이 812→822 로 커지며 `m0` 의 colsample RNG 가 밀려 선택이 재추첨된 것.
  게이트 +0.0012 는 **obs 가 0개인 G1·G3 에서 더 컸음**(내부 대조군) => 절차 노이즈. `[무효]`
* **v20b** — v13 의 폴드별 200개 고정 + obs 10개 강제 포함(210피처)으로 재측정.
  **−0.0057 [2022 −0.0044 / 2023 −0.0072 / 2024 −0.0056], 3/3 음수, 비율 4.07, 그룹 0/3.**
  판별 불가가 아니라 **일관된 악화**. 사전 등록 분기 3 발동 → **관측 축 폐기 확정.**
* 부수 진단: obs gain 비중 **1.3~1.9%**(컬럼 비중 4.8%의 1/3), 최고 순위 112~156위/210.
  0 이 아니므로 '안 씀' 이 아니라 **'쓰는데 해로움'**. => v20 에서 선택기가 걸러낸 것이 옳았음.
* `[가설·미검증]` 블록 상수 피처 10개가 사실상 **날짜 식별자**로 작동(3년치 블록 1,096개).
  학습 연도의 날짜별 특이성을 외우고 LOYO 에선 그 암기가 전량 손실. ES 트리 수 감소가
  5/8 폴드에서 관측되나 단조는 아님 — 부분 지지.

**다음 실험은 아직 정하지 않았음.** §6 후보에서 하나를 고르되 협업 규칙 1(3줄 설명 + 확인)
적용. 외부 데이터 축에 남은 것은 **ECMWF open data 하나뿐**이며, §3.32 의 블록 상수 규칙에
안 걸림(예보라 행마다 값이 다름).

`[보존]` `data/external/asos_hourly.csv`, `scripts/step33_download_asos.py`,
`src/features_obs.py` 는 지우지 않음 — 2차 심사 "무엇을 어떻게 검증했는가" 의 증거임.

## 3. 협업 규칙 (사용자와 합의된 것 — 반드시 지킬 것)

1. **한 번에 실험 하나. 시작 전에 "이게 뭘 확인하는 건지" 3줄 설명, 이해 확인 후 진행.**
   (한때 속도가 사용자 이해를 앞질러 크게 혼란 — 그 뒤 합의된 규칙)
2. 문서(project_strategy 등)는 **개조식(-음/-함)**, 대화는 평소 존댓말. 문서가 "AI 가 정리한 느낌"
   나지 않게.
3. **버전 규칙**: 버전 하나 = config 파일 + experiment_log 행 + saved_models 폴더 + submit 파일명
   전부 일치. 변형은 접미사(v13-nopost, v13-bag25). step 번호 29는 결번.
4. 제출 전 experiment_log.xlsx 등록 (train.py 는 자동, step 스크립트는 log_experiment 수동/--log).
5. 문서 갱신은 폴더에 직접 커밋. 갱신 시 §번호 체계 유지, 판정은 `[측정]`/`[폐기]`/`[가설]` 태그.
6. 모든 실험은 **사전 등록** (판정 기준을 결과 보기 전에 config notes 나 docstring 에 고정).

## 4. 판정 체계 (핵심 — 이걸 모르면 같은 실수를 반복함)

* **§3.6 게이트**: raw OOF 연도짝비교, `부호 3/3 ∧ 양수 ∧ |평균|>표준편차 ∧ 그룹 2개 이상 양수`.
  train.py `--baseline-oof` 가 자동 판정 (🛑 출력).
* **§3.30 체제 (v13 이후 LB 7전 7패가 만든 규칙)**:
  ① 내부 게이트는 **제출 금지 판정에만** 쓴다. 통과는 필요조건이지 충분조건이 아님.
  ② 제출 근거는 둘뿐: (a) 정보형 변화(새 정보원·표현 — 역대 유일 성공 계열),
     (b) 리더보드 자체를 계측기로 쓰는 것.
  ③ 정산구간형(후처리 적합류)·손실정렬형(MAE/분위)은 게이트를 통과해도 제출하지 않는다
     (LB 전이 실측 전부 ≤ 0).
* **LOYO 의 구조적 편향**: 3폴드 중 val2024 만 배치(과거→다음해)와 같은 모양.
  최신성·드리프트 관련 실험은 val2024 폴드를 따로 봐야 함 (v18·v19 는 여기서 죽었음).
* 무작위 행 분할 금지(자기상관 누설 +0.009 실측) — 시간 블록 분할. 크기 맞춤 대조군 필수.
  Δ는 1−NMAE/FICR 로 분해해서 읽기 (§3.23 계측 규칙).
* **`[07-28 신설]` 게이트 미달 ≠ 가설 기각 (§3.32).** 미달의 원인이 **계측 실패**일 수 있음.
  v20 이 그 사례 — 새 피처가 top-200 에서 전량 탈락해 실험이 실행되지 않았는데 미달로 떴음.
  **미달일 때도 "새 피처가 폴드별 선택에 실제로 들어갔는가" 를 한 번은 셈** (featcols_*.pkl, 학습 0회).
* **`[07-28 신설]` 피처 추가 A/B 는 선택 절차를 상수로 고정하고 잼 (§3.32).**
  풀 크기가 바뀌면 `m0` 의 colsample RNG 가 밀려 top-200 의 **25.6%(51개)** 가 재추첨됨.
  뽑히지도 않은 컬럼 10개가 그 짓을 했고 총점 영향이 ±0.005 — **우리가 쫓는 효과보다 큼.**
  => `--featcols-from <기준선>` + `--force-prefix <접두사>` 를 씀.

## 5. 닫힌 축 지도 (자세한 근거는 정본 § 참조. **재론 금지**)

| 축 | 판정 | 근거 |
|---|---|---|
| 후처리 함수공간 (해상도·버킷·산포변조·그룹분리) | 폐기 | §3.14~3.16 |
| 후처리 적합 절차 (연도가중·축소·출발점·배깅) | 폐기 (배깅은 OOF 통과 후 **LB −0.0020**) | §3.23 §3.27 |
| 손실 정렬 (MAE=v14, 분위 q50=v17) | 폐기 — **LB −0.012 두 번 재현.** 범인은 W 아닌 L | §3.10 §3.29 §3.30 |
| 기대효용 배치 (B-2b placement) | 폐기 (OOF −0.0115, 정지 왼꼬리 부재) | §3.29 |
| 격자 산포 피처 54개 (v15) | 폐기 (OOF 3/3 통과 후 LB −0.0010, 선택예산 대체관계) | §3.21 |
| 드리프트 대응 3종 (제외 step31 / 표본가중 v18 / 편차표현 v19) | 폐기 — 3전 3패. **사상은 연도 안정, 데이터 양 > 최신성** | §3.31 |
| ES 지표 정렬 (v16) | 폐기 (게이트 미달, 제출 안 함) | §3.28 |
| 시드 배깅 · 설정 앙상블 E1~E5 · 폴드 혼합 | 폐기 | §3.17 §3.11 |
| 가용률 기후 보정 | 폐기 — 정지가 터빈 양자라 중앙값 재구성(현행)이 이미 최적 | §3.26 |
| A-6 lead/블록, B-2 채점조건부, 안정도 19피처 | 폐기 | §3.18 §9 |
| 테스트 피처의 적대적 검증 | **규칙 위반이라 설계 폐기** — 유의사항 7 "평가 데이터셋은 추론 목적으로만" | §6.x |
| **max_depth 상향 (v22)** | **폐기 — −0.0104 (3/3, 비율 6.5), 트리수 40~50%↓.** 표현력은 병목이 아니고 깊이 6 이 이미 상한. 하향도 시험 안 함(정규화형 26%) | §3.34 |
| **A-4 격자 가중 (v21)** | **폐기 — Δ −0.0002 판별 불가.** gain 1위·비중 19% 인데 점수 0 => NWP 유도량 축 소진. along/cross(§6-6)도 같은 계열이라 기대치 급락 | §3.33 |
| **외부 관측 (ASOS, v20/v20b)** | **폐기 — v20b 강제포함 실측 −0.0057 (3/3 음수, 비율 4.07, 그룹 0/3).** 창·관측소·컬럼 조합 재시도 금지(사전등록) | §3.32 |

핵심 지식: 격차의 성분 — 1위는 우리 κ-곡선 위(그냥 더 정확함), 그러나 필드 중위권 4팀은
곡선 밖 +0.013~0.031 (같은 정확도에서 FICR 더 획득. 방법 미상) — §3.24·§3.25.
전환율 dFICR/d(1−NMAE) = 2.21 → 정확도 1점 = 총점 1.61점.

## 6. 남은 후보 (착수 전 3줄 설명+확인 규칙 적용)

> **`[07-28 신설]` 피처 계열 후보(1·3·6)는 이제 `--featcols-from` 으로 잰다.** §3.32 가
> 측정한 선택 재추첨 노이즈(±0.005)를 빼지 않으면 판정이 성립하지 않음. v20 이 그 사례임.

1. **ECMWF open data** (§8.8 검증 완료: `s3://ecmwf-forecasts` 익명, CC-BY-4.0,
   **2023-01-18~현재** — 2022 결측이나 G3 라벨연도는 완전 커버. 전일 12z/18z 런 = 기준시점 이전,
   리드 22~51h). 독립 NWP = 정보형. **외부 데이터 축에 남은 유일한 후보.**
   §3.32 블록 상수 규칙에 안 걸림(행마다 값이 다름). 비용 큼 (grib 다운로드·파싱).
2. **타깃 인코딩** hour×month×group (레이블 유도 = §3.8 우회. LOYO 폴드 내 계산으로 누설 방지).
   ⚠ §3.32 주의 — hour×month 는 블록 상수는 아니나 저해상도라 비슷한 암기 위험. 컬럼 1개로.
3. **A-4 격자 가중** (weighted_extract 미사용 상태. §3.20 사전 예측: G3 최대).
   §3.18 규칙대로 `_mean_` **대체**형으로 설계해야 함(추가형 아님).
4. ~~max_depth 상향~~ **폐기 (§3.34).** v22 −0.0104, 3/3, 비율 6.5. 표현력 축 종결.
5. LB 프로브 전략 (합법적 유일 2025 계측기. 예: 전역 배율 ±2% 프로브).
6. ~~흐름방향 경도 along/cross 2컬럼~~ **기대치 급락 (§3.33).** v21 이 "완벽히 포장하고
   예산도 안 쓰고 gain 1위로 쓰이게 해도 Δ=0" 을 보였음. 같은 원천의 다른 결합임.

## 7. LB 기록 전체 (public. v13 이후 전패에 유의)

| 제출 | total | 1−NMAE | FICR | 비고 |
|---|---|---|---|---|
| v11 | 0.636316 | 0.864890 | 0.407743 | 정제 타깃 |
| v12 | 0.636871 | 0.864932 | 0.408809 | |
| **v13** | **0.641458** | **0.867673** | **0.415244** | **현 최고. 통합학습(G1·G3)** |
| v14 | 0.629475 | 0.862562 | 0.396389 | MAE+가중 (손실정렬 1차 실패) |
| v14-nopost | 0.626863 | 0.861542 | 0.392183 | |
| v13-nopost | 0.620418 | 0.872184 | 0.368653 | 계측용 (후처리 여지 +0.021) |
| v15-blend-w50 | 0.633398 | 0.865043 | 0.401753 | |
| v15 | 0.640428 | 0.866593 | 0.414263 | 격자 산포 |
| v16 | (미제출) | | | ES 정렬, 게이트 미달 |
| v13-bag25 | 0.639500 | 0.867775 | 0.411226 | 후처리 배깅 (Δ정확도 +0.0001 = raw 동일 검산) |
| v17-q50post | 0.629282 | 0.859864 | 0.398701 | 분위 q50 (손실정렬 2차 실패, v14 재현) |
| v18/v19 | (미제출) | | | 게이트가 막음 (드리프트 대응) |

## 8. 파일 지도

```
project_strategy.md      결론 정본 (§ 체계. §0=인수인계, §3=측정, §6=규칙, §8=로드맵, §9=폐기 기록)
_INDEX.md                스크립트 색인 + 살아있는 것 표 + saved_models 정리 규칙
experiment_log.xlsx      실험 대장 (LB 점수 3열 포함. v20까지 등록됨... v20 학습 시 자동 추가)
main/train.py            플래그: --clean-target --top-k --joint-groups --grid-features
                         --anom-features --obs-features --year-weight --baseline-oof(게이트)
main/inference.py        마커 기반 자동 동기화 (grid/anom/obs 각각 marker json)
src/                     features(기본) / features_grid / features_anom / features_obs /
                         postprocessing / validation(LOYO·게이트·통합학습) / scada / causality
configs/config_v13.yaml  기준선. v16~v20 = 각 실험 (notes 에 사전등록 포함)
scripts/step1~33         실험 스크립트 (판정은 _INDEX 참조. 29 결번)
data/external/           asos_hourly.csv + asos_stations.csv (외부데이터. 소명 대상)
saved_models/v13/        절대 보존 (기준선 OOF + featcols — v17 이 재사용했음)
```

## 9. 규칙 확정 사항 (소명 대비)

* **유의사항 7**: 평가 데이터셋(ldaps/gfs_test)은 **추론 목적으로만**. 판별기·피처선택 등에 쓰면 위반.
* 규칙 3 실측 조항: 기준시점 이전 확정 관측자료는 사용 가능 (v20 의 근거). 재분석/사후자료 금지.
* 규칙 4 외부데이터: 공개·라이선스·행별 기준시점·재현 코드 4중 조건. ASOS = 공공누리 1유형,
  재현 코드 = step33_download_asos.py. ECMWF = CC-BY-4.0.
* 산식 검증 완료: 공식 utils.py 와 우리 채점기 부등호·집계 전부 일치 (12자리 대조).

## 10. API 키

기상청 API허브 키를 사용자가 보유 (대화에서 공유했음 — **코드·문서에 커밋 금지**, 환경변수
`KMA_API_KEY`). 활용신청 완료: 지점정보(stn_inf) / ASOS 단건(kma_sfctm2) / ASOS 기간(kma_sfctm3).
데이터는 이미 받아서 커밋했으므로 v20 학습에 키 불필요.
