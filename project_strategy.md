# 🌪️ 제3회 풍력발전량 예측 AI 경진대회 (BARAM 2026) — 프로젝트 전략 및 명세서

**v12 · 2026-07-26 개정** — v11 리더보드 결과(0.63632) 반영. 갭 분해와 우선순위 재산정.

> **이 문서의 규칙**: 모든 주장에 근거 표시를 단다.
> `[측정]` 실제 데이터/실험으로 확인함 · `[가설]` 아직 검증 안 됨 · `[폐기]` 검증 결과 틀렸음
> 이 구분은 2차 발표평가의 소명 요건("사용 데이터와 산출 과정을 설명할 수 있어야 함")에 직결된다.

---

## 1. 대회 개요 및 평가 체계

### 1.1 대회 정보

* **대회명**: 제3회 풍력발전량 예측 AI 경진대회 — BARAM 2026
* **주최/주관**: 한국동서발전, GS E&R, 태백가덕산풍력발전 / 데이콘
* **기간**: 2026-07-06 ~ 08-14 (1차 마감) / 08-17 (2차 산출물 마감)
* **참가 형태**: 개인 1인 (취업준비생 자격)
* **제출**: 1일 최대 5회

### 1.2 평가 산식

$$\text{Score} = 0.5 \times (1 - \text{NMAE}) + 0.5 \times \text{FICR}$$

* 평가 대상: 실제 발전량이 설비용량의 **10% 이상**인 시간대만
* 설비용량: `kpx_group_1` 21,600 kWh · `kpx_group_2` 21,600 kWh · `kpx_group_3` 21,000 kWh
* FICR 단가: 오차율 ≤6% → 4.0원 / ≤8% → 3.0원 / 초과 → 0원, **발전량 가중 합**

### 1.3 산식 분석 — 이 대회의 성격

**[측정] FICR의 한계효용이 NMAE의 33배.**
per-sample 한계효용을 전개하면

$$U_i \propto -e_i + \frac{a_i}{4\bar{Y}} \cdot \text{price}(e_i)$$

평균 발전량 시간대 기준, 오차를 0.09→0.06으로 줄여 밴드에 진입하는 이득이 같은 폭의 MAE 개선보다 **33배** 크다. 즉 이 대회는 MAE 최소화 문제가 아니라 **±6% 밴드 적중률 최대화 문제**다.

**[측정] 10% 미만 구간은 평가에서 제외되므로 저발전 예측에 floor를 거는 것이 엄밀히 우월하다.**
평가 대상은 항상 `actual ≥ 0.10·cap`이므로, `|0.13C − a| < a` 가 `a ≥ 0.065C` 에서 항상 성립한다. 0을 찍는 것보다 0.13C를 찍는 것이 모든 평가 케이스에서 낫다. (piecewise 후처리가 이 값을 자동으로 0.17까지 끌어올림 — 독립적으로 같은 결론에 도달)

**[측정] 그러나 밴드 타게팅 여력은 이미 1등과 동급이다. §2 참조.**

---

## 2. 현재 위치와 갭 분해 — 전략의 근거

### 2.1 리더보드 추이

|          | NMAE | FICR | Score |
|---|---:|---:|---:|
| v6        | 0.1361 | 0.3939 | 0.62893 |
| **v11**   | **0.1351** | **0.4077** | **0.63632** |
| 1등       | 0.1131 | 0.4545 | 0.67069 |
| 남은 갭   | 0.0220 | 0.0468 | **0.03437** |

### 2.2 `[측정]` v6 → v11 개선분은 93.5% 가 FICR 에서 왔다

```
총 +0.00739
  1-NMAE 기여  +0.00048  ( 6.5%)
  FICR   기여  +0.00690  (93.5%)
```

v11 은 네 가지를 한꺼번에 바꿨다(정제 타깃 · LOYO · ES 누설 차단 · piecewise 후처리).
사전 예측은 step2 의 OOF 델타(+0.0062)를 근거로 "0.633~0.636" 이었고 실측 0.63632 로 맞았다.
**그러나 기여 분해는 예측과 반대였다.** 정제 타깃이 NMAE 를 올릴 것으로 봤으나 NMAE 는
0.1361 → 0.1351 로 거의 움직이지 않았고, 점수는 FICR 에서 나왔다.
→ 총량 예측은 '측정된 델타를 옮긴 것' 이라 맞았고, 인과 해석은 틀렸다. §9 기록.

### 2.3 `[정정]` 밴드 프리미엄은 소진되지 않았다 — 오히려 1등을 넘었다

"그 NMAE 에서 자연스럽게 나올 FICR" 대비 실제 FICR 의 초과분(half-normal 기준):

|      | NMAE | FICR | 프리미엄 |
|---|---:|---:|---:|
| v6   | 0.1361 | 0.3939 | +0.0549 |
| v11  | 0.1351 | 0.4077 | **+0.0665** |
| 1등  | 0.1131 | 0.4545 | +0.0523 |

v11 문서에서 "프리미엄이 1등과 동급이라 후처리 여력은 소진됐다" 고 단언했으나 틀렸다.
piecewise 단조 보정이 프리미엄을 1등보다 위로 밀어올렸다.

**함의**: 후처리 쪽은 이제 정말 여력이 적다(1등보다 이미 앞섬). 남은 갭은 전부 NMAE 다.

### 2.4 목표 — NMAE 0.1351 → 0.12

프리미엄을 현 수준으로 유지한 채 NMAE 만 개선했을 때:

| NMAE | → FICR | → Score |
|---:|---:|---:|
| 0.1351 | 0.4078 | 0.63634 ← 현재 |
| 0.1300 | 0.4203 | 0.64515 |
| 0.1250 | 0.4335 | 0.65425 |
| 0.1200 | 0.4476 | 0.66378 |
| 0.1131 | 0.4688 | **0.67783** ← 1등 NMAE 도달 시 1등(0.67069) 추월 |

참고: v11 은 LOYO 라 fold 모델이 3개(v6 는 GroupKFold 5개)다. 배깅 효과가 줄어드는
핸디캡을 안고 낸 점수이므로 실제 개선폭은 이보다 클 수 있다.

## 3. 측정의 신뢰 구간 — 노이즈 바닥

**[측정] 합성 실험(5시드, 2022~2024 학습 / 2025 정답) 결과 연도 단위 점수의 변동은 ±0.013~0.016.**

| 검증 체계 | OOF | 실제 2025 | 격차 |
|---|---:|---:|---:|
| 월 GroupKFold + 검증폴드 ES (기존) | 0.6938±0.0022 | 0.7018±0.0129 | −0.0080±0.0123 |
| 월 GroupKFold + 내부 ES(블록) | 0.6920±0.0026 | 0.7025±0.0126 | −0.0105±0.0120 |
| LOYO + 내부 ES(블록) | 0.6932±0.0019 | 0.7032±0.0144 | −0.0099±0.0137 |

기존 대비 격차 축소: 내부 ES **+0.0024±0.0012**, LOYO **+0.0019±0.0023**

**함의가 크다.** v6~v10의 LB 점수 전체 스프레드가 0.0149인데 노이즈 바닥이 ±0.013이다.
→ v6(0.6289) vs v10(0.6185)의 0.010 차이는 **신호라는 근거가 없다.**
→ HPO/시드/가중치 스위프가 만드는 차이는 전부 이 바닥 아래다.

**[측정] 앙상블 가중치 최적화의 실질 이득은 +0.0003±0.0007** (균등가중 대비, 2025 실제 기준). 중첩 가중치도 +0.0000±0.0005. 즉 가중치 방식은 점수가 사는 곳이 아니다.

### 3.1 그래서 검증은 이렇게 한다

* **LOYO(Leave-One-Year-Out)**: 테스트가 2025년 통째이므로 fold도 연 단위여야 같은 난이도를 재현한다
* **연도별 짝비교(paired)**: 연도 난이도 항이 상쇄되어 감도가 15배 올라간다
  (같은 비교를 짝 안 지으면 ±0.018, 짝 지으면 ±0.0012)
* **판정 기준**: 모든 연도에서 부호 일치 ∧ |평균차| > 연도별 차이의 표준편차 → 신호. 아니면 제출하지 않는다

---

## 4. 데이터 구조 — 확정된 사실

### 4.1 예보 블록 구조 `[측정]`

* 매일 09 KST 초기화 → **13 KST 공개** → 익일 01:00 ~ 익익일 00:00 (24시간)
* **lead time 12~35시간**. 블록 90개 전부 정확히 24행 (`assert_block_structure` 확인)
* **같은 블록 내 미래 참조는 합법** (24시간이 동시 공개). 단 반드시 `groupby('data_available_kst_dtm')` 안에서
* 블록을 넘는 전방 참조는 **24시간 미래 정보 사용 = 규칙 3항 위반** (블록당 정확히 1행)

### 4.2 SCADA `[측정]`

| 항목 | 값 |
|---|---|
| `power_kw10m` 단위 | **kW가 아니라 10분간 kWh**. 시간값 = 6개 **합** |
| 정격 확인 | Vestas 600 kW10m(=3.6MW), Unison 700(=4.2MW) — 기준곡선 최댓값이 정확히 3600/4200 |
| 시각 정렬 | **Vestas +50분, Unison +60분** (벤더별로 구간 끝/시작을 다르게 찍음) |
| 정렬 R² | Vestas 0.99930 / Unison 0.99848 |
| 계통 손실계수 k | G1 0.9857 (RMSE 0.54%cap) · G2 0.9893 (0.60%) · G3 1.0009 (1.16%) |
| 기준곡선 표본 | Vestas 315,636개(40/60 bin) · Unison 86,980개(37/60 bin) |

**→ SCADA로 라벨을 사실상 완전 재구성 가능**(잔차 0.5~1.2% vs FICR 밴드 6%). 가동률 판정이 추정이 아니라 확정이 된다.

`[가설]` G3만 k≈1.0인 것은 원동 2~5호가 가덕산과 다른 연계점을 쓰기 때문으로 추정. 미검증.

### 4.3 가동률 `[측정]`

| | ok | stopped_maint | derated | stopped_weather | missing |
|---|---:|---:|---:|---:|---:|
| Vestas | 94.1% | 4.2% (13,134) | 1.7% (5,483) | 0.0% (137) | — |
| Unison | 92.6% | 3.8% (3,317) | 2.6% (2,260) | 0.2% (152) | 0.8% (735) |

그룹별 평균 가용률: G1 95.61% · G2 96.07% · G3 95.38%
**가용률 100%인 시간 비율: G1 85.8% · G2 88.5% · G3 56.3%**

**→ 터빈-시간의 약 6%가 정비/고장으로 눌려 있고 현재 전부 "바람이 약했던 시간"으로 학습되고 있다.**
특히 G3는 라벨이 2년치뿐인데 그중 절반 가까이가 부분 고장 상태다.

고풍속 컷아웃(`stopped_weather`)은 137/152건으로 거의 없다 → 이 사이트는 컷아웃이 드물다.
**단, 이 구분 자체는 필수다.** 기상 요인 정지는 예측 가능한 현상이므로 타깃에서 제거하면 안 된다.

### 4.4 격자 → 그룹 가중치 `[측정]`

LDAPS 16격자(1.5km), 표고 869~1001m. 터빈은 능선 정상부(~990m).

| 그룹 | 상위 격자 (표고 반영 IDW) |
|---|---|
| G1 | g5(0.27) g6(0.26) g10(0.08) g11(0.08) |
| G2 | g6(0.29) g11(0.20) g12(0.10) g7(0.09) |
| G3 | g12(0.36) g11(0.16) g16(0.07) g7(0.07) |

동일 시각 10m U성분: G1 5.378 / G3 6.072 / **16격자 균등평균 5.298**
→ 현재 세 그룹 모두에 5.298 하나를 먹이고 있다. 그룹 간 실제 차이 13%를 버리는 중.

GFS 9격자(0.25°): 최근접 g5가 4.9km, 나머지는 19~40km → g5가 지배.
**[측정] `surface_0_sp`로 기압경도를 만들면 안 된다.** g3 102,629 Pa vs g5 93,450 Pa — 9,000 Pa 차이가 전부 표고다. 종관 흐름은 850hPa u/v 또는 500hPa 지위고도를 쓸 것.

### 4.5 터빈 배치와 후류 `[측정 = 좌표 기하]` / `[가설 = 결손량]`

터빈 17기 전부 2.4×2.5 km 안, 허브고도 전부 117m. Vestas 체인 이웃간격 중앙값 **267m = 2.1D** (통상 4~7D).
그룹별 배치축: G1 29° · G2 154° · G3 142°

Jensen 모형 기반 풍향별 평균 속도결손(%) — **평지 가정 이상화 모형, SCADA로 검증 필요**:

```
 dir      0    30    60    90   120   150   180   210   240   270   300   330
 G1    11.4   6.7  11.0  14.3  17.0   8.1  11.6   5.6   5.5   2.2   3.1   7.1
 G2     6.9   4.2   0.0   6.4  17.0  15.8  11.3   1.9   7.2  14.0  20.8  11.1
 G3     4.1   0.0   3.0   3.3   6.4  14.0   0.0   3.5   1.1   7.5  17.6  20.0
```

교차 후류 비중: G1 35% · G2 54% · G3 60% (다른 그룹 터빈이 만드는 후류)
→ **세 그룹을 독립 모델링하면 이 신호를 원천적으로 못 잡는다.**

### 4.6 풍향계 상태 `[측정]`

| 터빈 | 오프셋 | 원형 산포 | 해석 |
|---|---:|---:|---|
| vestas_wtg04 | +23.4° | 10.2° | 계통적 오프셋 — 보정 대상 |
| vestas_wtg12 | −8.8° | **51.8°** | 오프셋 아닌 **불안정** — wake 분석에서 제외 권장 |
| vestas_wtg05 | −0.9° | 26.6° | 불안정 |
| vestas_wtg01 | −1.9° | 22.1° | 불안정 |
| unison 전체 | −28.7 ~ +23.8° | 9.0~19.3° | 능선 위 실제 공간 변동과 섞여 분리 곤란 |

---

## 5. 파이프라인 및 모듈 구조

```
wind_project/
├── run.py                     # CLI 엔트리포인트
├── configs/                   # yaml (config_v1 ~ v11)
├── information/               # data_description.md, info.xlsx
├── data/
│   ├── train/  test/
│   └── scada_derived/         # [신규] available_capacity.csv, clean_target.csv, wd_offset_*.csv
├── main/
│   ├── train.py               # [개정] argparse, LOYO, 누설없는 ES, 중첩 후처리 평가
│   ├── inference.py           # [개정] argparse, 모델파일 glob 탐색
│   └── ensemble.py
├── notebooks/                 # EDA.ipynb
├── scripts/
│   └── step1_scada_check.py   # [신규] SCADA 단독 점검
├── saved_models/
├── src/
│   ├── features.py            # 파생 피처
│   ├── postprocessing.py      # [개정] simplex 스케일 수정 + piecewise 단조 보정
│   ├── validation.py          # [신규] LOYO, 누설없는 ES, 연도별 오차막대/유의성
│   ├── causality.py           # [신규] 규칙 3항 자동 감사
│   ├── scada.py               # [신규] 정렬·집계·가동률·타깃 정제
│   ├── grids.py               # [신규] 그룹별 격자 가중 추출
│   ├── logger.py  utils.py
└── experiment_log.xlsx
```

### 5.1 후처리 `src/postprocessing.py` — 수정 내역

**[측정] 기존 Nelder-Mead는 실질적으로 alpha 하나만 최적화하고 있었다.**
scipy는 x0 성분이 0이면 0.00025, 아니면 ×1.05로 초기 simplex를 만든다.
`initial_guess=[1.0, 0.0, 10.0]` → 설비용량이 21,600 kWh인데 beta를 0.00025 kWh, zero_th를 0.5 kWh 스텝으로 탐색.

| | 점수 | 찾은 파라미터 |
|---|---:|---|
| 원본 OOF | 0.6570 | — |
| 기존 코드 | 0.6617 | α=0.96, β=−0.00, z=10.43 ← beta/zero_th 거의 안 움직임 |
| simplex 스케일 수정 | 0.6717 | α=0.88, β=1329, z=439 |
| 스케일 + 다중 시작점 | 0.6719 | α=0.83, β=1839, z=506 |

**한 줄 때문에 +0.010을 놓치고 있었다.**

추가로 구간별 단조 보정(piecewise) 도입. 정직한 holdout 기준:
linear(수정) **+0.0128** / piecewise **+0.0225**
piecewise가 찾은 절점 `[0.17, 0.206, 0.331, 0.552, 0.759, 0.988]` — §1.3의 floor 우월성을 독립적으로 재발견.

`[가설]` 다만 §2.2에 따르면 실제 데이터에서의 이득은 이보다 작을 가능성이 높다. 합성 OOF에 심어둔 편향(α=0.93, β=0.04C)을 현재 Nelder-Mead는 이미 α로 잡고 있기 때문. **측정 후 확정할 것.**

---

## 6. 규칙 준수 (대회 3항 Data Leakage)

`src/causality.py`가 **실행 가능한 증명**을 제공한다.
원리: 블록 B의 `data_available_kst_dtm` 이하인 행만으로 피처를 다시 만들어, 전체 데이터로 만든 값과 B 구간이 일치하는지 대조.

**[측정] 검증 결과**

| 방식 | 판정 |
|---|---|
| groupby 없는 전방참조 (`shift(-1)`, `rolling(center=True)`) | ❌ 블록당 1행씩 위반 |
| 현재 features.py 방식 (`diff(1)`, `shift(1)`, 후방 rolling) | ✅ 통과 |
| 블록 groupby 전방참조 | ✅ 통과 |

**현재까지 제출한 v1~v10은 규칙 위반이 아니다.** 후방 참조만 쓰고 있어서 블록을 넘어도 더 일찍 공개된 예보를 쓰는 것이기 때문. (물리적으로는 서로 다른 초기화 시각을 섞는 것이라 부정확)

`lead_hours`는 위반이 아니다. `forecast_kst_dtm`과 `data_available_kst_dtm` 모두 예측기준시점에 이미 손에 있는 일정 정보이며, 주최측이 test CSV에도 이 컬럼을 제공한다.

2차 산출물 제출 시 `--audit` 실행 로그를 첨부한다.

---

## 7. 실험 이력

| 버전 | OOF | Public LB | 1−NMAE | FICR | 주요 변경 |
|---|---:|---:|---:|---:|---|
| Baseline | — | 0.588346 | 0.863681 | 0.313010 | 샘플 제출 |
| v1 | 0.7137(Tr) | 0.593206 | 0.861465 | 0.324947 | 달력 + GFS 80m (RF) |
| v2 | 0.7993(Tr) | 0.602090 | 0.863945 | 0.340235 | LightGBM + 100m |
| v3 | 0.6864 | 0.605691 | 0.866023 | 0.345359 | 5-Fold GroupKFold |
| v4 | 0.6886 | 0.613708 | 0.870777 | 0.356640 | 격자 pivot + 117m 멱법칙 + 풍향 sin/cos |
| v5 | 0.6880 | 0.615171 | 0.870822 | 0.359520 | 푄/열돔 + 시계열 차분 |
| **v6** | **0.6087** | **0.628930** | 0.863924 | 0.393937 | 6대 물리피처 + Pruning + XGB CUDA + **Nelder-Mead** |
| v7 | 0.5925 | 0.614021 | 0.861273 | 0.366769 | LightGBM |
| v8 | 0.6103 | 0.618874 | 0.859303 | 0.378446 | CatBoost |
| v9 | 0.6118 | 0.549858 | 0.813310 | 0.286406 | [오류] 이중 후처리 |
| v10 | 0.6118 | 0.618476 | 0.858726 | 0.378225 | [수정] Raw 결합 후 단일 후처리 |
| **v11** | **0.6156**† | **0.636316** | 0.864890 | 0.407743 | **정제타깃 + LOYO + ES누설차단 + piecewise 후처리** |

† v11 OOF 는 **LOYO** 기준이라 v1~v10(월 GroupKFold)과 절대값 비교 불가.
  v11 상세: raw OOF 0.5870 → 후처리 0.6156, 연도별 표준편차 0.0102, 피처 812개, 80.7초.
  실험 로그(`experiment_log.xlsx`)에 `Validation` 열을 추가해 혼동을 차단했다.

**v6 대비 +0.00739. 1등(0.67069)까지 0.03437.**

**분해**: v1→v6 총 상승 +0.0357 중 FICR 기여 +0.0345 = **97%**. 1−NMAE는 10개 버전 동안 +0.0025밖에 안 움직였다.
→ 지금까지의 성과는 전부 후처리에서 나왔고, §2.2에 따라 그 여력은 소진됐다.

---

## 8. 로드맵 (v11 ~ )

### 8.1 폐기된 계획

v10 문서의 "95회 제출 소진 로드맵"(Tweedie 스위프 10회, max_depth 그리드 10회, 시드 아베리징 10회, 가중치 그리드 25회)은 **폐기한다.**

근거: §3에서 측정한 노이즈 바닥(±0.013)과 가중치 최적화 실질 이득(+0.0003)을 고려하면, 이 계획의 대부분이 노이즈를 신호로 읽는 절차다. 또한 Public 40% 기준 그리드 서치는 Public에 대한 과적합이며 Private 60%에서 되돌아온다.

### 8.2 진행 상태

| # | 작업 | 상태 | 기대 효과 |
|---|---|---|---|
| 1 | 후처리 simplex 버그 수정 + piecewise | 코드 완료 | 측정 필요 (합성 +0.013~0.022, 실제는 더 작을 듯) |
| 2 | LOYO + 누설없는 ES + 유의성 판정 | 코드 완료 | 점수 +0.002. 실질 가치는 이후 실험의 신뢰도 |
| 3 | 규칙 3항 자동 감사 | 코드 완료 | 점수 무관, 2차 소명 자료 |
| 4 | SCADA 정렬·집계·가동률·타깃 정제 | **점검 통과** | NMAE 직격 (6% 오염 제거) |
| 5 | 정제 타깃으로 학습 + LOYO 검증 | **완료** | A/B +0.0126(raw) / LB 총 +0.00739 |
| **배치 1** | **물리 핵심 피처** | **← 다음** | NMAE 직격 |
| 6-1 | LDAPS 50m 복원 → 10↔50m 시어 → LDAPS v117 | 미착수 | GFS 25km → LDAPS 1.5km |
| 6-2 | GFS 10/80/100 3점 시어 + 곡률 | 미착수 | 2점→3점 |
| 6-3 | 두 v117 앙상블 + 격차(불확실성) | 미착수 | |
| 6-4 | 로터 등가풍속 REWS (54~180m) | 미착수 | 고시어 구간 과대평가 보정 |
| 6-5 | 습윤공기 밀도(비습 q) + 허브고도 기압 | 미착수 | 지금 건조공기 + 지상기압 |
| 6-6 | IEC 밀도보정 풍속 v·(ρ/1.225)^(1/3) | 미착수 | 겨울/여름 4% 차이 |
| 6-7 | **SCADA 경험 파워커브 적용** | 곡선은 적합 완료 | **정격 평탄화 부재 해소** |
| **배치 2** | **환경·구조 피처** | 대기 | |
| 7-1 | 그룹별 격자 가중 (`grids.py`) | 코드 완료, 미적용 | 균등평균 5.298 vs G1 5.378 / G3 6.072 |
| 7-2 | 안정도: 온위·벌크Ri·NDNLW·운량·BLH↔117m·LLJ | 미착수 | 야간 하층제트를 EDA 로 확인했으나 설명 변수 전부 미사용 |
| 7-3 | 풍향: wd_sin/cos·veer·wake 결손 | 미착수 | 그룹 구분 |
| 7-4 | lead_hours + 블록 내 미래 참조 | 미착수 | 12~35h 축 부재 |
| 8 | 3그룹 통합 long-format 학습 | 미착수 | G3 학습량 실질 3배 |

### 8.4 미사용 컬럼 재고 `[측정]`

LDAPS 30개 / GFS 35개 변수 중 의미 있게 쓰는 것은 약 절반. 안 쓰는 절반이 하필
**안정도·복사·구름** 계열이다. 야간 하층제트를 EDA 에서 확인해놓고 그것을 설명하는
변수들이 전부 놀고 있다.

| 미사용 | 쓰임새 |
|---|---|
| LDAPS 50m 바람(`50MUmax/min` 중점) | 1.5km 해상도의 유일한 상층 바람. 현재 "난류 변동폭" 으로만 사용 |
| LDAPS `_2_q` 비습, `_2_r` 상대습도 | 습윤공기 밀도 |
| LDAPS `NDNLW` | 야간 복사냉각 → 안정층 → 시어 강화의 최직접 신호 |
| LDAPS 운량 4종 `hcc/mcc/lcc/VLCDC` | 맑은 밤 = 강한 안정층 |
| LDAPS `XBLWS/YBLWS` | 5m 경계층 바람 |
| GFS 80m 바람 | 10/80/100 3점 시어 |
| GFS `planetaryBoundaryLayer_0_u/v`, `VRATE` | 경계층 평균 바람, 환기율 |
| GFS 700/500 hPa | 850 만 사용 중. 종관 패턴 |

### 8.3 제출 운용 원칙

* HPO는 전부 로컬에서. 제출은 **서로 다른 가설당 1~2회**
* 제출 전 `is_difference_real()`로 판정 → ⚠️면 제출하지 않는다
* 최종 파일 선택은 **Public LB 1등이 아니라 로컬 LOYO 1등**으로

---

## 9. 폐기·정정된 가설 기록

2차 발표 시 "무엇을 어떻게 검증했는가"의 근거가 된다.

| 가설 | 결과 | 근거 |
|---|---|---|
| ES 누설이 CatBoost 85% 쏠림의 원인 | **[폐기]** 누설 효과는 0.0024±0.0012로 쏠림을 설명 못 함 | 5시드 합성 실험 |
| 앙상블 가중치 최적화가 중요하다 | **[폐기]** 균등가중 대비 +0.0003±0.0007 | 5시드 합성 실험 |
| XGB가 CatBoost보다 불연속 구간을 잘 잡는다 | **[보류]** 0.010 차이는 노이즈 바닥(±0.013) 이내 | §3 |
| FICR 후처리가 최우선 레버다 | **[정정]** v1→v6에서는 맞았으나 프리미엄이 1등과 동급 → 소진 | §2.2 |
| vestas_wtg12 풍향계가 170° 틀어짐 | **[폐기]** head 49시간의 착시. 실제로는 산포 51.8°의 불안정 | 3년 전체 |
| 커테일먼트 시간대(정오) 피처 | **[보류]** 제주 계통 논리, 태백 육상 계통에 근거 약함 | 미검증 |
| GFS `surface_0_sp` 격자차 = 기압경도 | **[폐기]** 9,000 Pa 차이가 전부 표고 | §4.4 |
| 밴드 프리미엄이 이미 1등과 동급 → 후처리 여력 소진 | **[폐기]** piecewise 가 프리미엄을 +0.0549→+0.0665 로 올려 1등(+0.0523) 추월 | §2.3 |
| 정제 타깃이 NMAE 를 올린다 | **[정정]** OOF 에선 +0.0048 였으나 LB NMAE 는 +0.0010 뿐. 실제 이득은 FICR 에서 나옴 | §2.2 |
| vestas_wtg12 풍향계 170° 오류 | **[폐기]** head 49시간 착시. 실제 오프셋 −8.8°, 산포 51.8°(불안정). 계통 오차는 wtg04(+23.4°, 산포 10.2°) 하나뿐 | 3년 전체 |