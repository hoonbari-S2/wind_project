# 🌪️ 제3회 풍력발전량 예측 AI 경진대회 (BARAM 2026) — 프로젝트 전략 및 명세서

**v20 · 2026-07-28 개정** — 외부 관측 도입 1차 시도가 `[무효]` 로 판정됨. 계측 규칙 하나를 더 얻음.

* `[측정 · 최우선]` **gain 기준 top-K 선택 자체가 노이즈 원천임.** 풀에 컬럼 10개를 더한
  것만으로 — 그 10개가 **하나도 안 뽑혔는데** — 폴드별 top-200 의 **25.6%(평균 51개)** 가
  뒤바뀜. `m0` 의 colsample RNG 스트림이 밀리기 때문. 총점 영향 ±0.005 로 **우리가 쫓는
  효과보다 큼.** => **피처 추가 A/B 는 선택 절차를 상수로 고정하고 잰다**(`--featcols-from`) (§3.32)
* `[무효]` **v20(ASOS 관측 10피처).** 게이트 미달이었으나 원인을 열어 보니 관측 피처가
  gain top-200 에서 **전량 탈락**(운영 경로 joint 는 0/30) — 실험이 실행되지 않았음.
  게이트가 잡은 +0.0012 는 관측이 0개인 G1·G3 에서 **더 컸음**(내부 대조군). 선택 재추첨임.
  '관측은 안 통한다' 가 아니라 **관측이 모델에 들어간 적이 없다.** => v20b 로 재시험 (§3.32)
* `[폐기]` **v20b — 관측 축 종결.** 210피처로 강제 포함해 재측정: **−0.0057, 3/3 음수,
  비율 4.07, 그룹 0/3.** 판별 불가가 아니라 **일관된 악화**임. obs gain 비중 1.3~1.9% 로
  0 이 아니므로 '안 씀' 이 아니라 **'쓰는데 해로움'**. => v20 에서 선택기가 걸러낸 것이 옳았음.
  `[가설]` 블록 상수 피처가 날짜 식별자로 작동해 LOYO 에서 암기 손실을 냄 (§3.32)
* `[규칙]` **블록 상수 피처는 LOYO 에서 위험함** — 유효 표본이 행이 아니라 블록(연 365)임.
  개수를 최소로, 수준이 아니라 편차 형태로. ECMWF 는 행별로 변하므로 이 규칙 밖임 (§3.32)
* `[폐기]` **A-4 격자 가중(v21).** 폴드별·그룹별 NNLS 가중 풍속 3컬럼. 게이트 −0.0002,
  부호 갈림, 그룹 0/3 = **판별 불가**. 그런데 그 3컬럼이 **gain 1위 · 비중 19%** 였음 —
  최대로 쓰였는데 점수가 0. **선택 예산을 쓰지 않고 압축 컬럼만 준 실험**이므로 §3.21 의
  "가치는 정보가 아니라 예산 절약" 명제가 직접 확정됨. NWP 유도량 축 소진 (§3.33)
* `[규칙]` **gain 순위·비중은 피처 가치의 증거가 아님.** '분할이 얼마나 편했나' 이지
  '없던 정보를 얼마나 줬나' 가 아님. gain 1위 · 19% 가 Δ −0.0002 와 공존함이 실측됨.
  유일한 증거는 여전히 **연도짝비교 Δ** (§3.33)
* `[폐기]` **max_depth 상향(v22).** 6→8 한 줄만 바꿔 −0.0104(3/3, 비율 6.5), 그룹 0/3.
  ES 트리 수 40~50% 감소로 깊이가 실제 작동했음이 확인됨 — **작동한 결과가 악화.**
  **표현력은 병목이 아니고 깊이 6 이 이미 상한 근처임.** 하향은 정규화형이라 시험 안 함 (§3.34)
* `[확정]` **`--featcols-from` 이 기준선을 비트 단위로 재현함** — v21 의 독립 경로가 v13 과
  폴드 점수 소수 넷째 자리까지 동일, `group_2 Δ = +0.0000`. §3.32 계측기 검증 완료 (§3.33)
* `[규칙]` **게이트가 미달로 떠도 '왜 미달인지' 를 한 번은 연다.** 미달의 원인이 계측 실패일
  수 있음. 최소 확인 = 새 피처가 폴드별 선택에 실제로 들어갔는가 (학습 0회) (§3.32)

---

**v19 · 2026-07-27 개정** — 후처리 **적합 절차** 축까지 닫음. 계측 규칙 두 개를 새로 얻음.

* `[확정]` **다중 출발점은 9개로 충분함**(9→25 가 +0.0002). 적합 곡선이 꽤 달라도 점수가
  거의 같음 = 최적점 근처가 평평함. => **§3.14~3.16 의 음수 판정에 옵티마이저 노이즈 없음** (§3.23)
* `[폐기]` **연도 가중.** 무작위 분할로 +0.0143 이 나왔으나 **+0.0092 가 자기상관 누설**이었음.
  월 블록 분할 후 +0.0051, 그중 **FICR 몫 91%** 라 문턱 배치에 불과함 (§3.23)
* `[계측 규칙]` **① 크기 맞춤 대조군 필수 ② 시간 블록으로 나눔 ③ Δ 를 1−NMAE/FICR 로 쪼갬.**
  ②를 안 지켰을 때의 누설이 **우리가 쫓던 효과 크기의 2배**였음 (§3.23, §9.1)
* `[폐기]` **강원 연 기후 특성 PDF 의 마지막 용도.** 부호 대조군이 반대로 나옴.
  정체는 이상기후가 아니라 **시간 인접성**이었음 (§3.23)
* `[측정]` **격자 산포는 raw OOF 3/3 통과 후 LB 에서 짐**(−0.0010). 원인은 선택 예산과의
  대체 관계. **컬럼 수가 곧 비용**임. 1~2컬럼짜리 흐름방향 경도만 남김 (§3.21)
* `[측정]` **통합 학습은 양이 아니라 동질성.** G1+G2 +0.0126 vs G1+G3 +0.0034.
  G3 의 세 번째 구조적 핸디캡 = **동질 파트너 부재** (§3.22)
* `[측정 · 07-27 LB 정찰]` **1위 실측 0.66912** (연식2, 1−NMAE 0.88483 / FICR 0.45342).
  갭 0.02766 = 정확도 31% + FICR 69% (이전 가정 19/81 을 수정함). **우리는 현재 top-40 밖**
  (#40 = 0.64869, #30 = 0.65032 → 산출물 커트라인까지 +0.0089) (§3.25)
* `[측정 · 07-27]` **모양 프리미엄의 존재가 횡단면에서 확정됨.** 우리보다 부정확한 팀(#28)이
  FICR 을 +0.022 더 가져감. 같은 정확도에서 +0.013~0.022 가 필드에서 실현 중 (§3.25)
* `[해소]` `available_capacity.csv` 는 **공식 제공 파일이 아님** (공식: LDAPS/GFS train·test,
  train_labels, SCADA vestas/unison **train만**, info.xlsx). => 2025 가용량은 존재하지 않음.
  '행별 기지 인자' 아이디어 기각, 가용률 기후값(월×시각)만 남음 (§0)
* `[프레임]` 1위와의 격차는 **81% 가 FICR** 인데 FICR 과 NMAE 가 결합돼 있어 그대로 못 읽음.
  **모양 프리미엄**으로 갈라야 함. `step28` 이 학습 0회로 냄 (§3.24) — **미측정**

---

## 0. 다음 세션 인수인계 `[2026-07-27 기준]`

> 이 문서 하나로 이어받을 수 있게 여기에 모아 둠. 대화 맥락은 없어도 됨.

**지금 대기 중인 확인 4개 (전부 저비용, 순서대로)**

| # | 할 것 | 비용 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | ~~ES 지표 확인~~ → **v16 학습 대기** | **확인 07-27** | v13/v15 는 `tweedie-nloglik@1.5`, v14 는 `mae` 로 멈추고 있었음. 패치 완료(validation.py `eval_metric: "comp"` 인터셉트, train.py 수정 0줄). config_v16 으로 학습 + 게이트 판정만 남음 |
| 2 | ~~평가 산식 코드 경계 확인~~ | **완료 07-27** | 공식 utils.py 대조: `<=0.06`/`<=0.08`/`>=0.10cap`/그룹평균 **전부 일치**. 난수 대조 12자리 일치 |
| 3 | ~~step28~~ | **완료 07-27** | 전환율 2.21 / 1위는 우리 κ-곡선 위(프리미엄 ≈0) / 필드 4팀은 곡선 밖 +0.013~0.031 / PART 5 대칭 → 조건부 이동 축 폐쇄. §3.24 갱신 |
| 4 | ~~step27c 진단~~ → **step27c 전체 실행 대기** | **(가) 확정 07-27** | obj(③)≥obj(②) 8/8, 49출발 개선 ≈0 → (나)(다) 배제. 후처리 적합의 문턱 과적합 실재. 평가Δ 평균 +0.0022, 2023·G3 +0.0168 최대. 처방=배깅(PART 2) |
| 6 | ~~step30 1차~~ → **제출 2건 + 재시험 1건** | 07-27 완료 | 분위 학습 +0.0131 (3/3, FICR 만 +0.0239) / 배치 −0.0115 (왼꼬리 부재 진단, §3.29) |

**07-27 밤 확정: 위 제출 2건 전부 v13 미만 (§3.30). 제출 큐 비움.**
step31 완료(§3.31): 드리프트 실재·단조·물리적, 피처 제외 기각. **v18(연도 가중 학습) 폐기** — val2024 폴드 −0.0061 로 사전등록 조건 위반, 제출 안 함. **v19 폐기(07-28)** — 게이트 부호 갈림, val2024 음수. 드리프트 축 3전 3패 완전 종결(§3.31). **v20 (step33) 학습 완료 — `[무효]` 판정, 제출 0회 소모 (§3.32). 후속 v20b 대기.** ASOS 시간자료 71,429행 (태백 #216 12.7km + 정선군 #217 27.7km, 2021-12~2025-12, 누락 0.15/0.37%) → data/external/asos_hourly.csv 커밋. `[정직한 주의]` 태백 관측소는 읍내 분지(712m)라 평균 풍속 1.3~1.5 m/s — 능선 바람이 아니라 골짜기 바람임. 풍속 피처는 신호가 약할 수 있고 기온·습도·기압·편차 쪽이 본명임. 산 위 AWS 는 최근접이 10.3km(하장)로 이점 없어 미추가. 게이트가 판정.

~~제출 큐 (오늘 2건 — 각각 단일 변수 LB 시험)~~

```
# ① 후처리 배깅 (§3.27. v13 raw 위에 적용 → v13 과 A/B)
python scripts/step27c_bagging.py --config configs/config_v13.yaml \
    --oof ./saved_models/v13/oof_preds.csv --make-submission sub25
# ② 분위 v17 (§3.29 게이트 1. v14 교란 분리 시험)
python scripts/step30_quantile_utility.py --config configs/config_v13.yaml \
    --skip-train --make-submission --submit-kind median
# ③ (제출 아님, 1~2분) 배치 재시험 — 정지 혼합
python scripts/step30_quantile_utility.py --config configs/config_v13.yaml \
    --skip-train --mix-avail
```
| 5 | ~~available_capacity / 가용률 기후값~~ | **폐기 07-27** | SCADA 파생물(train만) 확정. 기후 보정은 전 그룹 악화(§3.26) — 정지가 터빈 양자라 평활 배율이 밴드를 못 옮김. 현행 중앙값 재구성이 이미 최적 |

**07-27 public LB 스냅샷** (스크립트 `step28` PART 3b 에 하드코딩, 전 행 검산 통과)

| 순위 | 팀 | total | 1−NMAE | FICR | 제출 |
|---|---|---|---|---|---|
| 1 | 연식2 | 0.66912 | 0.88483 | 0.45342 | 62 |
| 2 | 별하솜 | 0.66888 | **0.88787** | 0.44989 | 32 |
| 3 | GBSNU | 0.66703 | 0.87932 | **0.45473** | **23** |
| 30 | RIS | 0.65032 | 0.87642 | 0.42421 | 39 |
| 40 | chaboom | 0.64869 | 0.86878 | 0.42860 | 32 |
| — | **우리 v13** | **0.641458** | 0.867673 | 0.415244 | ~20 |

* §3.13 의 구도 유지 — 1위는 2위보다 부정확한데 FICR 로 이김. 3위 GBSNU 는 **23회 제출**로
  전체 최고 FICR. 제출 횟수는 상위권의 필요조건이 아님 (8회로 8위인 팀도 있음)
* **top-30 커트라인 0.65032, 우리와 +0.0089.** 대회 종료(08-14)까지 바는 계속 오름

**미탐색 목록 — 기대값 순** (자세한 근거는 §8.2·§8.3)

1. ~~`available_capacity.csv` / 가용률 기후값~~ **폐기 (07-27, §3.26).** 정지는 터빈 양자
   (1/6·1/5 단위)라 평활 배율은 정상 행만 밴드 밖으로 밈. 전 그룹 악화 실측. 중앙값 재구성 유지
2. **B-2b 분위회귀 + 행별 기대효용.** `reg:quantileerror` 가 `quantile_alpha` 리스트를
   한 번에 받으므로 **학습 1~2회**면 됨. 이전 판에 '5~7회' 로 적혀 있던 것은 오류였음.
   **§3.25 가 직접적 동기** — 같은 정확도에서 +0.013~0.022 FICR 이 필드에서 실현 중이고,
   우리 단조 후처리는 국소최적이라 그 몫은 후처리 밖(행별 배치)에 있음
3. **적대적 검증 / 공변량 이동.** 2025 NWP **피처는 우리한테 있음.** 판별기 AUC 로
   "2025 는 어느 해를 닮았나" 를 **레이블 없이** 답함. 부수적으로, gain 기준 top-k 가지치기가
   **분포 이동한 피처를 우선 살리고 있을** 위험을 점검함
4. **타깃 인코딩** `hour × month × group`(864칸, out-of-fold). **§3.8 따름정리를 우회함** —
   NWP 유도량이 아니라 **레이블 유도량**이라서. 지금까지 실패한 피처가 전부 NWP 재계산이었음
5. **`tweedie_variance_power` 스윕**(1.1~1.9). 규제가 아니라 **손실 모양** 파라미터.
   밴드 추종 목적함수의 가장 싼 예비 실험
6. **정제 타깃 파라미터**(`min_avail_frac` 스윕 / 하드필터→가용량 비례 가중).
   §3.8 이 성공했다고 분류한 세 변화 중 **파라미터를 한 번도 안 쓸어본 유일한 것**
7. **흐름 방향 경도** `along`/`cross` — 그룹당 2컬럼 (§3.21)
8. **A-4 격자 가중** (§8.3, 사전 예측 = G3 최대)
9. **하이퍼파라미터 그리드서치** — §3.5 전이율상 **정규화형은 26%** 라 OOF +0.115 를 벌어야
   LB +0.03 임. **마무리용(+0.002~0.003)으로 두고 순위 기대는 접음.**
   단 `max_depth` 를 **올리는** 방향은 규제가 아니라 표현력이므로 예외

**이번 판에서 새로 쓴 스크립트**
`step27_post_fitting.py` (적합 절차 격자탐색) · `step27b_headroom.py` (여지 분해 · 누설 측정) ·
`step27c_bagging.py` (②−③ 진단 + 후처리 배깅) · `step28_ficr_geometry.py` (모양 프리미엄)

---

*(v18 · 2026-07-26)* — 잔차로 고른 실제 행을 처음 열어봄(step20/21). 손실 지도 §3.19 신설.
고풍속 컷아웃 가설 폐기(LDAPS ws10 ≥ 14 m/s 가 3년 41,146행 중 **59행**뿐, GFS 과대예보).
가용률 손실 = 행 15.2% / 손실 19.0% 인데 1위도 못 고치는 공통 손실이라 갭 분해에서 뺌 —
손실의 **81%가 `avail=1.0` 행**에 있고 거기가 1위와 갈리는 곳임. **G3 격차를 풍속 축에서 국소화**
(저풍속에선 3그룹 중 최고, 8~14 m/s 에서만 무너짐. 원인은 기종이 아니라 이질적 단지 구성 + 넓은 공간 범위, §3.20).

> 어투 메모: v18 부터 개조식(-음/-함)으로 통일. v16 이전 문단은 순차 변환 중이라 일부 섞여 있음.

*(v17 · 2026-07-26)* — **밴드 트랙의 후처리 축을 닫았음.** step15/16 이 후처리 이득의
분포를 처음으로 측정했고(§3.14), 격자 해상도를 양방향으로 시험해 전부 기각했음(§3.15).
step12·14·16 이 전부 죽은 이유가 하나로 정리됨: **후처리는 예측값 p 의 함수인데,
이득의 52%가 나오는 저역에서 p 가 무정보다.** 트랙 B 를 §8.2 에서 전면 재작성하고,
정확도 트랙의 A-6(lead time · 블록 구조)을 1순위로 올림.

*(v16 · 2026-07-26)* — v14 리더보드 역전(0.62948) 반영. 밴드 상금의 총량 제약 확정(§3.10),
중첩 후처리 추정을 계측기에서 완전 폐기(§3.6), 리더보드 1위 교체에 따른 갭 분해 재작성(§2, §3.13).

> **이 문서의 규칙**: 모든 주장에 근거 표시를 닮.
> `[측정]` 실제 데이터/실험으로 확인함 · `[가설]` 아직 검증 안 됨 · `[폐기]` 검증 결과 틀렸음
> 이 구분은 2차 발표평가의 소명 요건("사용 데이터와 산출 과정을 설명할 수 있어야 함")에 직결됨.

---

## 1. 대회 개요 및 평가 체계

### 1.1 대회 정보

* **대회명**: 제3회 풍력발전량 예측 AI 경진대회 — BARAM 2026
* **주최/주관**: 한국동서발전, GS E&R, 태백가덕산풍력발전 / 데이콘
* **기간**: 2026-07-06 ~ 08-14 (1차 마감) / 08-17 (2차 산출물 마감)
* **참가 형태**: 개인 1인 (취업준비생 자격)
* **제출**: 1일 최대 5회

### 1.2 평가 산식

$$\text{Score} = 0.5 \times (1 - \text{NMAE}) + 0.5 \times \text{FICR}$$

* 평가 대상: 실제 발전량이 설비용량의 **10% 이상**인 시간대만
* 설비용량: `kpx_group_1` 21,600 kWh · `kpx_group_2` 21,600 kWh · `kpx_group_3` 21,000 kWh
* FICR 단가: 오차율 ≤6% → 4.0원 / ≤8% → 3.0원 / 초과 → 0원, **발전량 가중 합**

### 1.3 산식 분석 — 이 대회의 성격

**[측정] FICR의 한계효용이 NMAE의 33배.**
per-sample 한계효용을 전개하면

$$U_i \propto -e_i + \frac{a_i}{4\bar{Y}} \cdot \text{price}(e_i)$$

평균 발전량 시간대 기준, 오차를 0.09→0.06으로 줄여 밴드에 진입하는 이득이 같은 폭의 MAE 개선보다 **33배** 큼. 즉 이 대회는 MAE 최소화 문제가 아니라 **±6% 밴드 적중률 최대화 문제**다.

**[측정] 10% 미만 구간은 평가에서 제외되므로 저발전 예측에 floor를 거는 것이 엄밀히 우월함.**
평가 대상은 항상 `actual ≥ 0.10·cap`이므로, `|0.13C − a| < a` 가 `a ≥ 0.065C` 에서 항상 성립함. 0을 찍는 것보다 0.13C를 찍는 것이 모든 평가 케이스에서 나음. (piecewise 후처리가 이 값을 자동으로 0.17까지 끌어올림 — 독립적으로 같은 결론에 도달)

**[측정] 밴드 상금의 총량은 정해져 있고, 후처리가 모델보다 그것을 잘 캠.**
v14 에서 목적함수를 밴드 쪽으로 정렬했더니 raw 는 좋아졌으나 후처리가 캘 것이 사라져
총점이 오히려 내려갔음. **밴드는 모델이 아니라 후처리 쪽에서 공략함.** §3.10 참조.

**[측정] 그리고 그 밴드는 아직 포화되지 않았음.** 현 1위의 프리미엄은 +0.0857 로
우리(+0.0668)보다 0.019 높음. §2, §3.13 참조.

---

## 2. 현재 위치와 갭 분해 — 전략의 근거

프리미엄은 `scripts/premium_track.py` 로 계산함. 오차 ~ 반정규(평균=NMAE), 발전량과 독립
가정에서 그 NMAE 가 자연히 함의하는 FICR 을 구하고 실제 FICR 과의 차를 봄.
(검증: v13 +0.0668 / 구 1위 +0.0519 로 기존 수기 기록과 0.0004 이내 일치. 가정이 잘 맞음.)

### 2.1 리더보드 추이

| | NMAE | 1−NMAE | FICR | 함의 FICR | 프리미엄 | Score | 순위 |
|---|---:|---:|---:|---:|---:|---:|---:|
| v6 | 0.1361 | 0.8639 | 0.3939 | 0.3395 | +0.0544 | 0.628930 | 496 |
| v11 | 0.1351 | 0.8649 | 0.4077 | 0.3418 | +0.0660 | 0.636316 | 321 |
| v12 | 0.1351 | 0.8649 | 0.4088 | 0.3419 | +0.0669 | 0.636871 | 291 |
| **v13** | **0.1323** | **0.8677** | **0.4152** | 0.3485 | **+0.0668** | **0.641458** | **189** |
| v14 | 0.1374 | 0.8626 | 0.3964 | 0.3363 | +0.0600 | 0.629475 | — |
| v14-nopost | 0.1385 | 0.8615 | 0.3922 | 0.3340 | +0.0582 | 0.626863 | — |
| 2위 (구 1위) | 0.1131 | 0.8869 | 0.4545 | 0.4026 | +0.0519 | 0.670700 | 2 |
| **1위 (신)** | 0.1206 | 0.8794 | **0.4653** | 0.3796 | **+0.0857** | **0.672315** | 1 |

현재 기준선은 **v13 (0.641458)** 임. v14 는 실패했고 원인은 §3.10 에 있음.

**리더보드가 극도로 조밀함.** v6→v11 은 0.00739 에 175계단, v11→v12 는 0.00055 에 30계단
(약 54계단 / 0.001점). 순위 변동을 성과 신호로 읽으면 안 되고 점수 자체로 봐야 함.

### 2.2 갭 분해 (v13 → 현 1위, 총 갭 0.030856)

산식을 반씩 쪼개면 NMAE 항 0.005844 / FICR 항 0.025013 이지만, **FICR 항의 대부분은
정확도가 자연히 함의하는 몫임.** 그래서 반사실로 분해함.

| 반사실 | 점수 | 남는 갭 | 의미 |
|---|---:|---:|---|
| 정확도만 1위와 같아짐 (프리미엄은 우리 것) | 0.662857 | +0.009458 | **밴드가 가진 몫** |
| 프리미엄만 1위와 같아짐 (정확도는 우리 것) | 0.650916 | +0.021399 | **정확도가 가진 몫** |

대략 **정확도 69% / 밴드 31%.** 프리미엄 차이는 FICR 기준 +0.0189, 총점 기준 +0.0095.

> **정확도가 지배적인 항이지만, 밴드도 포화되지 않았음.**
> 총점으로 0.0095, 순위로 대략 100계단이 밴드 쪽에 남아 있음. 두 트랙을 병행함(§8).

### 2.3 `[폐기]` "밴드는 포화됐음" — 같은 판단을 두 번 틀렸음

**1차 오판 (v11 시점)** "프리미엄이 1등과 동급이라 후처리 여력은 소진됐음" 고 단언했으나,
piecewise 단조 보정이 프리미엄을 +0.0549 → +0.0665 로 올려 당시 1등(+0.0523)을 추월했음.

**2차 오판 (v15 시점)** 위 정정을 근거로 이번엔 "우리가 1등보다 프리미엄이 높으니
이제야말로 밴드는 천장임, 남은 갭은 전부 정확도다" 라고 썼음.
**리더보드 상위 1점만 보고 상한을 단정한 것이었음.**
새 1위가 등장하며 즉시 반증됐음. 그쪽 프리미엄은 **+0.0857** 로 보드 최고이며,
심지어 **2위보다 정확도가 낮은데도(0.8794 vs 0.8869) 밴드로 뒤집어 1위가 됐음.**
정확도 −0.0075 를 내주고 FICR +0.0108 을 벌어 순증 +0.0016.
**v14 가 시도했다가 실패한 바로 그 방향이며, 저쪽은 성공했음.**

> **절차 교훈**: 리더보드 상위 1점으로 어떤 축의 상한도 추정하지 않음.
> 최소 상위 3~5점의 프리미엄 분포를 보고, 새 점수가 올라올 때마다 재계산함.

### 2.4 `[측정]` v6 → v11 개선분은 93.5% 가 FICR 에서 왔음

```
총 +0.00739
  1-NMAE 기여  +0.00048  ( 6.5%)
  FICR   기여  +0.00690  (93.5%)
```

v11 은 네 가지를 한꺼번에 바꿨다(정제 타깃 · LOYO · ES 누설 차단 · piecewise 후처리).
사전 예측은 step2 의 OOF 델타(+0.0062)를 근거로 "0.633~0.636" 이었고 실측 0.63632 로 맞았음.
**그러나 기여 분해는 예측과 반대였음.** 정제 타깃이 NMAE 를 올릴 것으로 봤으나 NMAE 는
0.1361 → 0.1351 로 거의 움직이지 않았고, 점수는 FICR 에서 나왔음.
→ 총량 예측은 '측정된 델타를 옮긴 것' 이라 맞았고, 인과 해석은 틀렸음. §9 기록.

### 2.5 목표 시나리오

프리미엄을 현 수준(+0.0668)으로 유지한 채 NMAE 만 개선했을 때:

| NMAE | → Score |
|---:|---:|
| 0.1351 | 0.63682 |
| 0.1323 | 0.64146 ← 현재 (v13) |
| 0.1300 | 0.64563 |
| 0.1250 | 0.65473 |
| 0.1206 | 0.66286 ← 현 1위의 정확도에 도달 (그래도 0.0095 부족) |
| 0.1200 | 0.66425 |
| 0.1131 | 0.67831 ← 현 2위의 정확도에 도달 |

프리미엄을 현 1위 수준(+0.0857)까지 올리면 위 표의 모든 행에 **+0.0095** 가 더해진다.
즉 NMAE 0.1250 + 프리미엄 0.0857 조합이면 0.6642 로 현 1위와 겨룰 수 있음.

---

## 3. 측정의 신뢰 구간 — 노이즈 바닥

**[측정] 합성 실험(5시드, 2022~2024 학습 / 2025 정답) 결과 연도 단위 점수의 변동은 ±0.013~0.016.**

| 검증 체계 | OOF | 실제 2025 | 격차 |
|---|---:|---:|---:|
| 월 GroupKFold + 검증폴드 ES (기존) | 0.6938±0.0022 | 0.7018±0.0129 | −0.0080±0.0123 |
| 월 GroupKFold + 내부 ES(블록) | 0.6920±0.0026 | 0.7025±0.0126 | −0.0105±0.0120 |
| LOYO + 내부 ES(블록) | 0.6932±0.0019 | 0.7032±0.0144 | −0.0099±0.0137 |

기존 대비 격차 축소: 내부 ES **+0.0024±0.0012**, LOYO **+0.0019±0.0023**

**함의가 큼.** v6~v10의 LB 점수 전체 스프레드가 0.0149인데 노이즈 바닥이 ±0.013임.
→ v6(0.6289) vs v10(0.6185)의 0.010 차이는 **신호라는 근거가 없음.**
→ HPO/시드/가중치 스위프가 만드는 차이는 전부 이 바닥 아래다.

**[측정] 앙상블 가중치 최적화의 실질 이득은 +0.0003±0.0007** (균등가중 대비, 2025 실제 기준). 중첩 가중치도 +0.0000±0.0005. 즉 가중치 방식은 점수가 사는 곳이 아님.

### 3.1 그래서 검증은 이렇게 함

* **LOYO(Leave-One-Year-Out)**: 테스트가 2025년 통째이므로 fold도 연 단위여야 같은 난이도를 재현함
* **연도별 짝비교(paired)**: 연도 난이도 항이 상쇄되어 감도가 15배 올라감
  (같은 비교를 짝 안 지으면 ±0.018, 짝 지으면 ±0.0012)
* **판정 기준**: 모든 연도에서 부호 일치 ∧ |평균차| > 연도별 차이의 표준편차 → 신호. 아니면 제출하지 않음
* **그룹별 분해**: 그룹마다 효과가 갈릴 수 있는 실험은 반드시 그룹 단위로 쪼개서 판정함 (§3.7)
* **[측정] LOYO 는 타당함** — step6 표류 진단에서 학습원을 바꿔가며 예측했을 때
  거리 효과 −0.0043, 방향 효과 −0.0017 로 작았고, 2023 년은 어느 학습원을 쓰든 일관되게
  어려웠다(표준편차 0.0093 으로 최저). 연도 간 기상 변동은 노이즈가 아니라 재현해야 할 난이도다.

---

### 3.2 `[측정]` 트리 모델은 단일 피처의 단조변환에 불변임 — 피처 설계의 제1원칙

배치1(물리 파생 피처 53개)이 실패한 뒤 합성 실험으로 원인을 확인했음.
풍속 v 하나와 잡음 20개로 파워커브 타깃을 학습시키고 파생 피처를 하나씩 더했음.

| 구성 | MAE(kWh) | 기준 대비 |
|---|---:|---:|
| ① 풍속만 | 559.7 | +0.0 |
| ② + v³ | 559.7 | **+0.0** |
| ③ + 파워커브 pc(v) | 560.1 | **+0.4** |
| ④ + 밀도보정 v×0.99 | 559.7 | **+0.0** |
| ⑤ + 전부 | 560.1 | +0.4 |

트리는 피처를 값이 아니라 **순서**로만 씀. 단조변환은 분할점 위치만 바꿀 뿐
같은 분할을 만들 수 있어 정보가 늘지 않음. pc(v) 는 오히려 악화됐는데,
컷아웃 구간을 제외하면 사실상 노이즈 컬럼 하나를 더 준 셈이기 때문임.

반면 이런 것은 실제로 이득이 있음:

| 구성 | MAE | 대비 |
|---|---:|---:|
| v, z 따로 | 387.7 | +0.0 |
| + 상호작용 pc(v)×z | 385.7 | **−2.0** |
| + 선형결합 0.6v+0.4z | 384.7 | **−3.1** |

**실데이터가 같은 말을 함.** v6 파생 피처 중 폴드 상위 30에 생존한 것은
`wpd_ldaps_10m`(ρ×ws³)과 `ldaps_blh_v117_interaction`(v/blh) 둘뿐이며 **둘 다 상호작용**임.
푄현상·열돔·착빙·커테일먼트·히스테리시스·능선분해는 전부 탈락했음.

> **피처 설계 규칙**: 새 피처를 만들기 전에 "이건 기존 컬럼 하나의 단조함수인가?"를 묻는다.
> 그렇다면 만들지 않음. 상호작용 · 선형결합 · 비단조변환 · 외부정보만 가치가 있음.

**`[측정, v17]` 이 규칙을 또 놓쳤다 — `lead_hours` 사례 (§3.18).**
예보 블록은 D일 13시 공개 → D+1일 01시 ~ D+2일 00시다. 따라서
`lead_hours = hour + 11` (hour ∈ 1..23), `hour = 0` 이면 35 다.
즉 **lead_hours 는 이미 있는 `hour` 컬럼의 일대일 재라벨링**이고 트리에게 새 정보가 0 임.
step18 실측 주효과 **+0.0001**. `lead_frac` 과 `blk_pos` 도 같은 이유로 공허함.
로드맵에 "12~35h 축이 아직 없음" 고 써 두었던 것이 오진이었음 — **축은 있었고 `hour` 로 인코딩돼 있었음.**
> **보강 규칙**: "없던 축" 이라고 쓰기 전에 그것이 기존 컬럼의 결정론적 함수가 아닌지
> 먼저 **수식으로** 확인함. 데이터 명세 한 번만 읽으면 알 수 있었음.

### 3.3 `[측정]` 피처 희석은 실재함 — 812 → 200

전체(812) 대비 폴드별 상위 K개 재학습, 연도별 짝비교:

| K | 차이 | 표준편차 | 판정 |
|---|---:|---:|---|
| 400 | +0.0034 | 0.0007 | ✅ |
| **200** | **+0.0021** | **0.0001** | ✅ 연도별 +0.0019/+0.0022/+0.0021 |
| 120 | +0.0033 | 0.0021 | ✅ |
| 60 | +0.0042 | 0.0055 | ⚠️ 2023 부호 반전 |
| 30 | −0.0005 | 0.0064 | ⚠️ |

gain 이 0 이 아닌 피처가 700~727개라 top30 까지 자르면 실제 정보 손실이 난다.
**선택 누설 차단**: 전체 데이터로 피처를 고르면 검증폴드를 본 것이 되므로,
각 LOYO 폴드의 학습셋 안에서만 중요도를 구하고 폴드마다 다른 상위 K를 씀.

### 3.4 `[측정]` 풍속 추정 진단과 격자 구조

허브고도 풍속 후보의 실제 발전량 순위상관(스피어만, 26,304행):

| 추정 | 평균 |
|---|---:|
| ldaps_ws10 | 0.7618 |
| ldaps_ws50 | 0.7581 |
| ldaps_v117 | 0.7493 |
| gfs_v117_powerlaw (현행) | 0.6888 |
| gfs_v117_3pt | 0.6887 |

* **LDAPS ≫ GFS** (0.749 vs 0.689). GFS 0.25° 격자가 해발 1,000m 능선의 지형 가속을 못 잡음.
* **외삽할수록 나빠짐**. 예보값 두 개의 비율로 만든 alpha 가 신호가 아니라 노이즈를 넣는다.
* GFS 3점 시어는 기존 2점과 소수점 넷째 자리까지 동일 → 무가치.

폴드 상위 30 생존 피처에서:
1. **전부 U성분(동서)**. V성분이 하나도 없음. 능선이 대체로 남북 방향이라 직교 성분이 지배적.
2. **개별 격자 > `_mean_`** (`gfs_g5_100u` 12/12 vs `gfs_mean_100u` 10/12). 균등평균이 정보를 죽인다.
   단 최상위 `ldaps_g13` 은 거리 기반 IDW 에서 최하위권이었음 → **격자 가중은 거리가 아니라
   데이터가 고른 것으로 가야 함.** (§4.4 의 IDW 가중치는 가설이며 반증됨)
3. `surface_0_dswrf` 가 7개 격자에서 반복 등장 → 주야/안정도 대리변수. EDA 의 야간 하층제트와 일치.

### 3.5 `[측정]` 변화의 종류가 전이율을 결정함

v11→v12 와 v12→v13 은 **깨끗한 단일 변경**임.

| 변경 | 종류 | raw OOF | 중첩 후처리 OOF | **LB** | raw 기준 전이율 |
|---|---|---:|---:|---:|---:|
| v12: `--top-k 200` | 정규화형 | +0.0021 | +0.0040 | +0.00055 | **26%** |
| v13: `--joint-groups` | **정보형** | +0.0058 | **−0.0035** | **+0.0046** | **79%** |
| v14: 목적함수·가중 | **밴드형** (신설) | +0.0255 | +0.0105 | **−0.0120** | 아래 참조 |

* **정규화형 변화**(가지치기, 규제 강화, 피처 축소) → LOYO 가 과대평가함.
  LOYO 는 2년치로 학습하지만 최종 모델은 3년치로 학습하므로,
  '표본이 적을 때 커지는' 이득은 배포하면 줄어듦.
* **정보형 변화**(새 데이터원, 없던 컬럼, 구조 변경) → 전이가 잘 됨.
* **[신설] 밴드형 변화**(목적함수·표본가중을 FICR 쪽으로 미는 것) → **후처리와 상금을 나눠 갖는다.**
  raw 자체는 정상 전이했으나(+0.0255 → +0.0090, 35%) 후처리 여지를 −0.0210 잃어
  순증이 음수가 됐음. v14 가 그 사례임. §3.10.

**실험 설계 규칙**: 새 실험 전에 세 가지를 묻는다.
(1) 모델에 없던 정보를 주는가? (2) 기존 정보를 다르게 다루는가? (3) **밴드를 겨냥하는가?**
(3)이면 후처리 여지 감소분을 반드시 함께 추정함.

### 3.6 `[폐기]` 중첩 후처리 추정은 계측기가 아님

이전 판에서는 "부호를 틀린 적이 있으니 보조 지표로만 씀" 였음. **이제 보조로도 쓰지 않음.**

**증거 1 — 부호를 틀렸음 (v13).**

```
v12 → v13    raw OOF  +0.0058   중첩 후처리 OOF  −0.0035   LB  +0.0046
```

step5b 는 후처리 후 +0.0005(⚠️ 노이즈)로 판정해 "제출 낭비 말 것" 을 띄웠으나,
실제 LB 는 +0.0046 으로 v11 이후 최대 단일 개선이었음.

**증거 2 — 같은 값을 네 번 재서 네 번 다르게 답했음 (v14 의 후처리 여지).**

| 출처 | 값 |
|---|---:|
| step10 PART 2 (step9 OOF) | +0.0017 |
| step11 w=1.0 (실제 v14 OOF) | +0.0101 |
| train.py 로그 (Val_Total − Raw OOF) | +0.0105 |
| **실제 LB (v14 − v14-nopost)** | **+0.0026** |

정답은 +0.0026 인데 셋 중 둘이 4배 부풀었고, 부풀리는 방향도 일정하지 않음.

**원인은 구조적임.** 후처리 파라미터는 그룹당 절점 6개 + 임계값 = 7개로 자유도가 큰데,
중첩 절차는 그것을 2년으로 적합해 1년에 적용하는 것을 3번 반복함. 검정력이 근본적으로 낮음.
실제 배포는 3년 전체로 적합하므로 절차 자체도 다름.

**운용 규칙 (강제)**

1. 제출 여부를 **중첩 후처리 점수로 판정하지 않음.** raw OOF 로만 판정함.
2. raw OOF 차이는 **연도짝비교 + 그룹별 분해**로 판정함 (부호일치 ∧ |평균| > 표준편차).
3. LB 기대치는 raw OOF 차이 × **전이율**로 추정함.
   현재 관측된 전이율: 정규화형 26% / 정보형 79% / 밴드형 raw 35%.
4. **후처리 여지는 LB 로만 측정 가능함.** `--no-post` 제출 1회가 유일한 계측기다.
5. 예외 — *같은 모델의 같은 OOF 위에서 후처리 방법 A vs B* 는 짝비교라 분산이 훨씬 작음.
   그 범위에서만 참고하고, 최종 판정은 LB 로 함.
6. **판정 규칙에는 반드시 부호 방향을 명시함.** `is_difference_real` 은 '차이가 실재하는가'
   만 답하지 '좋아졌는가' 는 답하지 않음. step16 에서 신규 절점이 **3/3 부호일치 ∧
   |평균|>표준편차** 로 ✅ 가 떴는데 방향이 −0.0015 였음. 사전 등록 문구에 방향이 없어
   규칙만 따르면 제출로 갈 뻔했음. 앞으로 채택 조건은 항상
   `부호일치 ∧ |평균|>표준편차 ∧ **부호가 양수** ∧ 그룹 2개 이상 양수` 네 개로 씀.

### 3.7 `[측정]` kpx_group_3 격차와 3그룹 통합 — 해결됨

step4 폴드별 점수(후처리 fit 기준):

| 그룹 | 점수 |
|---|---:|
| kpx_group_1 | ≈ 0.635 |
| kpx_group_2 | ≈ 0.660 |
| **kpx_group_3** | **≈ 0.588** |

최종 점수는 세 그룹 평균이므로 **G3 를 G1 수준까지만 올려도 총점 +0.016** 임.

G3 가 약한 구조적 이유:

| 원인 | 수치 | 고칠 수 있나 |
|---|---|---|
| 라벨이 2년치뿐 | 17,538행 (G1/G2 는 26,200행) | **가능** — 3그룹 통합 학습 |
| 가용률 100% 시간 비율 | **56.3%** (G1 85.8%, G2 88.5%) | 부분적 (정제 타깃으로 일부 완화됨) |
| 터빈 5기 — 1기 정지 시 20% 손실 | 분산이 구조적으로 큼 | 불가 |
| 기종이 다름 (U136 vs V126) | 파워커브 상이 | 불가 |

**[해결]** long-format 통합 학습(행 = 시각 × 그룹, 그룹 one-hot + 정적 스펙)을
G1·G3 에만 부분 적용해 v13 에서 **LB +0.0046** 을 얻었다.

통합(B) − 독립(A) 를 그룹별·연도별로 분해하면 부호가 완벽히 일관됨:

| 그룹 | 2022 | 2023 | 2024 | 평균 | 판정 |
|---|---:|---:|---:|---:|---|
| G1 | +0.0021 | +0.0072 | +0.0045 | +0.0046 | 3/3 양수 → 통합 채택 |
| G2 | −0.0104 | −0.0023 | −0.0028 | −0.0052 | 3/3 음수 → **독립 유지** |
| G3 | 라벨없음 | +0.0101 | +0.0270 | +0.0185 | 2/2 양수 → 통합 채택 |

borrowed strength 의 표준 패턴임. **데이터가 적은 쪽(G3)이 이득, 많은 쪽(G2)이 손해.**
G2 를 뺀 것은 사후 선택이 아니라 '라벨이 33% 적은 그룹만 borrowing 이득을 봄' 는
사전 예측 가능한 방향임.

**부수 발견 — 그룹별 짝비교가 총점 짝비교보다 강력함.**
위 표는 그룹별로 부호가 완벽한데 총점으로 합치면 G2 손실이 상쇄해 노이즈 판정이 났음.

**`[가설]` G3 는 밴드 쪽 레버에도 가장 민감함.**
step11 혼합 스윕의 그룹별 표(후처리 후 3년 평균):

| w | G1 | G2 | G3 |
|---:|---:|---:|---:|
| 0.00 (Tweedie) | 0.6247 | 0.6536 | 0.5707 |
| 0.75 | 0.6356 | 0.6587 | **0.5992** |
| 1.00 (MAE+W) | 0.6344 | 0.6584 | 0.5971 |

G3 만 +0.0285 이고, 세 그룹 모두 w=0.75 가 w=1.00 보다 나음.
다만 이 표는 §3.6 이 폐기한 계측기로 잰 값이므로 **[가설] 로만 남김.**
밴드 레버가 통한다면 통하는 지점은 G3 일 가능성이 높음. LB 확인 전까지 근거로 쓰지 않음.

### 3.8 `[폐기]` 2단계 SCADA 풍속 스태킹

**설계** 1단: NWP 피처 → SCADA 실측 풍속(`ws_mean`, `ws_std`, `ws_disp`) 예측.
2단: 그 OOF 예측을 피처로 추가해 발전량 예측.

**1단 성능** `ws_mean` OOF 상관 0.8764 / MAE 1.334 m/s, `ws_std` 0.3964, `ws_disp` 0.6467.
`ws_mean` 은 충분히 잘 맞았음. 그런데도 2단이 실패했음.

**2단 결과** A 0.5995 → B 0.5977 (**−0.0019**), G1 −0.0058 (0/3 연도).

**원인 (구조적, 재시도해도 같음)**
1단의 입력과 2단의 입력이 **같은 집합**임. 따라서 `pred_ws` 는 2단 모델이 이미 갖고 있는
피처들의 함수일 뿐이고, 트리에게는 새 정보가 0 임. 오히려 피처 1개를 희석에 보탠다.
SCADA 의 진짜 정보는 *NWP 로 설명되지 않는 잔차* 인데, 1단 학습이 정확히 그 잔차를 버림.

**일반화** 스태킹은 1단과 2단이 서로 다른 정보를 볼 때만 성립함.
같은 입력을 쓰는 모델 스태킹(XGB/LGBM/CatBoost 메타러너 포함)도 같은 병에 걸림.

**따름 정리** NWP 로부터 유도 가능한 모든 양은 이미 모델이 갖고 있음.
성공한 변화(정제 타깃·통합 학습·가지치기)는 전부 **학습 문제를 바꾼 것**이었고,
실패한 변화(물리 피처·SCADA 스태킹)는 전부 **NWP 유도량을 다시 계산한 것**이었음.

### 3.9 `[폐기]` 2023년 난이도의 램프 가설

**배경** step6 에서 2023 년이 어느 학습원을 쓰든 일관되게 어려웠다(표준편차 0.0093 으로 최저).
기상청 강원 연 기후 특성에서 2023 년 기온 변동성이 확인됐음
(11월 6.53℃, 12월 6.55℃; 겨울 평균 2022=3.73 / 2023=6.17 / 2024=4.07).
가설: 기온 변동성 → 대기 안정도 급변 → 램프 증가 → 예측 난이도 상승.

**결과: 기각.**
* 겨울 점수 갭 **−0.0018**, 비겨울 **−0.0179**. 방향이 반대다. 겨울이 문제가 아님.
* 11월은 2023 년이 오히려 **+0.0483 더 좋았다.**
* 최악의 달은 2월 / 5월 / 4월.
* 램프 5분위 점수 0.5847 / 0.5911 / 0.5892 / 0.5910 / 0.5747 — 비단조, 전체 폭 0.0100.

**규칙 준수 메모** 연 기후 특성 PDF 는 연말 이후 발간됨(2023년판 → 2024-01-16).
따라서 **피처로 쓸 수 없음.** 가설 생성 용도로만 씀.

### 3.10 `[측정]` 목적함수 정렬과 v14 역전 — 밴드는 총량이 정해진 상금임

#### 실험 (step9, 2×2 요인설계)

목적함수 T=`reg:tweedie(1.5)` / L=`reg:absoluteerror`, 가중 U=균일 / W=산식정합.
가중은 산식 구조를 그대로 이식했음. 채점행(actual ≥ 0.10·cap)에 `0.5 + 0.5·a/ā`,
채점 제외행에 0.2, 평균 1 정규화. 채점행 비율은 G1 60.7% / G2 60.7% / G3 53.7%.

| 조건 | G1 | G2 | G3 | 평균 | 1−NMAE | FICR |
|---|---:|---:|---:|---:|---:|---:|
| A T+U (v13) | 0.6014 | 0.6076 | 0.5760 | 0.5950 | 0.8627 | 0.3273 |
| B T+W | 0.6191 | 0.6213 | 0.5844 | 0.6083 | 0.8646 | 0.3520 |
| C L+U | 0.6149 | 0.6313 | 0.5844 | 0.6102 | 0.8642 | 0.3562 |
| D L+W | 0.6272 | 0.6442 | 0.5902 | **0.6205** | 0.8649 | 0.3762 |

주효과 목적함수(L−T) **+0.0137**, 가중(W−U) **+0.0118**, 상호작용 −0.0015(≈0, 가산적).
D−A 연도짝비교 +0.0260 [2022 +0.0191 / 2023 +0.0274 / 2024 +0.0316], 3/3 일관.
그룹×연도 8칸 중 7칸 양수. **여태 잰 것 중 가장 크고 일관된 raw 효과였음.**

#### Tweedie 선택의 재검토

v6 에서 Tweedie 를 고른 근거는 원본 라벨의 0점 질량(G1 11.45% / G2 11.42% / G3 16.23%)이었고
그 시점에는 옳았다. 이후 두 가지가 바뀌었음.
* v11 부터 학습 타깃이 정제 타깃(cf×21600)임. G3 의 0점 비율이 혼자 높았던 것은
  가동률 문제였고(§4.3: G3 가 100% 가동인 시간이 56.3%), 정제 타깃이 그것을 걷어냈음.
* 0점 질량은 전부 10%cap 선 아래, 즉 **채점 대상 밖**임.
  Tweedie 는 점수에 들어가지 않는 영역의 분포적합을 하고 있었음.

#### 이득의 정체 — 96%가 FICR

D−A 에서 NMAE 기여 +0.0011, FICR 기여 +0.0245.
MAE 로 바꾸면 NMAE 가 좋아질 것으로 예측했으나 **틀렸음.** 좋아진 것은 FICR 임.

기전: MAE 의 최적 예측은 조건부 **중앙값**이고, 우편향 분포에서 중앙값은 평균보다
최빈값에 가깝다. FICR 은 오차 6%/8% 계단함수라 평균 정확도가 아니라 **최빈 정확도**를 보상함.
가중도 같은 방향임. B·C 도 이득의 대부분이 FICR 이었음.
**즉 step9 의 결과는 전부 밴드 결과였음.**

#### LB 역전과 그 분해

```
v13  0.641458   v14  0.629475   차이 -0.011983
내역  NMAE -0.0051 / FICR -0.0189   (FICR 이 3.7배 크게 깨짐)
```

`--no-post` 제출로 후처리를 분리했음.

```
v14 (후처리)      0.629475
v14 (후처리 없음)  0.626863     ->  v14 의 후처리 여지 = +0.002612
```

step10 PART 2 가 잰 조건별 후처리 여지:

| 조건 | 여지 | 연도별 |
|---|---:|---|
| A (Tweedie) | **+0.0236** | +0.0171 / +0.0241 / +0.0295 (3/3, std 0.0062) |
| D (MAE+W) | **+0.0017** | −0.0023 / +0.0061 / +0.0014 (std 0.0042) |

이 둘로 LB 를 역산하면

```
LB(v14) - LB(v13) = (raw14 - raw13) - (여지A - 여지D)
      -0.0120     = (raw14 - raw13) - (0.0236 - 0.0026)
   => raw14 - raw13 = +0.0090
```

> **결론: MAE+W 모델은 2025 에서도 raw 로는 실제로 더 좋았다 (+0.0090).**
> OOF raw 차이 +0.0255 → LB +0.0090, 전이율 35%. 전이 자체는 정상이었음.
>
> v14 가 진 이유는 모델이 나빠서가 아님. **후처리는 약 +0.024 짜리 정해진 상금인데,
> MAE 모델이 그 상금을 스스로 +0.0090 만 캐고 나머지 +0.0146 을 후처리가 못 캐게 막았다.
> 후처리가 모델보다 밴드를 더 잘 캠.**

증거 하나 더: D 의 raw FICR 0.3762 는 A 의 **후처리 후** FICR 0.3759 와 같음.
모델이 후처리의 일을 대신하기 시작한 지점이 정확히 거기다.

#### 도출되는 원칙

> **밴드를 겨냥한 모델 개선은 후처리와 같은 상금을 두고 겹침. 합산되지 않음.**
> 개선안을 평가할 때 "이것은 정확도를 올리는가, 밴드를 올리는가" 를 먼저 판별함.
> 밴드 쪽이면 후처리 여지 감소분을 함께 추정해야 하며, 공략은 **후처리 쪽에서** 함.

누적 후처리 흡수 관측: 정제타깃 51% / 배치1 80% / D(v13) 84% / G(v13) 91% / v14 79%.

### 3.11 `[측정]` 폴드 평균은 MAE 에서 오히려 이득 — 배깅이 맞는 앙상블 방식

**가설(기각됨)** OOF 는 행마다 모델 1개의 예측인데 `inference.py` 는 폴드 모델 3개를 평균함.
MAE 모델의 이득은 예측을 밴드로 모으는 데서 나오므로 평균이 그 집중을 풀어 손해일 것임.

**측정 (step10 PART 1, 중첩 LOYO)** 바깥 연도를 가짜 2025 로 두고 나머지 2년으로
연도별 모델 2개를 만들어 단일 vs 평균을 비교했음.

| 조건 | Δ 총점 | Δ FICR | 양수/전체 |
|---|---:|---:|---:|
| A T+U | −0.0005 | −0.0048 | 2/6 |
| D L+W | **+0.0049** | **+0.0068** | **6/6** |

**정반대였음.** MAE 가 평균에서 더 이득을 봄.

**올바른 기전** `reg:absoluteerror` 의 잎 값은 중앙값이고, 중앙값은 평균보다 **분산이 큰 통계량**임.
단일 MAE 모델은 그만큼 노이즈가 많고, 평균이 그 분산을 깎음. Tweedie 는 이미 매끄러워
평균의 이득이 작음. 배깅이 고분산 추정량에서 더 크게 작동한다는 표준 결과임.

**따름 정리 — 앙상블 방식의 확정**
* **배깅(단순 평균)**: 채택. 이 데이터셋에서 실측으로 이득이 확인된 유일한 앙상블 형태.
* **스태킹**: 폐기. §3.8 의 이유로 같은 입력을 쓰는 한 정보가 0.
* **가중 최적화**: 폐기. 균등가중 대비 +0.0003±0.0007.
* **다양성의 출처**: 알고리즘이 아니라 **학습 문제**(목적함수·가중·피처집합)에서 뽑음.
  단 밴드 쪽 다양성은 후처리와 겹치므로(§3.10) 정확도 다양성만 취함.

### 3.12 `[보류]` Tweedie×MAE 혼합

**착상** MAE 는 raw 가 좋고 후처리 여지가 없음. Tweedie 는 그 반대다.
섞으면 raw 는 Tweedie 보다 좋으면서 후처리 여지가 남지 않을까.

**step11 스윕** (학습 없음, 저장된 OOF 만 사용)

| w | raw | 후처리 후 | 여지 |
|---:|---:|---:|---:|
| 0.00 (v13) | 0.6026 | 0.6229 | 0.0202 |
| 0.30 | 0.6136 | 0.6268 | 0.0132 |
| 0.60 | 0.6191 | 0.6321 | 0.0131 |
| 0.75 | 0.6207 | **0.6353** | 0.0146 |
| 0.90 | 0.6229 | 0.6350 | 0.0122 |
| 1.00 (v14) | 0.6239 | 0.6340 | 0.0101 |

nested 판정은 +0.0113 (3/3 부호일치, std 0.0066) 으로 ✅ 가 떴음. **그러나 채택하지 않음.**

**보류 사유 1 — 지표가 정답을 아는 지점에서 부호를 틀린다.**
step11 은 w=0 보다 w=1 이 **+0.0111 나음**고 말함. 실제 LB 는 w=1 이 **−0.0120 나쁨**.
크기로 0.023 틀렸고 방향도 반대다. 양 끝점의 순서를 거꾸로 매기는 자로
그 사이의 내부점을 판정할 수 없음.

**보류 사유 2 — 내부 최적점이 실재하지 않음.**
w = 0.75 / 0.90 / 1.00 이 0.6353 / 0.6350 / 0.6340. 차이 0.0013 은 노이즈 안임.
봉우리가 아니라 w ≥ 0.6 의 평평한 고원이며, 실질 주장은 "w 가 클수록 좋음" 다.
그것이 바로 LB 가 반증한 명제다.

**보류 사유 3 — 지표가 §3.6 에서 폐기된 그 계측기다.**
step11 이 쓴 v14 후처리 여지(+0.0101)는 실제값(+0.0026)의 4배다.

**살아남는 것** raw 열은 w 에 대해 단조 증가하고(0.6026 → 0.6239), raw 는 전이가 확인됐음.
**혼합의 raw 이득은 실재함.** 미해결은 후처리와의 상호작용뿐이고 그건 OOF 로 못 잼.
`v13 --no-post` 로 전이 모델을 2점으로 만든 뒤 재판정함 (§8.1).

### 3.13 `[측정]` 리더보드는 정확도–밴드 프런티어다

현 1위와 2위는 **정반대 전략**으로 거의 같은 총점에 도달해 있음.

| | 1−NMAE | FICR | 프리미엄 | 총점 |
|---|---:|---:|---:|---:|
| 1위 | 0.8794 | **0.4653** | **+0.0857** | 0.672315 |
| 2위 | **0.8869** | 0.4545 | +0.0519 | 0.670700 |
| v13 | 0.8677 | 0.4152 | +0.0668 | 0.641458 |

2위는 정확도로, 1위는 밴드로 왔음. 우리는 둘 다 중간임.
이는 이 대회 산식에 **두 개의 독립적인 승리 경로**가 있다는 뜻이며,
둘 다 아직 우리에게 열려 있다는 뜻이기도 함.

#### 왜 우리 후처리는 +0.0857 까지 못 가는가 — 구조적 한계

우리 후처리는 **전역 단조 변환**임. 모든 행에 같은 매핑을 적용함.
그런데 밴드의 최적 행동은 행마다 다름.

* 예측 불확실성이 6%·cap (G1 기준 1,296 kWh) 보다 작은 행:
  최빈값 쪽으로 밀면 밴드에 들어갈 확률이 높음. **밀어야 이득.**
* 불확실성이 그보다 훨씬 큰 행:
  밀어도 밴드에 못 들어감. NMAE 만 버림. **밀면 순손실.**

전역 변환은 이 둘을 구분하지 못하고 **밴드를 살 수 없는 행에서도 밴드 값을 지불함.**

#### 쓰지 않고 버리는 신호가 이미 있음

`inference.py` 는 폴드 모델 3개를 평균하면서 **그 산포(std)를 버림.**
이것은 행별 예측 불확실성의 자연스러운 대용치임. 외부 데이터가 아니므로 규칙 검토도 필요 없고,
이미 계산되어 있음.

**`[폐기]` 조건부 후처리** — 전역 변환의 강도를 행별 산포로 변조하는 안이었음.
step12 가 반대 부호를 냈고 step15 가 그것을 예측 수준 교란으로 밝혔음. §3.14 참조.
**단 위의 '왜 전역 변환은 +0.0857 까지 못 가는가' 진단 자체는 살아 있음.**
답이 '행별 산포로 변조' 가 아니었을 뿐임. §3.14 가 답을 좁힘.

### 3.14 `[측정]` 후처리 이득은 저역 5%에 몰려 있고, 그 저역에서 예측값은 무정보다

밴드 실험 세 개가 연달아 죽었음. 셋의 공통점을 찾기 위해 **후처리 이득의 분포**를
처음으로 측정했음(step15). 그때까지 우리는 이득의 **총량**만 재고 있었음.

#### 측정 — 예측 수준 3분위 x 산포 3분위, 칸별 Δ = score(후처리) − score(raw)

| 예측수준 | S1 최저산포 | S2 | S3 최고산포 | 채점행 비중 | 이득 몫 |
|---|---:|---:|---:|---:|---:|
| **L1 최저** | **+0.3492** | **+0.3259** | **+0.2593** | **5.3%** | **51.7%** |
| L2 | +0.0161 | +0.0130 | +0.0233 | 38.7% | 28.8% |
| L3 최고 | −0.0098 | +0.0125 | +0.0301 | 56.0% | 19.5% |

**채점행의 5.3%가 후처리 상금의 절반 이상을 나른다.** L3 최저산포 칸은 음수다 —
확신하는 고출력 행에서는 후처리가 오히려 손해다.

#### 그 저역에서 최적 행동은 '상수' 다

현행 절점은 `KNOTS = [0, 0.15, 0.35, 0.60, 0.85, 1.00]` 이고 적합 결과가
`out_knots = [0.17, 0.206, 0.331, 0.552, 0.759, 0.988]` 임. 예측 분포가 0 쪽으로
치우쳐 L1 이 통째로 `[0, 0.15]` 한 구간에 들어가므로, 그 구간의 사상은
`p∈[0,0.15] → 0.17~0.206` 으로 **사실상 상수**다.
§3.15 가 그 구간의 자유도를 5배로 늘려 보고 4·3 매듭으로 줄여 봤는데 **전부 총점이 내려갔음.**

> **최적 행동이 상수라는 것은 그 구간에서 p 가 정보를 담고 있지 않다는 뜻임.**
> 애초에 p 가 정보를 담았다면 그렇게 낮게 예측하고 틀리지 않았음.
> 이 행들은 컷인 무릎(P ∝ v³ 이라 풍속 오차가 발전량 오차로 최대 증폭되는 구간)이고,
> EDA 셀 10 의 '2022 결측 96%가 cut-in 미달 저풍속' 과 같은 영역임.

#### 그래서 세 실험이 왜 죽었는지가 한 문장으로 정리됨

| 실험 | 무엇을 바꿨나 | 결과 |
|---|---|---|
| step12 | 후처리 강도를 행별 산포로 변조 | 수준 교란. 폐기 |
| step14 | 시각·계절 버킷별 배율 | m = 1.000. 폐기 |
| step16 | 저역 절점 증설/축소 | 5개 변형 전부 하락 |

**셋 다 예측값 p 의 함수 공간 안에서만 움직였음.** 후처리는 정의상 p 의 함수이고,
이득의 52%가 나오는 L1 에서 p 는 무정보다.
=> **밴드 트랙은 후처리 축에서 포화다. 이것은 '다 해봤음' 가 아니라 원리적 한계임.**

#### 남는 길 — p 가 아니라 다른 타깃

채점 필터가 `a >= 0.10cap` 이므로 저역에서 점수를 최대화하는 점예측은
`E[a | x]`(현행 회귀가 주는 것)가 아니라 **`E[a | a >= 0.10cap, x]`** 다.
§1.3 의 floor 우월성 증명이 정확히 이 이야기이고, 현행은 그 값을 **전역 상수 0.17cap 하나**로
근사하고 있음. x 로 조건화하면 남음.

§3.8 의 따름정리("NWP 로 유도 가능한 것은 이미 모델이 갖고 있음")에 걸리지 않음.
스태킹은 **같은 타깃**을 재계산했지만 이것은 **다른 타깃**임. 같은 p 를 가진 두 행이
서로 다른 `E[a | a>=0.10cap, x]` 를 가질 수 있고, 트리 회귀는 그 차이를 점추정 하나로
압축해 버림. p 의 함수인 후처리는 원리적으로 되살릴 수 없음.
§3.10 의 흡수 문제도 피함 — 후처리가 아예 갖고 있지 않은 축임.
=> `scripts/step17_scored_conditional.py`. §8.2 B-2.

### 3.15 `[측정]` 후처리 격자 해상도는 양방향으로 닫혔다

같은 v13 OOF 위에서 절점 격자만 갈아끼우고 LOYO 중첩 짝비교(§3.6 규칙 5 범위).

| 격자 | L1 칸 Δ | L2 칸 Δ | L3 칸 Δ | 총점 Δ | 연도 |
|---|---:|---:|---:|---:|---|
| **6 현행** `[0,.15,.35,.60,.85,1]` | +0.2622 | +0.0201 | +0.0094 | — | — |
| 11 증설 (저역 5매듭) | +0.2489 | +0.0178 | +0.0088 | **−0.0015** | 3/3 음수 |
| 5 `[0,.15,.35,.60,1]` | +0.2636 | +0.0191 | +0.0081 | −0.0011 | 부호 갈림 |
| 4 `[0,.15,.50,1]` | **+0.2670** | +0.0184 | +0.0077 | **−0.0018** | 3/3 음수 |
| 3 `[0,.20,1]` | +0.2609 | +0.0178 | +0.0091 | −0.0015 | 부호 갈림 |

**변형 5개 전부 총점 하락.** 현행 6매듭이 국소 최적임.

**`[정정]` 처음 해석은 틀렸음.** step15 를 보고 "이득의 100%가 나오는 L1 에 자유도가
2개뿐이니 늘리면 됨" 고 썼으나, 늘려도(11) 줄여도(4) L1 칸 Δ 는 거의 안 움직인다.
오히려 4매듭이 L1 최고값을 냈음. **총점을 깎은 것은 전부 L2·L3 다.**

> L1 은 자유도가 필요 없음 — 상수가 최적이라 해상도와 무관함.
> **격자 해상도의 한계 감도는 행의 95%가 있는 L2·L3 에 있고, 거기서 현행이 이미 최적임.**

**`[관측, 쫓지 않음]`** 축소 격자 3개 전부에서 G3 만 양수였음(+0.0005 / +0.0012 / +0.0005).
라벨이 2년뿐이라 파라미터가 적은 쪽이 유리한 것으로 보임. 다만 총점 기여가 +0.0004 로
노이즈 바닥 한참 아래이고 3개 중 사후선택이므로 근거로 쓰지 않음.

### 3.16 `[폐기]` '후처리 후 평균 부호편향' 은 진단 지표가 아님

step14 PART 1 은 후처리를 통과한 뒤에도 시각·계절 버킷마다 +3~+9%cap 의 부호 있는
편향이 남는 것을 보고 "구조적 편향이 있음 => 캘 것이 있음" 로 판정했음. **거짓 양성임.**

편향을 **실제값 구간별로** 쪼개면 정체가 드러난다(step15 PART 2).

| 그룹 | a 0.10~0.20 | 0.20~0.35 | 0.35~0.60 | 0.60~1.10 |
|---|---:|---:|---:|---:|
| G1 | +11.21 | +8.34 | +4.31 | **−5.15** |
| G2 | +12.13 | +10.48 | +7.71 | **−1.99** |
| G3 | +12.68 | +12.73 | +9.70 | **−4.31** |

저출력 +, 고출력 − 의 완벽한 대칭. 이것은 **실제값으로 조건을 걸면 오차가 있는 어떤
예측기에서든 나오는 회귀-평균 인공물**임. 여기에 `0.17cap` floor 가 겹침 —
`a` 가 0.10~0.17cap 인 채점행에서는 **정의상** `p − a > 0` 이고, §1.3 에 따라 그것이
점수 최적 행동임. step14 의 대조군(예측 5분위 −2.40 ~ +8.08)이 0 근처가 아니었던 것이
이미 경고였는데 읽지 못했음.

> **규칙**: 잔여 편향은 진단 지표로 쓰지 않음. 모델을 아무리 고쳐도 이 패턴은 사라지지 않음.
> 밴드 진단은 **밴드 적중률**(오차 ≤6% / ≤8% 비율)을 구간별로 보는 것으로 대체함.

### 3.17 `[폐기]` 시드 배깅은 v13(Tweedie) 위에서 프리미엄 레버가 아님

§3.11 이 "분산 축소가 프리미엄 레버" 라는 가설을 낳았으므로 시드 8개를 학습해
N = 1,2,3,5,8 로 평균했음(step13).

| N | raw | 1−NMAE | FICR | 함의 FICR | 프리미엄 | 후처리 여지 |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 0.5950 | 0.8627 | 0.3273 | 0.3367 | −0.0094 | 0.0236 |
| 8 | 0.5950 | 0.8631 | 0.3270 | 0.3375 | −0.0104 | 0.0265 |

연도짝비교 N=8 − N=1 은 평균 −0.0003 (부호일치 False). **노이즈.**
Q2(후처리와 겹치는가)는 오히려 통과했음 — 여지가 0.0236 → 0.0265 로 **늘어** v14 식 상쇄 위험이 없음.
그러나 Q1 이 죽어 살 것이 없음.

**기전** 시드 간 예측 산포가 중앙값 0.9~1.1%cap 인데 밴드 반폭이 6%cap 임.
8시드 평균이 깎는 양은 그중 √(1−1/8) ≈ 0.35%cap 으로, 밴드 경계를 넘나드는 행을
뒤집기엔 한 자릿수 작음.

**절차 교훈 (§9.1 에도 기록)** 이 결과는 **§3.11 표가 이미 예측하고 있었음.**
폴드 평균의 이득은 D(MAE+W)에서 6/6 · +0.0049 였고 **A(Tweedie+U)에서는 −0.0005 · 2/6** 였음.
v13 은 A 계열임. 배깅 이득이 없는 것이 사전 예측 가능했음.
> **새 실험을 설계하기 전에 기존 표에서 그 조건에 해당하는 칸을 먼저 조회함.**

### 3.18 `[폐기]` A-6 — lead time 은 이미 있었고, 블록 피처는 선택을 밀어냄

2x2 요인(step18, 그룹 독립, 나머지 v13 고정). raw OOF.

| 조건 | 피처 수 | raw | 1−NMAE | FICR |
|---|---:|---:|---:|---:|
| A 기준(v13 독립) | 812 | 0.5895 | 0.8609 | 0.3181 |
| B +lead | 815 | 0.5896 | 0.8602 | 0.3191 |
| C +블록 | 908 | 0.5860 | 0.8604 | 0.3115 |
| D +둘다 | 911 | 0.5861 | 0.8606 | 0.3116 |

**lead 주효과 +0.0001 / 블록 주효과 −0.0035 / 상호작용 −0.0000**

#### lead 는 구조적으로 공허했음

`lead_hours = hour + 11` 의 일대일 재라벨링임(§3.2 보강 규칙 참조).
실측 출력이 그대로 증명함 — 블록이 전부 정확히 24행이고 lead 범위가 12~35 다.
**로드맵의 '없던 축' 판단이 오진이었음.**

#### 블록 피처는 노이즈가 아니라 '해' 였음 — 선택 변위(selection displacement)

C 는 연도 3/3 음수(−0.0009 / −0.0068 / −0.0023)로 **일관되게 나쁨.**
그룹별로도 0/3 양수, G2 가 −0.0083 으로 최악임.
쓸모없기만 하다면 top-200 에서 안 뽑히고 효과가 0 이어야 하는데 그렇지 않음.

**기전 가설 (검증 가능)**: 블록 피처 96개는 §3.4 의 생존 상위 12컬럼에서 파생했으므로
원본과 강하게 상관돼 있음. `run_cv` 의 top-K 선택은 **gain 기반**(`feature_importances_`)인데,
상관된 파생 피처들이 원본의 gain 을 나눠 가지면서 **다른 계열의 피처를 top-200 밖으로 밀어냄.**
§3.3 이 측정한 '812→200 이 +0.0021' 의 이득이 그만큼 훼손됨.
(검증법: 폴드별 top-200 중 블록 피처 개수를 셈. 50개 이상이면 변위 가설 지지)

> **`[신규 규칙]` 피처를 대량 추가할 때는 선택 예산도 같이 조정함.**
> 812 에 96 을 더하면서 top-200 을 그대로 두는 것은 **기존 피처의 11%를 잘라내는 것**과 같음.
> 다음 후보인 A-4(격자 가중, 약 +90컬럼)가 정확히 같은 함정에 들어감.
> A-4 는 (a) `_mean_` 을 **대체**하거나 (b) top-K 를 비례해서 늘리거나
> (c) 계열별로 따로 뽑는 방식 중 하나를 반드시 같이 시험해야 함.

**EDA 와의 불일치도 기록해 둠.** EDA(셀 28)는 *"Gain 지표의 Train 과적합과 변수 편향을
TreeSHAP 의 OOF 일반화 기여도로 보완함"* 는 원칙을 세웠는데, 실제 `run_cv` 의 top-K 는
**gain 만 씀.** step4 가 gain 만으로도 +0.0021 을 측정했으므로 당장은 문제가 아니지만,
상관된 파생 피처를 대량 투입할 때는 이 차이가 드러날 수 있음.

### 3.19 `[측정]` 잔차 케이스 분석 — 손실의 지도를 처음으로 그렸음

v13 이후 7연속 기각. 집계만으로는 새 가설이 안 나와 **잔차로 고른 실제 행**을 열었다
(step20/21, 학습 0회). 세 가지가 확정됐음.

#### (1) `[폐기]` 고풍속 컷아웃 — 이 사이트에는 그 구간이 없음

step20 최악 120개가 "예보는 강한 서풍인데 실제 발전량 10~28%cap" 단일 패턴이었고
(wpd z=+2.31 / v117 z=+1.74 / wdir 250~275° 집중), GFS v117 이 19~21 m/s 로 찍혔다.
그런데 **LDAPS ws10 기준 14 m/s 이상이 3년 41,146 채점행 중 59행뿐임**(G1 20 / G2 21 / G3 18).
GFS 의 과대예보였고, §3.4 의 "GFS 0.25° 격자가 능선 가속을 못 잡음" 와 정확히 같은 현상임.
§4.3 의 `stopped_weather 137/152건` 과도 일치함.

> `features.py` 의 `turbine_active_flag`(25 m/s), `soft_cutout_factor`(20 m/s),
> `scada.py` 의 `ref[centers>26]=0` 은 전부 **작동하지 않는 상수**다. 버그는 아니지만
> 고칠 가치도 없음 — 그 구간에 데이터가 없음.

#### (2) `[측정]` 가용률 손실의 크기 — 그리고 그것이 갭을 설명하지 않는 이유

| 가용률 | 채점행 | 평균편향 | 밴드적중(≤6%) | 칸 점수 | NMAE 손실 |
|---|---:|---:|---:|---:|---:|
| < 0.5 | 73 | +58.49 | 0.0% | 0.2076 | 42.7 |
| 0.5~0.8 | 748 | +25.63 | 8.7% | 0.4199 | 198.1 |
| 0.8~1.0 | 5,435 | +10.58 | 27.4% | 0.5879 | 838.8 |
| **= 1.0** | **34,890** | **+2.87** | **31.4%** | **0.6226** | **4,602.7** |
| 계 | 41,146 | | | | 5,682.3 |

* `avail < 1.0` : 행 **15.2%**, 손실 **19.0%** (enrichment 1.25배)
* `avail = 1.0` : 행 84.8%, **손실 81.0%**

**`[정정]` step20 의 "① 가용률 60%" 는 배타 분류 순서와 `avail<0.90` 임계가 만든 수치였음.**
최악 꼬리에는 확실히 몰려 있으나(lift 3.8배) 전체 손실로는 5분의 1임.

**`[정정]` 더 중요한 것: 가용률 손실은 우리와 1위의 차이를 설명하지 않음.**
1위도 2025 정비 일정을 모름. **모든 참가자의 공통 손실**이므로 §2.2 의 갭 분해에 넣으면 안 됨.
=> **손실의 81%가 `avail = 1.0` 행에 있고, 거기가 1위와 갈리는 곳임.**

부수 확인: 전역 배율로는 못 고친다. 가용률 평균이 약 0.98, 중앙값이 1.0 이라 `scale[g]` 을
평균으로 바꿔도 2% 전역 축소일 뿐이고 그것은 후처리가 이미 최적화한 축임(§3.14).
게다가 2%는 밴드 6% 안이라 다수(85%)에 손해도 이득도 아니고, 소수(15%)는 10~58%cap 벗어나
어차피 못 건진다. **후처리가 그 축을 안 건드린 것이 정답이었음.**

#### (3) `[측정]` G3 격차의 국소화 — 풍속에 따라 무너짐

`avail = 1.0` 행만, LDAPS ws10 축. **밴드 적중률(≤6%, 발전량 가중)**:

| 풍속 m/s | G1 | G2 | **G3** |
|---|---:|---:|---:|
| 0~5 | 28.9% | 29.4% | **32.3%** ← 3그룹 중 최고 |
| 5~8 | 30.8% | 35.9% | **23.5%** |
| 8~11 | 36.8% | 49.8% | **19.1%** |
| 11~14 | 40.5% | 58.5% | **11.5%** |

**G1·G2 는 풍속이 오를수록 좋아지고 G3 만 정반대로 무너짐.**
G3 칸점수 0.6351 → 0.5616 → 0.5148 → 0.4647.

§3.7 의 "G3 격차" 를 처음으로 국소화한 것임. **G3 는 전반적으로 나쁜 것이 아님 —
저풍속에서는 세 그룹 중 가장 좋고, 중고풍속에서만 무너짐.**

**후처리가 왜 이것을 못 잡았는지도 설명됨.** G3 의 8~11 m/s 편향은 **+8.12%cap** 으로
표 전체 최대인데 후처리는 깎지 않았음. 밴드적중이 19% 라 8% 를 깎아도 밴드에 못 들어가
NMAE 만 좋아지고 FICR 은 그대로이기 때문임. 산식상 이득이 없어 최적화가 무시했음.
> **즉 G3 의 문제는 편향이 아니라 분산임.** 편향을 지워도 오차 분포가 6%cap 보다 넓으면
> 소용이 없음. 후처리·배율·격자 — 지금까지의 모든 레버가 G3 에서 안 통한 이유가 이것임.

### 3.20 `[측정]` G3 는 왜 고풍속에서 무너지는가 — 기종이 아니라 배치다

`info.xlsx` 로 세 가설을 갈랐음.

**`[폐기]` 기종·로터 크기 차이** — 비출력(rated / 스윕면적)이 사실상 동일함.

| | 로터 | 스윕면적 | 정격 | **비출력** | 실측 정격도달 |
|---|---:|---:|---:|---:|---:|
| V126 (G1·G2) | 126 m | 12,469 m² | 3,600 kW | **0.2887 kW/m²** | 12.2 m/s |
| U136 (G3) | 136 m | 14,527 m² | 4,200 kW | **0.2891 kW/m²** | 11.8 m/s |

**차이 0.14%.** 로터가 큰 만큼 정격출력도 커서 정확히 상쇄됨. 파워커브 모양이 사실상 같음.
=> 기종 가설 기각.

**`[부분]` 터빈 수** — 1기 정지가 G3 는 20.0%cap, G1·G2 는 16.7%cap. 둘 다 밴드(6%)를
훨씬 넘으므로 하한을 만들지만, 밴드적중 49.8% vs 19.1%(2.6배)를 설명하기엔 부족함.

**`[유력]` 배치와 단지 구성** — `info.xlsx` 에서 새로 확인한 것이 둘임.

1. **G3 는 단일 단지가 아님.** 5기 중 **1호기만 '태백가덕산', 2~5호기는 '태백원동'** 임.
   G1·G2 는 전부 가덕산임. §4.2 가 남긴 미검증 가설이 여기서 확인됨 —
   계통 손실계수 잔차가 G1 0.54% / G2 0.60% 인데 **G3 만 1.16% 로 2배**이고,
   "원동 2~5호가 다른 연계점을 씀" 는 추정이 그 원인이었음.
   **서로 다른 운영 단위 5기를 하나의 KPX 그룹으로 합친 것이 G3 다.**
2. **공간 범위가 훨씬 넓음.**

| | 체인 길이 | 기수 | 기당 간격 | 로터 대비 |
|---|---:|---:|---:|---:|
| G1 | 약 1.20 km | 6 | 240 m | **1.9 D** |
| G3 | 약 2.05 km | 5 | 512 m | **3.8 D** |

   **G3 의 내부 후류는 오히려 G1 보다 적음**(간격이 2배). 후류 가설은 내부에 대해서는 반대다.
   대신 **단지 전체 범위가 1.7배 넓음.**

**왜 하필 고풍속에서인가 — 이것이 기전임.**
파워커브의 `dP/dv` 는 정격(11.8~12.2 m/s) 직전에서 최대다. 8~14 m/s 구간이 정확히 거기다.
* G1(1.2 km)은 터빈들이 거의 같은 바람을 봄 → 고풍속에서 전부 정격에 붙어 **포화**함.
  그래서 밴드적중이 40.5% / 58.5% 로 **올라감.**
* G3(2.05 km, 능선 끝까지 뻗음)은 터빈마다 다른 바람을 봄 → 일부는 정격, 일부는 미달.
  **그룹 전체가 포화하지 않음.** 11~14 m/s 에서 G3 평균 실제가 **79.0%** 에 그친다
  (G2 84.3%). 그리고 그 미포화 정도가 시간마다 달라져 **분산이 커짐.**

> **결론: G3 격차는 기종이 아니라 (a) 이질적 단지 구성 (b) 1.7배 넓은 공간 범위 때문이며,
> 고풍속에서 증폭되는 이유는 그 구간에서 `dP/dv` 가 최대라 공간 풍속 편차가
> 출력 편차로 최대 증폭되기 때문임.**

**따름 정리 — A-4(격자 가중)의 사전 예측.**
현재 파이프라인은 `ldaps_mean_*`(16격자 **균등평균**) 하나로 세 그룹을 전부 대표함.
§4.4 는 동일 시각 10m U성분이 G1 5.378 / G3 6.072 / 균등평균 5.298 로
**그룹 간 실제 차이 13%를 버리고 있음**고 이미 측정했음.
공간 범위가 가장 넓은 G3 가 그 오차를 가장 크게 받는다.
=> **A-4 는 G3 에서 가장 큰 효과를 내야 함. 이것이 사전 예측이며 판정 기준임.**
(효과가 G1·G2 에 더 크게 나오면 기전이 다른 것이므로 해석을 다시 씀)

### 3.21 `[측정]` 격자 산포 — OOF 는 이겼고 LB 는 졌음

`src/features_grid.py` 로 격자 간 표준편차·범위·공간 경도 54개를 만들어 v15 로 학습했음.

| 계측기 | 결과 |
|---|---|
| raw OOF 연도짝비교 (step23) | **+0.0023** [+0.0021 +0.0020 +0.0029] 3/3 양수, 신뢰비율 4.6 |
| 그룹별 | G1 +0.0021 / G2 +0.0013 / **G3 +0.0039** — §3.20 사전 예측 적중 |
| **실제 LB (v15)** | **0.640428** vs v13 0.641458 = **−0.0010** |

**raw OOF 게이트를 3/3 으로 통과했는데 LB 에서 졌음.** §3.5 전이율(정보형 79%)대로면
+0.0018 이 나왔어야 함. 부호가 뒤집힌 것이므로 전이율 문제가 아님.

**진단 — 선택 예산과의 대체 관계** (step23b, 2×2 요인)
상호작용 −0.0027 (3/3, 비율 2.46). top-k 를 250 으로 올리면 산포 효과가 −0.0004 로 소멸함.
K=201~250 에서 복귀하는 개별 격자 컬럼이 같은 정보를 담고 있기 때문임.

> **보정된 이해.** 트리는 16컬럼 표준편차를 *정확히는* 못 만들지만 개별 격자 컬럼이 충분하면
> 여러 분할로 부분 근사함. 이 피처의 가치는 '없는 정보를 준다' 가 아니라
> **'같은 정보를 1개 컬럼으로 압축해 선택 예산을 절약한다'** 임.
> 그러므로 **컬럼 수가 곧 비용**이고, 54개는 그 비용이 이득을 넘었음.

**남은 것 — 컬럼 1~2개짜리 버전.** `gdx`/`gdy` 는 **고정된 동/북 축**임. 물리적으로 의미 있는
것은 흐름 방향 경도이며, 이건 트리가 원리적으로 못 만듦(비 나눗셈 + 4컬럼 곱).

```
along = (gdx·u + gdy·v) / |V|      # 곧 도착할 공기가 지금과 얼마나 다른가 = 위상/램프 지표
cross = (−gdx·v + gdy·u) / |V|
```

그룹당 2컬럼이므로 §3.21 의 대체 관계 위험이 사실상 없음. **v15 의 나머지 50컬럼은 빼고
이것만 넣는 구성**이 다음 시도임.

### 3.22 `[측정]` 통합 학습의 이득은 양이 아니라 **동질성**에서 나옴

step26 이 풀(pool) 축과 사용(use) 축을 분리해 6개 아암으로 쟀음.

* G1 이 **G2** 와 묶일 때 **+0.0126**, **G3** 와 묶일 때 **+0.0034**
* 라벨 비율은 1.49배뿐인데 이득은 3.7배임 => **양이 아니라 동질성**

**G3 의 세 번째 구조적 핸디캡.** §3.20 이 둘(5기뿐이라 1기 정지 = 20%cap / 공간 범위 1.7배)을
찾았고, 여기서 세 번째가 나옴 — **G3 에는 동질 파트너가 없음.** G1·G2 는 서로에게서 힘을 빌리는데
G3 는 빌릴 상대가 없음. 라벨도 2년뿐임.

### 3.23 `[측정]` 후처리 **적합 절차** 축도 닫힘 — 그리고 계측 규칙 두 개를 얻음

§3.14~3.16 이 닫은 것은 후처리의 **함수 공간**이었음. 이번에는 **추정량**을 쟀음
(step27/27b) — 연도 가중 · 그룹 간 축소 · 다중 출발점.

#### 결과 1 `[확정]` 출발점 9개로 충분함

9 → 25 가 +0.0002 (신뢰비율 0.15). 그리고 in-sample 총점이 −0.0004 차이인데
적합된 곡선은 꽤 달랐음(G1 은 마지막 두 절점이 0.926 으로 붙고, G3 는 1.039 로 1 을 넘음).

> **서로 다른 곡선이 거의 같은 점수를 냄 = 최적점 근처가 평평함.**
> 국소최적에 갇히는 게 아니라 봉우리가 넓은 것임.
> => **§3.14~3.16 의 음수 판정들에 옵티마이저 노이즈가 섞여 있지 않았음.** 그 결론들이 더 단단해짐.

#### 결과 2 `[폐기]` 연도 가중

| 분할 방식 | Δ정규(같은해 − 다른해, 크기 맞춤) |
|---|---:|
| 무작위 행 | +0.0143 (8/8 양수) |
| 주 블록(7일) | +0.0109 (8/8) |
| **월 블록(30일)** | **+0.0051 (5/8)** |

**무작위 − 월블록 = +0.0092 가 자기상관 누설이었음.** 남은 +0.0051 도
1−NMAE +0.0009 / FICR +0.0092 = **FICR 몫 91%** 라 문턱 배치에 불과함.
게다가 +0.0051 은 '같은 해 데이터를 봤을 때' 의 상한인데 2025 라벨은 0행임.
=> **연도 가중 축 종결.**

**강원 연 기후 특성 PDF 의 세 번째이자 마지막 사용도 여기서 기각됨.** 부호 대조군
(`y2023_down` −0.0008 vs `y2023_up` **+0.0024**)이 기후 근거와 **반대** 방향을 냈음.
정체는 이상기후가 아니라 **시간 인접성**이었음(2023 은 가운데 해라 양쪽 폴드에 인접).

#### 결과 3 `[계측 규칙]` 앞으로 모든 '데이터를 나눠 비교' 에 적용함

1. **크기 맞춤 대조군을 반드시 둠.** 첫 판 설계는 '같은 해 ½년' vs '다른 해 2년' 이었는데
   그건 표본 크기와 연도 효과가 섞인 비교임. 음수가 나와도 해석이 안 됨.
   **비교 대상은 학습 표본 크기를 같게 맞춘 것이어야 함.**
2. **시간 블록으로 나눔.** 풍속 자기상관이 매우 강해 무작위 행 분할은 홀드아웃의
   **복제본**을 훈련쪽에 넣음. 절점 6개짜리 전역 사상이라도 **6%/8% 문턱 배치는 복제본을
   타고 전이됨.** 이 데이터셋에서 그 누설은 **+0.0092** 였음 — 우리가 쫓던 효과 크기의 2배임.
3. **Δ 를 1−NMAE 와 FICR 로 쪼갬.** FICR 몫이 90% 를 넘고 1−NMAE 가 0 근처면
   그건 곡선 모양이 아니라 문턱 배치이고 **다른 표본으로 안 옮겨감.**

### 3.24 `[측정]` 격차의 81% 는 FICR 인데, 그 해석에는 함정이 있음

1위 총점 0.672 가정 시 (§3.13 의 1−NMAE 0.8794, FICR = 2T − (1−NMAE))

| | total | 1−NMAE | FICR |
|---|---:|---:|---:|
| 우리 v13 | 0.641458 | 0.867673 | 0.415244 |
| 1위 | ~0.672 | 0.8794 | ~0.4646 |

격차 0.0305 = 정확도 몫 0.0059 (**19%**) + FICR 몫 0.0247 (**81%**).

**함정 — FICR 과 NMAE 는 독립이 아님.** 정확해지면 자동으로 더 많은 행이 ±6% 안에 들어옴.
따라서 물어야 할 것은 **"1위의 FICR 우위 중 얼마가 그냥 더 정확해서인가"** 임.

  모양 프리미엄 = 1위의 ΔFICR − (전환율 dFICR/d(1−NMAE) × 1위의 Δ정확도)

`scripts/step28_ficr_geometry.py` 가 학습 0회로 이 값을 냄. **`[측정 완료 07-27]`**

#### 측정 결과 — 지도가 다시 그려짐

* **전환율 dFICR/d(1−NMAE) = 2.21** (LB 작동점 보간). 정확도 1점 = 총점 **1.61점**.
  합성데이터의 8~9 는 우리 분포가 아니었음 — 실분포는 꼬리가 두꺼워(채점행의 34.7%가
  오차 15%cap 초과) 문턱 근처 질량이 얇고, 그래서 전환율이 낮음.
* **1위(연식2)는 정확히 우리 κ-곡선 위에 있음.** 정확도 우위 +0.0172 가 자동으로 끌고 오는
  FICR = +0.0380, 실제 +0.0382. **모양 프리미엄 +0.0002 ≈ 0.** 1위는 그냥 더 정확함.
* **그런데 §3.25 의 4팀(#6/#19/#28/#40)은 곡선 위에 없음** — 곡선이 예측하는 값보다
  +0.013~0.031 위에 떠 있음. 두 관측은 모순이 아니라 **필드가 이질적**이라는 뜻임:
  정확도형 상위(1·2위)와 밴드배치형 중위(3·6·19·28위)가 공존함.
* **1위로 가는 산수.** 정확도 단독 = ΔNMAE +0.0172 필요(v11→v13 누적 개선의 6배).
  밴드배치 단독 = +0.011 총점 = 0.652 에서 멈춤. **둘 다 필요함** — 정확도 +0.010,
  배치 +0.008~0.011 규모의 조합이 현실적인 1위 경로임.
* **PART 5 로 조건부 이동 축도 닫힘.** 근접실패(6~10%)의 과대예측 비중이 전 p 구간에서
  52~65% 로 거의 대칭 — 사전 등록한 해석 규칙대로, 단조든 조건부든 **이동**으로는 근접실패를
  못 고침. 고역 평균편향 +8~9.5%cap 은 정지 꼬리 스큐(§3.26 공통손실)로 설명되며 L1 최적에서
  남는 것이 정상임.


### 3.25 `[측정]` 필드 횡단면 — 모양 프리미엄은 실재하고, 우리가 그걸 안 갖고 있음

**출처.** 2026-07-27 public LB (전 행 `total = 0.5(1−NMAE)+0.5·FICR` 검산 통과 — 조작·오독 아님).
§3.24 는 "모양 프리미엄을 재야 함" 까지 갔는데, LB 가 세 컬럼을 다 보여 주므로
**우리 오차 분포 모형 없이 관측만으로** 존재가 확정됨.

#### 점별 비교 — 회귀 가정 없음

| 팀(순위) | 1−NMAE | Δ정확도 (우리 대비) | FICR | ΔFICR |
|---|---:|---:|---:|---:|
| theseung (#6) | 0.87164 | +0.0040 | 0.44588 | **+0.0306** |
| 두바이쫀뜩쿠키 (#19) | 0.86871 | +0.0010 | 0.43695 | **+0.0217** |
| **채대언 (#28)** | 0.86367 | **−0.0040** | 0.43746 | **+0.0221** |
| chaboom (#40) | 0.86878 | +0.0011 | 0.42860 | +0.0134 |

**#28 은 우리보다 부정확한데 FICR 을 +0.022 더 가져감.** 정확도로는 설명이 안 되는,
같은 정확도 수준에서 +0.013~0.022 의 FICR 이 필드에서 실제로 실현되고 있음.
상위 10팀 회귀로도 같은 그림 — 우리 정확도에서 필드 예측 FICR 0.4390 vs 우리 0.4152,
**잔차 −0.0238 (총점 −0.0119).** 1위까지 갭(0.0277)의 **43%가 이 잔차**임.

#### §3.14 와의 관계 — 모순이 아니라 완성

§3.14/3.15 는 **p 의 단조함수 공간**이 국소최적까지 소진됐음을 증명했음. 그러므로 이 잔차는
후처리 재적합으로는 못 먹음 (step27/27b/27c 가 3중으로 재확인함). 남는 통로는 셋뿐임.

    (a) 오차 분포 중심을 더 뾰족하게 — 손실 모양(tweedie power·밴드 추종 손실)·앙상블
    (b) x-조건부 점예측 배치 — B-2b (분위회귀 + 행별 기대효용)
    (c) 발전량-가중 배치 — FICR 은 a-가중이고 NMAE 는 행-균등이므로,
        같은 NMAE 라도 고발전 행에서 정확하면 FICR 이 더 나옴

**즉 §8.2 의 B-2b 는 '가설' 에서 '필드가 실현 중인 것을 따라잡는 일' 로 격이 바뀜.**

#### 실측 갭 분해 (v19 이전 가정치 폐기)

    1위 실측 0.66912 (가정 0.672 폐기)
    갭 0.02766 = 정확도 0.00858 (31%) + FICR 0.01909 (69%)   ← 19%/81% 가정 수정
    top-30 (산출물 커트라인) 0.65032, 우리와 +0.0089. 우리는 현재 top-40 밖.

**부수 관측.** ① §3.13 구도 유지 — 1위(연식2)는 2위보다 부정확한데 FICR 로 이김.
② 3위 GBSNU 는 23회 제출로 전체 최고 FICR(0.45473) — 제출 물량은 필요조건이 아님.
③ 공개 코드는 공식 RF 베이스라인뿐 — 0.649~0.659 에 30팀이 몰린 것은 공유 노트북이 아니라
**"잘 만든 GBM 이 수렴하는 지점"** 임. 그 위로 올라간 팀들은 구조적으로 다른 무언가를 하고 있음.

---


### 3.26 `[측정]` 가용률 재구성은 이미 최적이었음 — 기후 보정 폐기

**배경.** train.py 의 정제 타깃 되돌리기는 `scale[g] = median(label / (cf·cap)) = k × median(avail)`
인데, **avail = 1.0 인 행이 84~89% 라 중앙값이 정확히 1.0** — 즉 현행 "기대가용률" 은 사실상
1.0(전 터빈 정상 가정)임. 월×시각 가용률 기후값으로 채우는 안을 데이터로 판정함 (2026-07-27,
available_capacity.csv / clean_target.csv 직접 계산).

**판정 1 — 정지는 터빈 양자 단위로 lumpy 함.**
G1 분포: 1.0 이 85.8%, 다음 질량이 **0.833**(터빈 1기 정지). 연속 스펙트럼이 아님.

**판정 2 — LOYO 월 중앙값 96칸 중 93칸이 1.000.**
'다른 해들의 그 달' 중앙값이 1.0 미만인 칸은 3개뿐이고 전부 단발 사건(2022-02 G2 대정지 등)의
그림자임. 재발하는 계절 신호가 아님.

**판정 3 — 그래도 평균 배율로 보정하면? 전 그룹 악화.**

| 그룹 | 평균\|오차\| 현행→기후 | 6%밴드 포획(a가중) 현행→기후 |
|---|---|---|
| G1 | 1.71 → **2.77** %cap | 87.6% → 81.7% (**−5.9%p**) |
| G2 | 1.18 → **1.96** %cap | 91.4% → 88.8% (−2.6%p) |
| G3 | 2.74 → **3.71** %cap | 81.1% → 75.3% (**−5.8%p**) |

**기전.** 정지가 16.7%/20% 양자라, 평활한 월평균 배율(≈0.95)은
(a) 85% 의 정상 행을 밴드 가장자리로 밀어내고 (b) 정지 행은 어차피 16%p 이상 벗어나 있어
어디에도 못 넣음. **L1 과 밴드 구조 모두에서 중앙값(=1.0) 재구성이 최적임.**

**결론.** ① 가용률 기후값 축 폐기. ② 현행 파이프라인의 상수 재구성은 실수가 아니라
**사후적으로 정당화된 최적 선택**이었음. ③ §3.19 의 "가용률 손실 19% 는 공통 손실" 이
'기댓값으로도 보정 불가' 까지 강화됨 — 1위도 이 몫은 못 먹음.

---


### 3.27 `[측정]` 후처리 적합 배깅 — 4/4 아암 게이트 통과. LB 판정 대기

step27b 가 우연히 드러낸 ②>③ 관측을 step27c 가 진단·처방까지 완결함 (07-27).

**진단 (PART 1).** obj(③) ≥ obj(②) **8/8** — 전체 적합이 훈련 목적함수는 더 잘 맞췄는데
평가에서 짐. 출발점 49·maxiter 4배의 obj 개선이 전부 ~0 이라 옵티마이저 실패(나)와
재현 실패(다)가 동시 배제됨. **후처리 적합이 6%/8% 문턱을 훈련 표본에 맞추는 과적합이 실재.**

**처방 (PART 2).** 부분표본 K개 적합 후 곡선 평균. 사전 등록 4아암 **전부** §3.6 규칙 6 통과.

| 아암 | Δ (vs 현행) | 신뢰비율 | 그룹 |
|---|---:|---:|---|
| **sub25** (f=0.25, K=32) | **+0.0034 ± 0.0011** | 3.02 | 3/3 양수 |
| boot (K=16) | +0.0026 | 2.70 | 3/3 |
| sub50 (K=32) | +0.0023 | 1.90 | 3/3 |
| sub50_k8 | +0.0020 | 2.41 | 3/3 |

부분표본↓·K↑ 방향으로 단조 — (가) 기전과 정합. G3 최대(+0.0055), §3.15 관측과 일치.
§3.17(모델 시드 배깅 폐기)과 다른 축 — 이건 **후처리 적합의 표본 배깅**임.
⚠ 정산구간형이라 최종 판정은 LB 뿐(§3.6 규칙 4). v13 raw 에 적용하므로 v13 과 깨끗한 A/B.

### 3.28 `[폐기]` ES 지표 정렬 (v16)

조기종료를 대회점수로 바꾼 단일 변경. raw OOF 짝비교 +0.0016, **2022 부호 반전**(−0.0030)
→ 판별 불가, 게이트 미달, 제출 안 함. tweedie-nloglik 정지점이 이미 플래토 근처였다는 뜻.
축 종결. (참고: LOYO 가 val2025 라는 1행짜리 폴드를 만들며 전 데이터 모델이 하나 더 학습되어
추론 평균에 섞임 — v13 부터 있던 기존 동작이고 무해하나 기록해 둠.)

### 3.29 `[측정]` 분위회귀 — 학습은 크게 이겼고, 배치는 졌고, 원인은 분포의 왼꼬리

step30 (07-27). v13 과 폴드 피처·통합 구성·정제 타깃을 전부 미러하고 **손실만**
`reg:quantileerror`(분위 5개)로 바꿈.

**게이트 1 — 분위 학습 자체가 이김.**

    q50 raw − v13 raw = **+0.0131** [2022 +0.0093 / 2023 +0.0136 / 2024 +0.0163]
    비율 3.6.  내역: 1−NMAE +0.0007 / **FICR +0.0239** — 정확도 같은데 FICR 만 큼.

step9 의 목적함수 주효과 L−T(+0.0137)와 일치 — **v14(L+W)에서 가중 W 를 뗀 C 조건의 최초
단독 측정**임. 오차 분포 중심이 뾰족해지는 기전(§3.25 의 (a))이 그대로 실현됨.
⚠ **v14 전례**: raw +0.0260 이 LB −0.0120 으로 뒤집힌 적 있음(§3.10). 다만 v14 는 W(가중)
교란이 섞여 있었고, 이번 제출(v17 = q50 + 표준 후처리)이 그 교란을 분리한 시험임.

**게이트 2 — 기대효용 배치는 짐.**

    placed − v13후처리(insample) = −0.0115 (그룹 0/3 양수) / placed − q50후처리 = −0.0155
    (in-sample 비교 인플레 ~+0.008 감안해도 명확한 음수)

**진단.** 분위 모델은 정제 타깃(무정지 발전량)을 학습했으므로 **예측 분포에 정지 왼꼬리가
없음.** 실제 라벨 a = 무정지발전량 × V (V ~ §3.26 가용률 경험분포: 1.0 85% / 0.833 10% / …).
왼꼬리가 없는 분포로 기대효용을 적분하면 밴드 포획 확률을 과대평가해 위로 과감하게 배치함
(placed 의 1−NMAE 0.8557 로 최악). **수정: V 를 독립 혼합으로 곱해 적분** (--mix-avail).
재시험에서도 지면 배치 축 폐기, 분위 학습만 채택.


### 3.30 `[측정]` v13 이후 LB 제출 7건 전패 — 내부 계측기와 2025 의 불일치는 계통적임

**기록 (v13 = 0.641458 기준, 07-27 밤 확정).**

| 제출 | 종류 | 내부 판정 | LB Δtotal | Δ(1−NMAE) | ΔFICR |
|---|---|---|---:|---:|---:|
| v14 | 손실 정렬 (L+W) | raw +0.0260 (3/3) | **−0.0120** | −0.0051 | −0.0189 |
| v14-nopost | 계측용 | — | −0.0146 | | |
| v13-nopost | 계측용 | — | −0.0210 | | |
| v15-blend-w50 | 정산구간형 | §3.12 종결용 | −0.0081 | | |
| v15 (격자산포) | 피처 | raw +0.0023 (3/3) | −0.0010 | −0.0011 | −0.0010 |
| **v13-bag25** | 정산구간형 | 중첩 +0.0034 (4/4 아암) | **−0.0020** | **+0.0001** | **−0.0040** |
| **v17-q50post** | 손실 정렬 (L만) | raw +0.0131 (3/3, 비율 3.6) | **−0.0122** | **−0.0078** | **−0.0165** |

#### 이 표가 답한 것

**1. §3.10 의 3주 묵은 질문이 닫힘 — 범인은 가중(W)이 아니라 손실(L)임.**
v14(L+W) −0.0120 과 v17(L만) −0.0122 가 사실상 같음. 게다가 v17 은 LOYO 에서 정확도가
v13 과 같았는데(+0.0007) **2025 에서는 정확도 자체가 −0.0078 로 무너짐.**
중앙값 계열 손실은 2022–24 홀드아웃으로는 tweedie 와 구분이 안 되는데 2025 에서만 짐.
=> **중앙값/분위 손실 계열 전체 폐기.** 재론 금지.

**2. 정산구간형(후처리 적합 변경)의 LB 전이는 감쇠가 아니라 부호 반전임.**
관측 4건(w-blend, bag25, v14 의 FICR 성분, v17 의 FICR 성분) 전부 음수.
§3.5 의 "정산구간형 raw 25%" 는 **"≤ 0"** 로 정정함. bag25 의 분해가 결정적임 —
같은 raw 예측(Δ정확도 +0.0001)에 곡선만 바꿨는데 FICR −0.0040.
**우리 OOF 가 '문턱 과적합' 이라 부른 그 날카로운 조정이 2025 public 에서는 오히려 벌고 있음.**
2022–24 로 잰 '일반화' 와 2025 로의 일반화가 다른 방향임.

**3. 역대 LB 상승은 전부 정보형이었음.** v11(정제 타깃), v12, v13(통합 학습)만 LB 를 올렸고,
그 뒤 정규화형·정산구간형·손실정렬형·피처형 시도는 전패임.

#### 운용 규칙 (강제)

1. **내부 게이트(§3.6)는 이제 '제출 금지' 판정에만 쓴다.** 통과는 제출의 필요조건이지
   충분조건이 아니다 — 정산구간형·손실정렬형은 게이트를 통과해도 제출하지 않는다.
2. 남은 제출 근거는 두 가지뿐: **(a) 정보형 변화** (새 정보·표현 — 역대 유일한 상승 계열),
   **(b) 리더보드 자체를 계측기로 쓰는 것.**
   `[정정 07-28]` 당초 (b) 를 "테스트 피처의 적대적 검증" 으로 적었으나 **철회함.**
   유의사항 7 — "평가 데이터셋은 제출 파일 생성을 위한 **추론 목적으로만** 사용" — 에 따라
   테스트 기상 피처를 판별기·피처 선택 등 추론 외 용도로 쓰는 것은 규칙 위반임
   (작성 단계에서 발견, 실행 전 폐기). 2025 를 합법적으로 보는 창은 **리더보드뿐**이고,
   연도 간 드리프트는 **학습 3개년 사이에서만** 잼 (step31 v2).
3. v13 변형의 소규모 개선 시도로 제출을 더 쓰지 않는다. 195→200위 하락은 우리 점수
   정체 + 필드 상승이며, 이 계열로는 따라잡지 못한다는 것이 7건의 결론이다.

---


### 3.31 `[측정]` 연도 간 드리프트는 실재하고, 물리적이고, 단조다 — 그러나 피처 제외로는 못 잡음

step31 v2 (07-28, 학습 데이터 전용 — 유의사항 7 준수).

**판별 AUC.** 22vs23 0.802 / 23vs24 0.824 / **22vs24 0.843** (원거리쌍 최대 = 누적형 표류).
예보블록 GroupKFold 라 자기상관 부풀림 없음.

**이동의 정체 — 상위 30 피처가 세 계열로 정리됨. 전부 물리적으로 일관.**

| 계열 | 개수 | 방향 (22→23→24) | 예 |
|---|---|---|---|
| 습도 (r·q·sh) | 13 | **전부 단조 증가** | ldaps_g8_r: 69.3→71.3→77.4 (+8%p) |
| U성분 (10m~500hPa) | 7 | **전 고도 단조 감소** | 10m u: 3.03→2.89→2.29 (−24%) |
| 기온 (지상·500hPa) | 3+ | 단조 상승 | 지상 +1.1K, 500hPa +1.6K |

500hPa 까지 같은 방향 = 센서/모델 버전 문제가 아니라 **대기 순환의 추세**임.
습해지고, 따뜻해지고, 서풍이 약해짐. 2025 는 이 추세의 **외삽 위치**(2024 너머)일 개연성.

**피처 제외는 기각 (사전 등록 분기 발동).** 상위 30개를 제거해도 연도 판별력이 그대로
(−3%~+1%). 이동이 수백 컬럼에 중복돼 있음 — §3.21 의 대체관계와 같은 구조.
=> 컬럼이 아니라 **표본**을 손봐야 함.

**후속 = v18 (연도 최신 가중 학습, 2022:0.5/2023:0.75/2024:1.0).**
step27 의 연도 가중은 후처리 곡선 층이었고 이건 모델 학습 표본 층 — 최초 시도.
LOYO 가 이 변화에 구조적으로 불리한 점(역방향 폴드 2/3)까지 config_v18 notes 에 사전 등록함.

`[정직한 한계]` 드리프트가 §3.30 7연패의 원인이라는 증명은 없음 — v17 은 val2024 폴드가
가장 좋았는데도 LB 에서 죽었음. v18 이 그 가설의 직접 시험이었고, 아래와 같이 기각됨.

#### `[07-28 판정]` v18 폐기 — 사전 등록 조건 (a) 위반. 제출 0회 소모

게이트: 평균 **−0.0028** (3/3 음수), 그룹 양수 0/3. 그리고 결정적인 것 —

> **배치와 같은 모양인 val2024 폴드가 −0.0061 로 세 폴드 중 최악.**
> 드리프트 가설의 예측("최신 해 가중 → 다음 해 예측 개선")과 정반대 방향.

부수 관측: ES 트리 수가 반토막 남 (G1 val2024 282→129, 통합 295~381→138~151).
가중이 유효 표본 크기를 깎아 학습이 일찍 멈춤 — 잃은 것(데이터)이 얻은 것(최신성)보다 컸음.

**결론.** 공변량은 표류하지만(§3.31 상단) **사상(x→y)은 연도 간에 잘 전이됨.
데이터 양 > 최신성.** step27b 가 후처리 층에서 얻은 결론("연도는 서로 잘 전이됨")이
모델 층에서도 성립하고, §3.7/§3.22 의 "통합(데이터↑)이 이긴다" 와도 정합함.
드리프트→7연패 인과 가설은 추가로 약화됨. 남은 드리프트 대응은 표본이 아니라
**표현**임 — 후행 이동평균 편차(anomaly) 피처, step32 후보.

§3.30 체제의 첫 성과: 게이트+사전등록이 제출 낭비를 막았음 (v14/v17 식이었으면 1회 소모).

#### `[07-28 판정 2]` v19(표현 대응)도 폐기 — 드리프트 축 3전 3패로 완전 종결

게이트: 평균 −0.0016, 부호 갈림 [2022 +0.0006 / 2023 −0.0030 / 2024 −0.0024], 그룹 0/3.
**val2024(외삽 모양 폴드)가 또 음수** — 편차 표현이 드리프트 하에서 유효하다면 여기서
나타났어야 함 (2024 는 22·23 학습 범위 밖의 추세 연장점).

| 드리프트 대응 | 층 | 판정 |
|---|---|---|
| 피처 제외 | 컬럼 | step31 PART C 기각 — 이동이 수백 컬럼에 중복 |
| 표본 가중 | 행 | v18 기각 — val2024 −0.0061. 데이터 양 > 최신성 |
| 편차 표현 | 표현 | v19 기각 — val2024 −0.0024 |

**종합: 공변량 드리프트는 실재하나 사상(x→y)은 연도 간 안정적이고, 트리 모델은 주변분포
이동에 이미 견고함. 축 전체 종결.** 발표 소명으로는 완결된 서사임(현상 발견 → 대응 3종
검정 → 전부 기각 → 파이프라인의 견고성 입증). 제출 0회 소모 — §3.30 체제 두 번째 성과.

---


### 3.32 `[측정]` v20 은 실패가 아니라 `[무효]` — gain 기준 top-K 선택 자체가 노이즈 원천임

step33 (07-28). ASOS 관측 피처 10개를 v13 위에 얹어 학습했음. 게이트가 미달로 떴는데,
**미달의 원인을 확인하러 들어갔더니 실험이 실행되지 않았던 것**이 드러남.

#### 관측 1 — 관측 피처가 모델에 들어가지 않았음

폴드 11개 × obs 컬럼 10개 = 110칸 중 폴드별 top-200 에 살아남은 것이 **5칸뿐**임.

| 경로 | obs 생존 | 비고 |
|---|---|---|
| **joint (G1·G3)** | **0 / 30** | v13 의 운영 경로. 여기가 0 인 것이 결정적 |
| G2 독립 | 4 / 30 | `obs_hm_mean24` 2회, `obs_nwp_bias24`·`obs_pa_tend24` 각 1회 |
| G1·G3 독립 | 1 / 50 | |

`[가설]` 관측 피처는 **블록 상수**(24행이 같은 값)라 연간 유효 분할점이 90개뿐임.
시간별로 변하는 NWP 컬럼 800개와 gain 경쟁에서 구조적으로 불리함. 검증법 = v20b 에서
강제 포함한 뒤 gain 비중을 보는 것.

#### 관측 2 — 컬럼 10개가 top-200 의 25%를 뒤바꿨음

v13 과 v20 의 폴드별 top-200 을 맞춰 봤음.

| | 값 |
|---|---:|
| 겹침 | **1,636 / 2,200 = 74.4%** |
| 폴드당 평균 교체 | **51.3개** |
| 교체분 중 obs | **0개** (전부 기존 NWP 컬럼끼리 자리바꿈) |

`run_cv` 의 top-K 는 전체 컬럼으로 1차 모델 `m0` 를 학습해 gain 순으로 자름.
풀이 812 → 822 로 바뀌면 `colsample_bytree=0.8` 의 컬럼 추첨 스트림이 밀려 `m0` 가 달라지고,
gain 순위가 재추첨됨. §3.3 이 "gain 이 0 이 아닌 피처가 700~727개" 라고 쟀듯 K 경계 부근은
거의 동점이라 작은 섭동에도 대량으로 뒤집힘.

> **§3.18 이 가설로만 적어 둔 '선택 변위(selection displacement)' 의 순수한 형태임.**
> 거기서는 상관된 파생 피처가 gain 을 나눠 갖는 기전을 상정했는데, 여기서는
> **뽑히지도 않은 컬럼 10개가 RNG 를 미는 것만으로** 같은 크기의 변위가 났음.
> 정보 경합이 아니라 **절차 노이즈**임.

#### 관측 3 — 이 런이 우연히 내부 대조군을 만들어 줬음

게이트 결과 (compare_oof.py, 학습 0회):

    v20 − v13: 평균 +0.0012  [2022 +0.0027 / 2023 +0.0027 / 2024 −0.0019]  표준편차 0.0026
    부호 3/3 ❌  |평균|>표준편차 ❌  부호 양수 ✅  그룹 2+ ✅   =>  ❌ 판별 불가

그런데 그룹별로 쪼개면:

| 그룹 | 예측 경로 | obs 컬럼 | Δ |
|---|---|---|---:|
| G1 | joint | **0개** | **+0.0013** |
| G3 | joint | **0개** | **+0.0011** |
| G2 | 독립 | 있음 | +0.0007 |

**관측이 하나도 안 들어간 팔(G1·G3)이 들어간 팔(G2)보다 더 올랐음.**
=> +0.0012 는 관측 정보가 아니라 선택 재추첨의 산물임. 부호가 연도마다 갈린 것도 같은 이유임.

#### 판정과 도출되는 규칙

**v20 = `[무효]`.** '관측은 안 통한다' 가 아니라 **관측이 모델에 들어간 적이 없다.**
여기서 폐기로 기록했으면 §9 에 거짓 음성을 하나 남길 뻔했음.

> **`[신규 계측 규칙]` 피처 추가 A/B 는 선택 절차를 상수로 고정하고 잰다.**
> `--featcols-from` 으로 기준선이 쓴 폴드별 피처를 그대로 재사용하고 새 컬럼만 덧붙임.
> 그러지 않으면 재는 것이 '새 피처의 효과' 가 아니라 '선택 재추첨' 임.
> 이 데이터셋에서 그 재추첨의 크기는 **총점 ±0.005 · 컬럼 25%** 로, 우리가 쫓는 효과보다 큼.
> (§3.23 의 누설 +0.0092 에 이어 두 번째로 발견한 '계측기가 만든 가짜 신호' 임)

> **`[보강 규칙]` 게이트가 미달로 떴을 때도 '왜 미달인지' 를 한 번은 연다.**
> §3.30 체제는 미달을 제출 금지 판정으로 쓰는데, **미달의 원인이 계측 실패일 수 있음.**
> 최소 확인: 새 피처가 폴드별 선택에 실제로 들어갔는가(featcols_*.pkl 카운트, 학습 0회).

`[소급 검토 필요 — 미측정]` 같은 함정에 걸렸을 수 있는 과거 판정:
v15(격자 산포 54컬럼)는 게이트를 3/3 으로 **통과**했고 그 피처들이 선택에 실제로 들어갔음이
step23b 의 대체관계 측정으로 확인되므로 영향 없음. v19(anomaly)·step18(블록 96컬럼)은
생존 카운트를 안 쟀음 — 다만 둘 다 **음수 방향 3/3** 이라 '재추첨 노이즈' 로 설명되지 않음
(노이즈면 부호가 갈려야 함). => **소급 정정 대상 없음.** 기록만 남김.

#### 후속 = v20b (config_v20b.yaml)

폴드별 피처 = v13 이 저장한 그 200개 그대로 + obs 10개 = 210개. 선택 재선택 없음.
검증 완료: v13 컬럼 100% 보존 ∧ 추가분 10개 전부 `obs_`. 유일 변수가 관측 컬럼 10개임.
사전 등록은 config_v20b notes 에 고정했음 — 게이트 §3.6 규칙 6, 신뢰비율 2.0 미만이면
난수컬럼 대조군 필수, 미달이면 관측 축 폐기 확정. **통과해도 제출은 별도 판단(§3.30 규칙 1).**

#### `[07-28 판정]` v20b `[폐기]` — 관측 축 종결. 선택기의 판단이 옳았음

폴드별 210피처(v13 의 200 + obs 10) 확인하고 학습함. **판별 불가가 아니라 일관된 악화임.**

    v20b − v13: 평균 **−0.0057**  [2022 −0.0044 / 2023 −0.0072 / 2024 −0.0056]
    부호 3/3 일치(전부 음수) ∧ 신뢰비율 **4.07** ∧ 그룹 **0/3** 양수
    그룹별: G1 −0.0062 · G2 −0.0076 · G3 −0.0031
    독립 경로 폴드 8칸도 7/8 음수, 평균 −0.0079

신뢰비율 4.07 이므로 사전 등록 분기 2(난수컬럼 대조군)는 발동하지 않음. 분기 3 발동 —
**관측 축 폐기 확정.**

**부수 진단(사전 등록 4) — 모델은 관측을 쓴다. 쓰는데 해롭다.**

| 폴드 | obs gain 비중 | obs 최고 순위 / 210 |
|---|---:|---:|
| joint val2022 | 1.27% | 134위 (`obs_nwp_bias24`) |
| joint val2023 | 1.75% | 137위 (`obs_ta_mean24`) |
| joint val2024 | 1.62% | 112위 (`obs_hm_mean24`) |
| G2 val2024 | 1.88% | 138위 (`obs_hm_mean24`) |

컬럼 비중은 10/210 = 4.8% 인데 gain 비중은 1.3~1.9% — **평균 이하 기여자**임.
gain 이 0 이 아니므로 '트리가 안 썼다' 는 아님. **쓰는데 점수가 내려감.**

> **결론: v20 에서 gain 선택기가 관측을 전량 탈락시킨 것은 옳은 판단이었음.**
> 두 실험이 합쳐져야 축이 닫힘 — v20 은 **선택기의 판단**(안 뽑음), v20b 는 **실측 검증**
> (강제로 넣으면 −0.0057). v20 하나만으로는 어느 쪽도 증명 못 했음.

**`[가설 · 미검증]` 왜 해로운가 — 블록 상수의 날짜-식별자 성질.**
관측 피처는 블록 상수라 3년치에 서로 다른 값이 **블록 1,096개**뿐임. 기온·습도·기압변화를
함께 쓰면 사실상 '그 날이 며칠인가' 를 식별할 수 있고, 트리가 그것으로 분할하면 학습 연도의
**날짜별 특이성**을 외움. LOYO 는 검증 연도의 블록이 학습에 아예 없으므로 그 암기가 전량
손실이 됨. `[정직한 한계]` ES 트리 수가 5/8 폴드에서 줄었으나(G1 2022 281→193, G2 2022
220→156) 전 폴드 단조는 아님(G1 2024 129→141). '더 빨리 과적합' 기전을 **부분적으로만** 지지함.

> **`[신규 규칙]` 블록 상수 피처는 LOYO 에서 위험함.** 예보 블록 안에서 상수인 피처는
> 유효 표본이 행 수가 아니라 **블록 수(연 365)** 이고, 여러 개를 같이 쓰면 날짜 식별자가 됨.
> 앞으로 블록 상수 피처를 도입할 때는 (a) 개수를 최소로 하고 (b) 수준(level)이 아니라
> **편차·비율 형태**로만 넣음. — 단 **ECMWF 는 이 규칙에 안 걸림**(예보라 행별로 변함).

`[기록]` 관측 축은 '이 표현' 에서 닫힌 것이지 '관측 일반' 이 무용하다는 증명은 아님.
그러나 사전 등록이 "창 24h·관측소 2개·컬럼 10개 하나의 구성만, 격자 탐색 금지" 였으므로
창 길이·관측소·컬럼 조합을 바꿔 가며 재시도하지 않음. 그것이 이 프로젝트가 §9 에 반복해
기록해 온 사후선택임. **제출 0회 소모 — §3.30 체제 세 번째 성과.**

---


### 3.33 `[측정]` A-4 격자 가중 `[폐기]` — gain 1위 · 비중 19% 인 피처가 점수를 0 움직였음

v21 (07-28). 통합 경로에 폴드별·그룹별 NNLS 가중 풍속 3컬럼(`wgrid_ws/u/v`)을 명시적으로 줌.
`--featcols-from ./saved_models/v13` 로 선택 절차를 고정했으므로 v13 과의 차이는
**정확히 이 3컬럼뿐**임 (§3.32 규칙 적용).

#### 무결성 확인 (사전 등록 0번 — 판정보다 먼저)

**`group_2 Δ = +0.0000` (정확히 0).** 독립 경로 폴드 점수도 v13 과 소수점 네 자리까지 동일함
(G1 0.6002/0.5855/0.5809 · G2 0.5742/0.5957/0.6376 · G3 0.5710/0.5101).
=> **`--featcols-from` 이 기준선을 비트 단위로 재현함이 실증됨.** §3.32 에서 만든 계측기가
설계대로 작동한다는 직접 증거이고, 앞으로의 피처 A/B 는 이 위에서 잼.

#### 게이트 — 판별 불가

    v21 − v13: 평균 **−0.0002**  [2022 +0.0016 / 2023 −0.0003 / 2024 −0.0020]  표준편차 0.0018
    부호 3/3 ❌ (갈림)  |평균|>표준편차 ❌  부호 양수 ❌  그룹 2+ ❌ (0/3)
    그룹별: G1 −0.0002 · **G2 +0.0000** · G3 −0.0016

v20b(3/3 음수·비율 4.07 = **해로움**)와 달리 이건 **판별 불가**임. §3.6 해석 규칙대로
'나쁘다' 가 아니라 '구분 안 됨' 으로 읽고 **더 단순한 v13 을 유지함.**
§3.20 의 방향 예측(G3 최대)은 어느 그룹도 양수가 아니라 **검정 자체가 불가**했음.
(굳이 보면 G3 가 가장 음수였으나 노이즈 바닥 안이므로 근거로 쓰지 않음.)

#### `[핵심]` 부수 진단이 본 결과보다 중요함 — gain 은 정보량이 아니라 편의성을 잰다

| 폴드 | wgrid 3컬럼의 gain 비중 | `wgrid_ws` 순위 |
|---|---:|---:|
| joint val2023 | **19.27%** | **1위 / 203** |
| joint val2024 | **19.11%** | **1위 / 203** |

**모델은 이 피처를 203개 중 가장 많이 썼다. 그런데 점수는 0 움직였다.**

* '안 뽑혀서 미측정' 이었던 v20 과 정반대 상황임. 여기서는 **최대로 쓰였는데 이득이 0** 임.
* 기전: 트리가 개별 격자 83개로 힘들게 만들던 조합을 미리 합쳐 주니 **같은 분할을 더 싸게**
  할 뿐임. 정보가 늘지 않았으므로 점수도 안 변함.
* NNLS 가중치가 실제로 **2~3격자로 붕괴**했음 (예: val2024 G1 g12 0.59 / g7 0.41,
  G3 g7 0.90 / g16 0.09). 16격자가 서로 0.99 수준으로 상관돼 있어 생긴 결과임.
  즉 트리 입장에서 '두 컬럼의 가중평균' 이고, 그건 트리가 근사할 수 있는 형태였음.

> **`[신규 규칙]` gain 순위·비중을 피처 가치의 증거로 쓰지 않는다.**
> gain 은 '그 컬럼으로 분할하는 것이 얼마나 편했는가' 이지 '없던 정보를 얼마나 줬는가' 가
> 아님. **gain 1위 · 비중 19% 가 총점 Δ −0.0002 와 공존함**이 이 데이터셋에서 실측됨.
> 피처 가치의 유일한 증거는 여전히 **연도짝비교 Δ** 뿐임.

> **`[§3.21 완성]`** §3.21 은 격자 산포의 가치를 "없는 정보를 주는 것이 아니라 **같은 정보를
> 1개 컬럼으로 압축해 선택 예산을 절약하는 것**" 으로 정정했음. v21 은 그 명제를 직접
> 시험함 — **선택 예산을 전혀 쓰지 않고**(3컬럼 강제 포함, 나머지 200 고정) 압축 컬럼만
> 준 것이므로, 남는 것은 순수한 정보 성분뿐임. 그 성분이 **0** 으로 측정됐음.
> => 압축형 격자 피처의 정보 잔량은 없음. §3.21 의 정정이 옳았음이 확정됨.

#### 판정과 따름 정리

**A-4 `[폐기]`. 축 종결.** 사전 등록 분기 3 발동 — 가중치 산출 방식(NNLS→상관→gain)이나
변수(10m→50m)를 바꿔 가며 재시도하지 않음. 제출 0회 소모.

`[정직한 한계]` NNLS 가 2~3격자로 붕괴했으므로 실제로 시험된 것은 '16격자 가중결합' 이 아니라
'2~3격자 가중평균' 임. 그럼에도 재시도하지 않는 이유는 (a) 사전 등록이 명시적으로 금지했고
(b) 붕괴 자체가 데이터의 답(격자 간 상관 0.99)이지 절차 실패가 아니기 때문임.
v20→v20b 는 피처가 **모델에 들어가지도 않은** 실행 실패였으므로 재시험이 정당했으나,
여기서는 gain 1위로 최대한 쓰였음 — 실행은 완전했음.

> **따름 정리 — §6 후보 6번(흐름방향 경도 along/cross)의 기대치가 크게 낮아짐.**
> along/cross 도 같은 계열임: LDAPS 격자값들의 **결합**을 미리 계산해 주는 것.
> v21 이 "완벽히 포장하고 예산도 안 쓰고 gain 1위로 쓰이게 해도 0" 을 보였으므로,
> 같은 원천의 다른 결합에서 이득을 기대할 근거가 사실상 없음.
> **=> NWP 유도량 축은 §3.8 따름정리대로 소진됐다고 본다.** 피처 쪽에 남은 길은
> (a) 레이블 유도량(타깃 인코딩) 또는 (b) 원천이 다른 데이터뿐임.

---


### 3.34 `[측정]` max_depth 상향 `[폐기]` — 표현력은 병목이 아님. 깊이 6이 이미 상한 근처

v22 (07-28). v13 과 **`max_depth: 6 → 8` 한 줄만** 다름. `--featcols-from` 으로 선택까지 고정.
점수 후보가 아니라 **다음 실험(타깃 인코딩)의 전제를 확인하는 진단**으로 사전 등록했음.

    v22 − v13: 평균 **−0.0104**  [2022 −0.0093 / 2023 −0.0096 / 2024 −0.0122]  표준편차 0.0016
    부호 3/3 음수 ∧ 신뢰비율 **6.5** ∧ 그룹 0/3  =>  사전 등록 **분기 C** 발동

**부수 진단(사전 등록 6)이 기전을 확인함 — ES 트리 수가 40~50% 감소.**

| 폴드 | v13 트리 수 | v22 트리 수 |
|---|---:|---:|
| G1 val2022 | 281 | **141** |
| G1 val2023 | 175 | **109** |
| G2 val2022 | 220 | **141** |
| G2 val2024 | 166 | **121** |
| G3 val2023 | 116 | **91** |

용량이 커져 더 빨리 수렴한 것이 맞음 — 즉 깊이가 실제로 작동했고, **작동한 결과가 악화**임.
비율 6.5 는 지금까지 잰 것 중 가장 큰 일관 효과 중 하나임.

> **판정: 표현력 축 종결.** 깊이 6 은 부족한 것이 아니라 **이미 상한 근처**임.
> 하향은 시험하지 않음 — §3.5 상 정규화형이라 이득이 나와도 전이율 26% 임.
> 제출 0회 소모.

#### 이 진단이 타깃 인코딩에 주는 함의 (사전 등록 분기 B·C)

착수 전 전제였던 "hour×month 288칸을 깊이 6 이 못 만들어서 놓치고 있다" 는
**지지되지 않음.** 깊이 8 은 그 칸을 만들 수 있었고 총점은 −0.0104 였음.

`[정직한 한계]` 다만 이것이 타깃 인코딩을 **죽이지는 않음.** 기전이 다르기 때문임 —
깊이 상향은 **모든 축의 용량**을 늘려 분산을 키우는 반면, 타깃 인코딩은 컬럼 1개로
**특정 상호작용만** 싸게 주는 것임(게다가 out-of-fold 평균이라 정규화 효과도 있음).
v22 가 반증한 것은 '용량 부족' 가설이지 '이 상호작용이 비어 있다' 는 아님.
=> 사전 등록대로 **기대치는 v21 수준으로 하향**하고 §6 후보 2(정제 타깃 파라미터)를 앞세우되,
   타깃 인코딩을 폐기 목록에 넣지는 않음.

---

## 4. 데이터 구조 — 확정된 사실

### 4.1 예보 블록 구조 `[측정]`

* 매일 09 KST 초기화 → **13 KST 공개** → 익일 01:00 ~ 익익일 00:00 (24시간)
* **lead time 12~35시간**. 블록 90개 전부 정확히 24행 (`assert_block_structure` 확인)
* **같은 블록 내 미래 참조는 합법** (24시간이 동시 공개). 단 반드시 `groupby('data_available_kst_dtm')` 안에서
* 블록을 넘는 전방 참조는 **24시간 미래 정보 사용 = 규칙 3항 위반** (블록당 정확히 1행)

### 4.2 SCADA `[측정]`

| 항목 | 값 |
|---|---|
| `power_kw10m` 단위 | **kW가 아니라 10분간 kWh**. 시간값 = 6개 **합** |
| 정격 확인 | Vestas 600 kW10m(=3.6MW), Unison 700(=4.2MW) — 기준곡선 최댓값이 정확히 3600/4200 |
| 시각 정렬 | **Vestas +50분, Unison +60분** (벤더별로 구간 끝/시작을 다르게 찍음) |
| 정렬 R² | Vestas 0.99930 / Unison 0.99848 |
| 계통 손실계수 k | G1 0.9857 (RMSE 0.54%cap) · G2 0.9893 (0.60%) · G3 1.0009 (1.16%) |
| 기준곡선 표본 | Vestas 315,636개(40/60 bin) · Unison 86,980개(37/60 bin) |

**→ SCADA로 라벨을 사실상 완전 재구성 가능**(잔차 0.5~1.2% vs FICR 밴드 6%). 가동률 판정이 추정이 아니라 확정이 됨.

`[가설]` G3만 k≈1.0인 것은 원동 2~5호가 가덕산과 다른 연계점을 쓰기 때문으로 추정. 미검증.

**[측정] SCADA 를 피처로 되먹이는 경로는 막혀 있음.** §3.8 참조. SCADA 의 가치는
피처가 아니라 **타깃 정제**(가동률 보정)에 있으며 그것은 이미 v11 에서 실현됐음.

### 4.3 가동률 `[측정]`

| | ok | stopped_maint | derated | stopped_weather | missing |
|---|---:|---:|---:|---:|---:|
| Vestas | 94.1% | 4.2% (13,134) | 1.7% (5,483) | 0.0% (137) | — |
| Unison | 92.6% | 3.8% (3,317) | 2.6% (2,260) | 0.2% (152) | 0.8% (735) |

그룹별 평균 가용률: G1 95.61% · G2 96.07% · G3 95.38%
**가용률 100%인 시간 비율: G1 85.8% · G2 88.5% · G3 56.3%**

**→ 터빈-시간의 약 6%가 정비/고장으로 눌려 있고, 원래는 전부 "바람이 약했던 시간"으로 학습되고 있었음.**

고풍속 컷아웃(`stopped_weather`)은 137/152건으로 거의 없음 → 이 사이트는 컷아웃이 드물다.
**단, 이 구분 자체는 필수다.** 기상 요인 정지는 예측 가능한 현상이므로 타깃에서 제거하면 안 됨.

### 4.4 격자 → 그룹 가중치 `[측정]`

LDAPS 16격자(1.5km), 표고 869~1001m. 터빈은 능선 정상부(~990m).

| 그룹 | 상위 격자 (표고 반영 IDW) |
|---|---|
| G1 | g5(0.27) g6(0.26) g10(0.08) g11(0.08) |
| G2 | g6(0.29) g11(0.20) g12(0.10) g7(0.09) |
| G3 | g12(0.36) g11(0.16) g16(0.07) g7(0.07) |

동일 시각 10m U성분: G1 5.378 / G3 6.072 / **16격자 균등평균 5.298**
→ 현재 세 그룹 모두에 5.298 하나를 먹이고 있음. 그룹 간 실제 차이 13%를 버리는 중.

**단 이 IDW 가중은 `[폐기]`다.** §3.4 — 생존 1위 격자 `ldaps_g13` 이 IDW 최하위권임.
거리가 설명력과 일치하지 않음. 격자 가중은 데이터가 고르게 해야 함.

GFS 9격자(0.25°): 최근접 g5가 4.9km, 나머지는 19~40km → g5가 지배.
**[측정] `surface_0_sp`로 기압경도를 만들면 안 됨.** g3 102,629 Pa vs g5 93,450 Pa — 9,000 Pa 차이가 전부 표고다. 종관 흐름은 850hPa u/v 또는 500hPa 지위고도를 쓸 것.

### 4.5 터빈 배치와 후류 `[측정 = 좌표 기하]` / `[가설 = 결손량]`

터빈 17기 전부 2.4×2.5 km 안, 허브고도 전부 117m. Vestas 체인 이웃간격 중앙값 **267m = 2.1D** (통상 4~7D).
그룹별 배치축: G1 29° · G2 154° · G3 142°

Jensen 모형 기반 풍향별 평균 속도결손(%) — **평지 가정 이상화 모형, SCADA로 검증 필요**:

```
 dir      0    30    60    90   120   150   180   210   240   270   300   330
 G1    11.4   6.7  11.0  14.3  17.0   8.1  11.6   5.6   5.5   2.2   3.1   7.1
 G2     6.9   4.2   0.0   6.4  17.0  15.8  11.3   1.9   7.2  14.0  20.8  11.1
 G3     4.1   0.0   3.0   3.3   6.4  14.0   0.0   3.5   1.1   7.5  17.6  20.0
```

교차 후류 비중: G1 35% · G2 54% · G3 60% (다른 그룹 터빈이 만드는 후류)
→ **세 그룹을 독립 모델링하면 이 신호를 원천적으로 못 잡음.**
v13 의 long-format 통합이 이 구조를 일부 흡수했을 가능성이 있음 `[가설]`.

### 4.6 풍향계 상태 `[측정]`

| 터빈 | 오프셋 | 원형 산포 | 해석 |
|---|---:|---:|---|
| vestas_wtg04 | +23.4° | 10.2° | 계통적 오프셋 — 보정 대상 |
| vestas_wtg12 | −8.8° | **51.8°** | 오프셋 아닌 **불안정** — wake 분석에서 제외 권장 |
| vestas_wtg05 | −0.9° | 26.6° | 불안정 |
| vestas_wtg01 | −1.9° | 22.1° | 불안정 |
| unison 전체 | −28.7 ~ +23.8° | 9.0~19.3° | 능선 위 실제 공간 변동과 섞여 분리 곤란 |

---

## 5. 파이프라인 및 모듈 구조

```
wind_project/
├── configs/                        # config_v1 ~ v14
├── information/                    # data_description.md, info.xlsx
├── train/  test/                   # 대회 원본 (data/ 아래가 아님)
├── data/
│   └── scada_derived/              # available_capacity.csv, clean_target.csv,
│                                   #   wd_offset_*.csv, power_curve_{maker}.csv
├── main/
│   ├── train.py                    # argparse, LOYO, 누설없는 ES, 목적함수/가중 오버라이드
│   ├── inference.py                # 모델 glob, joint 경로, --no-post
│   └── ensemble.py
├── notebooks/                      # EDA.ipynb
├── scripts/
│   ├── step1_scada_check.py        # SCADA 정렬·가동률·손실계수·정제타깃 생성
│   ├── step2_clean_target_ab.py    # 정제 타깃 A/B          → 채택 (+0.0126 raw)
│   ├── step3_batch1_ab.py          # 물리 피처 A/B          → 폐기 (§3.2)
│   ├── step4_prune_sweep.py        # 폴드별 top-K 스윕      → 채택 200 (§3.3)
│   ├── step5_joint_group_ab.py     # 3그룹 통합 A/B/C
│   ├── step5b_hybrid_eval.py       # 통합 조합 D~H          → 채택 G (§3.7)
│   ├── step6_drift_check.py        # LOYO 타당성 진단       → LOYO 유지 (§3.1)
│   ├── step7_scada_wind_stack.py   # 2단 스태킹             → 폐기 (§3.8)
│   ├── step8_ramp_diagnosis.py     # 램프 가설              → 기각 (§3.9)
│   ├── step9_objective_ab.py       # 목적함수×가중 2×2      → v14, LB 역전 (§3.10)
│   ├── step10_avg_and_post_diag.py # 폴드평균/후처리 여지   → §3.11, §3.6
│   ├── step11_blend_sweep.py       # Tweedie×MAE 혼합       → 보류 (§3.12)
│   ├── step12_conditional_post.py  # 산포 변조 후처리       → 폐기 (§3.14)
│   ├── step13_seed_bagging.py      # 시드 배깅 N=1..8       → 노이즈 (§3.17)
│   ├── step14_covariate_post.py    # 시각·계절 버킷 배율    → 폐기 (§3.14, §3.16)
│   ├── step15_disp_stratified.py   # 이득 분포 층화 진단    → §3.14, §3.16 의 근거
│   ├── step16_lowend_knots.py      # 절점 격자 스윕         → 폐기 (§3.15)
│   ├── step17_scored_conditional.py# 채점조건부 타깃 학습   → 진행 (§8.2 B-2)
│   ├── step18_block_features.py    # lead time + 블록 구조  → 진행 (§8.3 A-6)
│   ├── check_v14_integrity.py      # 저장 산출물 무결성 대조
│   └── premium_track.py            # 밴드 프리미엄 계산·갭 분해
├── saved_models/                   # v11 ~ v14, _ab_step5, _ab_step9
├── src/
│   ├── features.py                 # v6 도메인 피처
│   ├── features_physics.py         # 배치1 (폐기, 코드는 보존)
│   ├── postprocessing.py           # simplex 스케일 수정 + piecewise 단조 보정
│   ├── validation.py               # LOYO, 누설없는 ES, 짝비교, 표본가중 전달
│   ├── causality.py                # 규칙 3항 자동 감사
│   ├── scada.py                    # 정렬·집계·가동률·타깃 정제
│   ├── grids.py                    # 격자 가중 (IDW 는 폐기, 재설계 대기)
│   ├── logger.py  utils.py
└── experiment_log.xlsx
```

### 5.1 후처리 `src/postprocessing.py` — 수정 내역

**[측정] 기존 Nelder-Mead는 실질적으로 alpha 하나만 최적화하고 있었음.**
scipy는 x0 성분이 0이면 0.00025, 아니면 ×1.05로 초기 simplex를 만듦.
`initial_guess=[1.0, 0.0, 10.0]` → 설비용량이 21,600 kWh인데 beta를 0.00025 kWh, zero_th를 0.5 kWh 스텝으로 탐색.

| | 점수 | 찾은 파라미터 |
|---|---:|---|
| 원본 OOF | 0.6570 | — |
| 기존 코드 | 0.6617 | α=0.96, β=−0.00, z=10.43 ← beta/zero_th 거의 안 움직임 |
| simplex 스케일 수정 | 0.6717 | α=0.88, β=1329, z=439 |
| 스케일 + 다중 시작점 | 0.6719 | α=0.83, β=1839, z=506 |

**한 줄 때문에 +0.010을 놓치고 있었음.**

추가로 구간별 단조 보정(piecewise) 도입.
piecewise가 찾은 절점 `[0.17, 0.206, 0.331, 0.552, 0.759, 0.988]` — §1.3의 floor 우월성을 독립적으로 재발견.

**[측정] 실제 후처리 여지는 LB 기준 +0.0236(Tweedie) / +0.0026(MAE+W)** (§3.10).
OOF 로 잰 값은 믿지 않음 (§3.6).

### 5.2 실험 로그 스키마

`Objective` / `Sample Weight` 열 추가 (목적함수를 실험 변수로 쓰기 시작했으므로).
LB 를 `Public_LB_Score` / `Public_LB_1-nMAE` / `Public_LB_FiCR` / `private_score` 로 분해.
**총점만 적으면 "왜 떨어졌나" 를 사후에 분해할 수 없음. v14 가 정확히 그 경우였음.**
로거는 엑셀 수기 편집이 만드는 공백 행과 Notes 줄바꿈 흘러넘침을 자동 복구함.

---

## 6. 규칙 준수 (대회 3항 Data Leakage)

`src/causality.py`가 **실행 가능한 증명**을 제공함.
원리: 블록 B의 `data_available_kst_dtm` 이하인 행만으로 피처를 다시 만들어, 전체 데이터로 만든 값과 B 구간이 일치하는지 대조.

**[측정] 검증 결과**

| 방식 | 판정 |
|---|---|
| groupby 없는 전방참조 (`shift(-1)`, `rolling(center=True)`) | ❌ 블록당 1행씩 위반 |
| 현재 features.py 방식 (`diff(1)`, `shift(1)`, 후방 rolling) | ✅ 통과 |
| 블록 groupby 전방참조 | ✅ 통과 |

**현재까지 제출한 전 버전이 규칙 위반이 아님.** 후방 참조만 쓰고 있어서 블록을 넘어도 더 일찍 공개된 예보를 쓰는 것이기 때문.

`lead_hours`는 위반이 아님. `forecast_kst_dtm`과 `data_available_kst_dtm` 모두 예측기준시점에 이미 손에 있는 일정 정보이며, 주최측이 test CSV에도 이 컬럼을 제공함.

**표본가중은 위반이 아님.** 가중은 학습 라벨로만 만들며 추론 시점에 쓰이지 않음.
학습 라벨은 예측기준시점 이전의 확정 실적임.

**연 기후 특성 PDF 는 사용 불가.** 연말 이후 발간(2023년판 → 2024-01-16)이므로
예측기준시점에 존재하지 않음. 가설 생성 용도로만 씀 (§3.9).

2차 산출물 제출 시 `--audit` 실행 로그를 첨부함.

---

### 6.x `[07-28 추가]` 유의사항 7 — 평가 데이터는 추론 전용

"평가 데이터셋은 제출 파일(예측 결과) 생성을 위한 **추론 목적으로만** 사용할 수 있습니다."

* 허용: inference.py 가 테스트 기상 피처를 읽어 각 행을 예측 (현행 파이프라인 전부 준수)
* 금지: 테스트 피처로 판별기 학습·피처 선택·분포 진단 등 추론 외 사용.
  transduction 이 관행인 대회도 있으나 **이 대회는 명문으로 금지함.**
* 이 조항 때문에 step31 v1(train vs test 적대적 검증)을 실행 전에 폐기하고
  학습 3개년 내부 드리프트(v2)로 대체함.

## 7. 실험 이력

| 버전 | OOF | Public LB | 1−NMAE | FICR | 주요 변경 |
|---|---:|---:|---:|---:|---|
| Baseline | — | 0.588346 | 0.863681 | 0.313010 | 샘플 제출 |
| v1 | 0.7137(Tr) | 0.593206 | 0.861465 | 0.324947 | 달력 + GFS 80m (RF) |
| v2 | 0.7993(Tr) | 0.602090 | 0.863945 | 0.340235 | LightGBM + 100m |
| v3 | 0.6864 | 0.605691 | 0.866023 | 0.345359 | 5-Fold GroupKFold |
| v4 | 0.6886 | 0.613708 | 0.870777 | 0.356640 | 격자 pivot + 117m 멱법칙 + 풍향 sin/cos |
| v5 | 0.6880 | 0.615171 | 0.870822 | 0.359520 | 푄/열돔 + 시계열 차분 |
| **v6** | **0.6087** | **0.628930** | 0.863924 | 0.393937 | 6대 물리피처 + Pruning + XGB CUDA + **Nelder-Mead** |
| v7 | 0.5925 | 0.614021 | 0.861273 | 0.366769 | LightGBM |
| v8 | 0.6103 | 0.618874 | 0.859303 | 0.378446 | CatBoost |
| v9 | 0.6118 | 0.549858 | 0.813310 | 0.286406 | [오류] 이중 후처리 |
| v10 | 0.6118 | 0.618476 | 0.858726 | 0.378225 | [수정] Raw 결합 후 단일 후처리 |
| **v11** | **0.6156**† | **0.636316** | 0.864890 | 0.407743 | **정제타깃 + LOYO + ES누설차단 + piecewise 후처리** (321위) |
| **v12** | **0.6196**† | **0.636871** | 0.864932 | 0.408809 | **폴드별 top200 가지치기** (291위) |
| **v13** | **0.6161**†‡ | **0.641458** | 0.867673 | 0.415244 | **3그룹 통합을 G1·G3 에 부분 적용** (189위) — **현 기준선** |
| v14 | 0.6298†‡ | 0.629475 | 0.862562 | 0.396389 | 목적함수 MAE + 산식정합 가중 — **실패** (§3.10) |
| v14-nopost | 0.6193 | 0.626863 | 0.861542 | 0.392183 | v14 에서 후처리만 제거. **계측용 제출** |
| v13-nopost | 0.5995 | **_기입 대기_** | | | v13 에서 후처리만 제거. **계측용 제출** — §8.1. 이 한 칸이 전이 모델의 두 번째 점임 |
| v20 | 0.6005 | (미제출) | | | ASOS 관측 10피처. **`[무효]`** — 관측이 gain top-200 에서 전량 탈락, 실험 미실행 (§3.32) |
| v20b | 0.5938 | (미제출) | | | v13 featcols 200 + 관측 10 강제포함. **`[폐기]`** −0.0057, 3/3 음수, 비율 4.07, 그룹 0/3 (§3.32) |
| v21 | 0.5989 | (미제출) | | | A-4 격자 가중 3컬럼. **`[폐기]`** −0.0002 판별 불가. gain 1위·19% 인데 Δ≈0 (§3.33) |
| v22 | 0.5894 | (미제출) | | | max_depth 6→8 **진단**. **`[폐기]`** −0.0104, 3/3 음수, 비율 6.5. 트리 수 40~50%↓ (§3.34) |

† v11 이후 OOF 는 **LOYO** 기준이라 v1~v10(월 GroupKFold)과 절대값 비교 불가.
‡ 중첩 후처리 OOF 는 §3.6 에서 폐기했음. 판정은 `Raw OOF` 열로 함
  (v13 0.5995 / v14 0.6193 — raw 로는 v14 가 높은데 LB 는 낮음. 이유는 §3.10).

**v6(496위) → v11(321위) → v12(291위) → v13(189위) → v14(역전).**
현 1위(0.672315)까지 **0.030856.**

**분해 (v1→v6)**: 총 상승 +0.0357 중 FICR 기여 +0.0345 = **97%**.
**분해 (v6→v13)**: 총 상승 +0.0125 중 NMAE 기여 +0.0019 / FICR 기여 +0.0107.
**v13 에서 처음으로 NMAE 가 유의하게 움직였음** (0.1351 → 0.1323).

---

## 8. 로드맵

### 8.1 즉시 — 전이 모델을 2점으로 만듦

현재 후처리 여지의 LB 실측점이 **1개(v14)뿐**이라 아무것도 고정할 수 없음.
`v13 --no-post` 제출 1회가 v13 의 raw LB 를 직접 줌.
**점수가 아니라 정보를 사는 제출임.**

사전 예측 (둘이 서로 안 맞는다는 것 자체가 이 제출이 필요한 이유다):

| 유도 경로 | 예측 |
|---|---:|
| raw 오프셋 (v14 raw: OOF 0.6239 → LB 0.6269, 오프셋 +0.0030) | **0.606** |
| 여지 차감 (0.641458 − 0.0202) | **0.621** |

**판정표 (사전 고정)**

| v13-nopost | 해석 | 다음 행동 |
|---|---|---|
| ≤ 0.612 | Tweedie 의 실제 여지가 ≥0.029. 후처리가 Tweedie 를 강하게 편애 | 높은 w 혼합 폐기. 트랙 A 로 전량 전환 |
| 0.612 ~ 0.628 | OOF 여지 추정이 대체로 맞음. 혼합 성립 여지 있음 | **w=0.5** 혼합 1회로 볼록성 검정 |
| ≥ 0.632 | v13 도 여지가 작음 → §3.10 의 raw 분해가 틀렸음 | §3.6 · §3.10 재작성. (**'제출 중단' 은 v17 에서 삭제** — §8.7 참조) |

**`[개정 v17]` B-1 은 §3.14 에서 폐기됐으므로 위 표의 'B-1 먼저' 는 전부 삭제함.**
어느 칸이든 다음 행동은 §8.2 의 B-2 와 §8.3 의 A-6 임.

**w=0.5 혼합의 사전 등록 판정 (학습 0회 — 양쪽 `raw_test_preds.csv` 평균 후 후처리)**
> total(0)=0.641458, total(1)=0.629475 가 이미 알려져 있고 raw 는 w 에 단조 증가(§3.12)이므로,
> 여지(w)가 선형이면 w=0.5 는 두 끝점 사이를 벗어날 수 없음.
> 즉 이 제출은 수준이 아니라 **여지(w)의 볼록성**을 검정함.
> `> 0.6415` → 내부 최적점 실재. w=0.35 로 한 번 더.
> `≤ 0.6415` → 선형 이상 아님. **§3.12 혼합 트랙 종결.**

### 8.2 `[v17 전면 재작성]` 트랙 B — 밴드 (총점 0.0095, 약 100계단)

**후처리 축은 닫혔다.** 남은 것은 하나뿐이며, 그것은 후처리가 아니라 학습 문제의 변경임.

| # | 작업 | 상태 | 근거 |
|---|---|---|---|
| B-1 | 조건부 후처리 — 강도를 행별 산포로 변조 | **폐기** | step12/15. 산포 기울기가 예측 수준 교란이었음 (§3.14) |
| B-1' | 시각·계절 버킷별 배율 | **폐기** | step14. m=1.000. 진단 자체가 거짓 양성 (§3.16) |
| B-3 | 그룹별 후처리 강도 분리 | **폐기** | 같은 p 함수 공간. G3 관측치는 총점 +0.0004 로 노이즈 밑 (§3.15) |
| — | 절점 격자 해상도 | **폐기** | step16. 변형 5개 전부 하락, 양방향 폐쇄 (§3.15) |
| **B-2** | **채점조건부 타깃 학습** `Ê[a \| a ≥ 0.10cap, x]` | **← 유일한 생존. 다음** | §3.14. p 의 함수가 아닌 축. §3.8·§3.10 어디에도 안 걸림 |
| B-2b | 행별 예측분포 → 기대효용 직접 최적화 | B-2 통과 시 | B-2 는 조건부 기댓값 1개만 씀. 완전판은 분위회귀로 각 행의 분포를 뽑아 `argmax_f E[0.5(1−\|f−a\|/C) + 0.5·price(\|f−a\|/C)]` 를 푼다 |

**B-2b 가 왜 유망한가.** 현 1위는 2위보다 **정확도가 낮은데(0.8794 vs 0.8869) FICR 로 이겼다**
(§3.13). 전역 단조 변환으로는 그 정도 프리미엄이 안 나온다는 것을 §3.14/3.15 가 증명했음.
남은 설명은 **행별 분포에 근거한 점예측 배치**뿐임. XGBoost 2.0+ 의 `reg:quantileerror` 로
분위 5~7개를 뽑으면 재학습 5~7회로 구현 가능함.

절차: B-2 는 raw OOF 연도짝비교로 판정(§3.6 규칙 1~2, 부호 방향 명시), 최종은 LB.

### 8.3 트랙 A — 정확도 (총점 0.0214)

몫이 크지만 비싸다. §3.8 의 따름 정리(NWP 로부터 유도 가능한 것은 이미 전부 모델이 갖고 있음)
때문에 남은 것은 **정보를 늘리거나 표현을 바꾸는 것** 뿐임.

| # | 작업 | 상태 | 비고 |
|---|---|---|---|
| A-6 | lead_hours + 블록 내 전방 참조 | **폐기 (§3.18)** | lead 는 `hour` 의 재라벨링(+0.0001), 블록 피처는 선택 변위로 −0.0035 (3/3 음수) |
| **A-4** | **격자 가중 재설계 (거리 아님, 데이터 기반)** | **← 1순위. 코드는 이미 있고, §3.20 이 사전 예측까지 줌 (G3 에서 가장 커야 함)** | `src/grids.py` 의 `weighted_extract` 가 임의 가중치를 받는데 **한 번도 안 썼음.** §3.4 가 "격자 가중은 데이터가 고르게 해야 함" 고 지시했는데 지금도 균등평균 `_mean_` 과 개별 격자를 나란히 던져 주고 있음 |
| A-3 | 설정 배깅 (학습 문제가 다른 모델 단순 평균) | **3순위. 재평가 필요** | 시드 배깅은 산포가 1%cap 뿐이라 죽었음(§3.17). 그러나 v13↔v14 산포는 2.4~5.9%cap 임(step12). **설정 다양성은 시드 다양성의 3~6배다.** 한 번도 제대로 안 해봤음 |
| A-2 | 격자 구조 인코더 (작은 NN) | 미착수 | A-4 가 실패하면 그때. 트리가 표현 못 하는 유일한 남은 구조 |
| A-5 | 안정도 상호작용 (온위·벌크Ri·NDNLW·운량·BLH↔117m) | 미착수 | §3.2 규칙에 따라 **상호작용 형태로만** |
| A-1 | 외부 NWP 1종 추가 (후보: ECMWF open data) | 규칙 검토 필요 | 아래 3조건 전부 충족해야 착수 |

**A-6 상술 — 모델이 아직 한 번도 못 본 축이 둘 있음.**

1. **`lead_hours`** — 예보 블록은 09KST 초기화 → 13KST 공개 → 익일 01시~익익일 00시다.
   즉 **같은 예보 안에서도 lead time 이 12~35시간, 3배 차이**가 난다. NWP 정확도는 이 축을
   따라 크게 떨어지는데 모델에게는 그 컬럼이 없음. 파생이 아니라 **없던 축**이라
   §3.8 따름정리에 걸리지 않음. §6: 위반 아님(주최측이 test CSV 에도 줌).
2. **블록 내 전방 참조** — 24시간이 동시 공개이므로 `groupby('data_available_kst_dtm')` 안에서
   `shift(-1)` / `rolling(center=True)` 는 합법임(§6, `causality.py` 로 실행 가능한 증명 보유).
   지금은 후방 참조만 씀.
   **부수 결함**: `features.py` 의 `gfs_v117_lag1 / ramp_1h / ramp_2h / accel / roll_mean_3h /
   roll_std_3h` 는 **groupby 없이** 계산되어 블록 경계를 넘음. 규칙 위반은 아니지만
   (전부 후방 참조) 서로 다른 초기화 시각을 섞고 있어 물리적으로 부정확함.
   A-6 은 이 수정을 겸함.

설계는 step9 와 같은 2x2 요인(A 기준 / B +lead / C +블록 / D 둘다)으로 **주효과를 분리**함.
무엇이 원인인지 모른 채 두 개를 같이 바꾸면 다음 실험을 설계할 수 없음(§3.5).

**A-1 착수 전 확인 (하나라도 아니면 즉시 폐기)**
1. 2022–2025 기간 과거 예보를 무료·공개로 받을 수 있는가
2. 각 예보의 **발표 시각**이 명시되어 예측기준시각 이전임을 증명할 수 있는가
3. 라이선스가 상업이용 허용이고 2차 심사에 제출·재현 가능한가

규칙 위반 리스크가 기대이득보다 큼. 애매하면 하지 않음.

### 8.4 하지 않을 것

| 항목 | 근거 |
|---|---|
| 모델 교체 (LightGBM/CatBoost 단독) | v6/v7/v8 폭 0.0149 는 노이즈 바닥 ±0.013 안 |
| 스태킹 | §3.8 — 같은 입력을 쓰면 정보가 0 |
| 앙상블 가중 최적화 | +0.0003±0.0007 |
| **전역** 후처리의 매듭 수·형태 튜닝 | **[v17 확정]** 포화. 근거가 '다 해봤음' 에서 **'변형 5개 전부 하락, 현행 6매듭이 국소 최적'** 으로 바뀌었음 (§3.15) |
| **조건부/공변량 후처리 일체** | **[v17 신규]** §3.14 — 후처리는 p 의 함수인데 이득의 52%가 나오는 저역에서 p 가 무정보다. 원리적 한계이므로 이 축의 어떤 변형도 시도하지 않음 |
| 잔여 부호편향을 근거로 한 밴드 진단 | **[v17 신규]** §3.16 — 회귀-평균 인공물 + floor 의 흔적. 어떤 모델에서도 사라지지 않음 |
| 시드 스위프·시드 배깅 | **[v17 확정]** §3.17 — 시드 산포가 1%cap, 밴드 반폭이 6%cap |
| 목적함수를 밴드 쪽으로 더 미는 것 | §3.10 — 후처리와 같은 상금을 두고 겹침 |
| 물리 파생 피처 추가 | §3.2 — 트리 단조변환 불변성 |
| HPO / 시드 스위프 | §3 — 전부 노이즈 바닥 아래 |
| Public LB 기준 최종 선택 | Public 40% 과적합. 최종은 로컬 LOYO 기준 |

### 8.5 `[정정]` "미사용 컬럼 재고" 는 틀린 진단이었음

이전 판에서 LDAPS 50m·NDNLW·운량·GFS 80m·PBL 등을 "안 쓰는 절반" 으로 분류했으나,
확인 결과 **이 컬럼들은 전부 원본 그대로 812개 안에 이미 들어 있음.**
가지치기가 지우는 것은 lsm/지형고도/강수/기압 격자 계열뿐임.
안 쓴 것은 컬럼이 아니라 **그 컬럼들의 상호작용 형태**다. A-5 가 그것을 겨냥함.

### 8.6 `[폐기]` v10 문서의 "95회 제출 소진 로드맵"

Tweedie 스위프 10회, max_depth 그리드 10회, 시드 아베리징 10회, 가중치 그리드 25회.
근거: §3 의 노이즈 바닥(±0.013)과 가중치 최적화 실질 이득(+0.0003)을 고려하면
이 계획의 대부분이 노이즈를 신호로 읽는 절차다. 또한 Public 40% 기준 그리드 서치는
Public 과적합이며 Private 60% 에서 되돌아온다.

### 8.7 제출 운용 원칙

* **`[v17 개정]` 제약은 제출 횟수가 아님.** 1·2위는 각각 101 / 98회 제출했고 우리는 16회다
  (7-26 기준 잔여 약 90회). 그리고 §3.6 이 후처리 축의 OOF 계측기를 폐기했으므로
  **밴드 축 질문은 LB 로만 잴 수 있는데 그 계측기가 남아돈다.**
  → 밴드/후처리 축 가설은 OOF 로 '좁히고' **LB 로 판정**함. 정확도 축은 기존대로 OOF 판정.
  사전 등록된 서로 다른 가설을 LB 로 검정하는 것은 LB 그리드 서치가 아님.
  (단 최종 파일 선택은 여전히 로컬 LOYO 기준 — 아래 마지막 줄)
* HPO는 전부 로컬에서. 정확도 축 제출은 **서로 다른 가설당 1~2회**
* 제출 전 raw OOF 로 `is_difference_real()` 판정 → ⚠️면 제출하지 않음 (§3.6)
* **`[v17 신규]` 판정 규칙에는 부호 방향을 반드시 씀.** `is_difference_real` 은 '차이가
  실재하는가' 만 답함. step16 에서 3/3 일관 ✅ 인데 −0.0015 였음 (§3.6 규칙 6)
* **제출 전에 판정 규칙을 문서에 먼저 씀.** 결과를 보고 해석을 맞추면 사후선택임
* 하루 5회는 **한꺼번에 던지지 말고 순차로 분기**함. 데이콘은 제출 즉시 public 점수가 뜨므로
  1회 결과로 다음 1회를 정할 수 있음. 배치 제출은 그 정보를 버리는 것임
* 제출 후 `premium_track.py` 로 프리미엄까지 기록함. 총점만 보면 어느 축에서
  움직였는지 알 수 없음 (§2.3 의 오판이 그 때문이었음)
* 최종 파일 선택은 **Public LB 1등이 아니라 로컬 LOYO 1등**으로

---


### 8.8 `[검토 07-28]` 외부 데이터 — 후보 2개, 폐기 다수

규칙 4 는 외부 데이터를 허용하되 4중 조건(공개·라이선스·행별 기준시점·재현 의무)을 걺.
후보를 그 조건에 대본 결과:

**~~1순위~~ `[폐기 07-28]` 기상청 공공 관측(ASOS/AWS) 최근 관측 피처.**
> **v20b 실측 −0.0057 (3/3 음수, 비율 4.07, 그룹 0/3) 으로 종결됨 (§3.32).**
> 규칙·데이터·구현은 전부 문제없었고 **정보 자체가 이 표현으로는 해로웠음.**
> 데이터(`data/external/asos_hourly.csv`)와 코드(`step33_download_asos.py`, `features_obs.py`)는
> 2차 심사 소명용으로 보존함. 아래 원문은 착수 시점의 근거로 남겨 둠.

규칙 3의 실측 조항이 이 패턴을 **명문으로 허용함**: "기상 관측자료 등 실측 데이터는
예측기준시점 전에 이미 관측·공개·확정된 과거 자료에 한해 사용할 수 있습니다."
* 가치 ① — **2025 체제를 행별로 합법 추적하는 유일한 통로.** 각 행의 기준시점(전날 13시)
  이전의 실제 바람·습도가 피처로 들어가면, 모델이 2025 의 밀린 체제를 관측으로 봄.
* 가치 ② — 전날 관측 − 전날 LDAPS 예보 = **최근 NWP 편차.** 관측은 NWP 에서 유도
  불가능하므로 §3.8 따름정리에 안 걸리는 진짜 새 정보임.
* `[검증 07-28]` ⑵ API 확인 — 공공데이터포털 '지상(종관, ASOS) 시간자료 조회서비스'
  + 기상청 API허브, 둘 다 무료 가입 키 → '누구나 접근 가능' 충족. ⑶ ASOS 시간자료는
  수십 년 아카이브라 2022~2025 완비. ⑷ ASOS 는 준실시간 공개 — 기준시점 −1h(12:00)까지만
  쓰면 안전 여유 충분. ⑴ 관측소 근접성만 남음 — 관측지점 메타데이터를 받아 info.xlsx
  터빈 좌표와 거리 계산으로 스크립트가 자동 선정 (태백 216 ASOS 유력, AWS 더 근접 가능).

**`[07-28 승격]` 1순위 — ECMWF open data (§8.3 A-1 재개).** ASOS 폐기로 남은 유일한 외부 원천임.
§3.32 의 블록 상수 규칙에 **안 걸림** — 예보라 리드타임별로 행마다 값이 다름.
* 라이선스 CC-BY-4.0 (2022-10 이후) = 상업이용 허용 → A-1 조건 3 통과.
* `[검증 완료 07-28]` AWS 공개 버킷 `ecmwf-forecasts` 직접 리스팅으로 확인:
  ⑴ 커버리지 **2023-01-18 ~ 현재** (2025년 1~12월 완비). **2022 는 없음** — G1·G2 는
  1/3 연도 결측(NaN 학습), G3 는 라벨 연도(23~24)가 완전 커버됨. ⑵ 구조
  `{날짜}/{00z,06z,12z,18z}/{ifs,aifs}/` — 전일 12z/18z 런 사용 시 기준시점(13KST) 이전
  공개 충족, 리드 22~51h. ⑶ 익명 접근(`--no-sign-request`) + CC-BY-4.0 → 재현·라이선스 통과.
* 비용 큼 (grib 2년치 다운로드·파싱). 착수는 ASOS 결과 뒤.

**폐기.** ERA5/재분석(규칙 3 실측 조항이 명시 금지), 민간 유료 API(공개성),
발전량·계통 통계(정답 인접 리스크), 연 기후 특성 PDF(§3.9·§3.23 에서 용도 소진).

## 9. 폐기·정정된 가설 기록

2차 발표 시 "무엇을 어떻게 검증했는가"의 근거가 됨.

### 9.1 측정·검증 방법론

| 가설 | 결과 | 근거 |
|---|---|---|
| ES 누설이 CatBoost 85% 쏠림의 원인 | **[폐기]** 누설 효과는 0.0024±0.0012로 쏠림을 설명 못 함 | 5시드 합성 실험 |
| 앙상블 가중치 최적화가 중요함 | **[폐기]** 균등가중 대비 +0.0003±0.0007 | 5시드 합성 실험 |
| XGB가 CatBoost보다 불연속 구간을 잘 잡음 | **[보류]** 0.010 차이는 노이즈 바닥(±0.013) 이내 | §3 |
| 순위 상승 = 유의미한 개선 | **[정정]** LB 가 극도로 조밀(54계단/0.001점). 30계단 상승이 0.00055 점 차이였음 | §2.1 |
| OOF 개선이 LB 로 그대로 전이됨 | **[정정 → 확정]** 변화의 종류에 달렸다. 정규화형 26% / 정보형 79% / 밴드형은 순증이 음수일 수 있음 | §3.5 |
| 중첩 후처리 OOF 가 배포 성능의 최선 추정치다 | **[폐기]** v13 에서 부호까지 틀렸음(−0.0035 vs LB +0.0046) | §3.6 |
| 중첩 후처리는 보조 지표로는 쓸 수 있음 | **[폐기]** 같은 값을 네 번 재서 네 번 다르게 답했음(+0.0017/+0.0101/+0.0105 vs 실제 +0.0026). 계측기가 아님 | §3.6 |
| 후처리 흡수(91%)가 실질 제약임 | **[폐기]** 흡수처럼 보인 것은 중첩 지표의 결함이었음 | §3.6 |
| LOYO 가 연도 간 기상 변동을 노이즈로 만듦 | **[폐기]** step6: 거리 −0.0043, 방향 −0.0017 로 작음. 연도 변동은 재현해야 할 난이도다 | §3.1 |
| 리더보드 상위 1점으로 축의 상한을 알 수 있음 | **[폐기]** 1위가 바뀌자 밴드 상한 추정이 즉시 반증됐음. 상위 3~5점의 분포를 봄 | §2.3 |
| 이득의 **총량**만 재면 다음 실험을 설계할 수 있음 | **[정정]** 총량은 같아도 분포가 다르면 처방이 완전히 달라진다. step12·13·14 가 전부 총량만 재다가 죽었고, 층화 한 번에 세 실험이 못 본 것이 나왔음 | §3.14 |
| `is_difference_real` ✅ 면 채택해도 됨 | **[정정]** 그 함수는 '차이가 실재하는가' 만 답함. step16 이 3/3 일관 ✅ 로 −0.0015 를 냈음. 판정 문구에 부호 방향을 반드시 씀 | §3.6 규칙 6 |
| 새 가설은 새 실험으로 검증함 | **[정정]** step13 의 결론은 §3.11 표에 이미 있었음(A 조건 −0.0005 · 2/6). **설계 전에 기존 표의 해당 칸을 먼저 조회함** | §3.17 |
| 무작위 행 분할로 홀드아웃을 만들어도 됨 | **[정정]** 풍속 자기상관 때문에 훈련쪽에 홀드아웃 복제본이 들어감. 6%/8% 문턱 배치가 그걸 타고 전이됨. 이 데이터셋에서 누설 **+0.0092** — 쫓던 효과의 2배. **시간 블록으로 나눔** | §3.23 |
| 학습 표본이 다른 두 구성을 그냥 비교하면 됨 | **[정정]** 표본 크기와 조건이 섞임. **크기를 맞춘 대조군**이 없으면 부호도 해석 불가 | §3.23 |
| 후처리 적합 절차(연도 가중·축소·출발점)에 여지가 있음 | **[폐기]** 출발점은 9개로 충분(+0.0002). 연도 여지는 누설 제거 후 +0.0051 이고 그중 FICR 몫 91% | §3.23 |
| 강원 연 기후 특성으로 연도 가중을 정당화할 수 있음 | **[폐기]** 부호 대조군이 반대로 나옴(down −0.0008 / up +0.0024). 정체는 이상기후가 아니라 **시간 인접성** | §3.23 |
| 제출 횟수가 희소 자원임 | **[정정]** 1·2위 101/98회 vs 우리 16회. 게다가 밴드 축은 LB 가 유일한 유효 계측기다(§3.6). 제약은 제출이 아니라 **계측기**였음 | §8.7 |
| 피처를 추가하고 top-K 를 그대로 두면 '추가의 효과' 를 잰다 | **[정정]** 풀 크기가 812→822 로 바뀌면 `m0` 의 colsample RNG 가 밀려 top-200 의 **25.6%(51개)** 가 재추첨됨. **뽑히지도 않은 컬럼 10개가 51개를 갈아치웠음.** 피처 추가 A/B 는 `--featcols-from` 으로 선택을 상수로 고정하고 잼 | §3.32 |
| 게이트가 미달이면 그 축은 닫힌 것임 | **[정정]** 미달의 원인이 **계측 실패**일 수 있음. v20 은 새 피처가 선택에서 전량 탈락해 실험이 실행되지 않았는데 미달로 떴음. 미달일 때도 원인을 한 번은 염(학습 0회) | §3.32 |

### 9.2 피처·데이터

| 가설 | 결과 | 근거 |
|---|---|---|
| vestas_wtg12 풍향계가 170° 틀어짐 | **[폐기]** head 49시간의 착시. 실제 오프셋 −8.8°, 산포 51.8°(불안정). 계통 오차는 wtg04(+23.4°, 산포 10.2°) 하나뿐 | 3년 전체 |
| 커테일먼트 시간대(정오) 피처 | **[보류]** 제주 계통 논리, 태백 육상 계통에 근거 약함 | 미검증 |
| GFS `surface_0_sp` 격자차 = 기압경도 | **[폐기]** 9,000 Pa 차이가 전부 표고 | §4.4 |
| 물리 파생 피처(배치1)가 NMAE 를 깎음 | **[폐기]** raw +0.0005 / 후처리 +0.0001. 대부분이 단조변환이라 트리에 정보를 주지 않음 | §3.2 |
| LDAPS 50m·복사·운량은 "미사용 컬럼" 임 | **[정정]** 원본으로 812개 안에 이미 있음. 안 쓴 것은 컬럼이 아니라 상호작용 형태다 | §8.5 |
| 거리 기반 IDW 격자 가중이 옳음 | **[폐기]** 최상위 생존 격자 ldaps_g13 이 IDW 최하위권 | §3.4 |
| 피처는 많을수록 좋음 (암묵 가정) | **[폐기]** 812→200 이 +0.0021, 연도별 전부 동일 부호 | §3.3 |
| SCADA 풍속을 2단 스태킹으로 되먹이면 이득 | **[폐기]** −0.0019, G1 0/3. 1단·2단 입력이 같아 구조적으로 정보 0 | §3.8 |
| `lead_hours` 는 모델이 못 본 새 축임 | **[폐기]** `lead = hour + 11` 의 일대일 재라벨링. 주효과 +0.0001. **데이터 명세만 읽었어도 알 수 있었음** | §3.2 §3.18 |
| 블록 내 전방참조가 새 정보를 줌 | **[폐기]** 3/3 음수 −0.0035. 상관된 파생 96개가 gain 을 나눠 가지며 top-200 에서 다른 계열을 밀어냄 | §3.18 |
| 피처를 더할 때 top-K 는 그대로 둬도 됨 | **[정정]** 812에 96을 더하며 top-200 을 유지하는 것은 기존 피처의 11%를 잘라내는 것과 같음. 추가와 선택 예산은 함께 정함 | §3.18 |
| 2023년이 어려운 이유는 기온 변동성 → 램프 | **[폐기]** 겨울 갭이 비겨울보다 작고, 11월은 오히려 +0.0483 좋았으며, 램프 5분위가 비단조 | §3.9 |
| 고풍속 컷아웃이 최악 오차의 원인임 | **[폐기]** LDAPS ws10 ≥14 m/s 가 41,146 채점행 중 **59행**. GFS 의 과대예보였음. 컷아웃 상수(20/25/26)는 작동하지 않지만 고칠 가치도 없음 | §3.19 |
| G3 격차는 기종(U136)·로터 크기 차이 때문임 | **[폐기]** 비출력 0.2887 vs 0.2891 kW/m² — **차이 0.14%**. 로터가 큰 만큼 정격도 커서 상쇄. 실측 정격도달 12.2 vs 11.8 m/s | §3.20 |
| G3 는 전반적으로 어려운 그룹임 | **[정정]** 저풍속(0~5)에서는 **세 그룹 중 최고**(밴드 32.3%). 8~14 m/s 에서만 무너짐(19.1%→11.5%). 원인은 이질적 단지 구성 + 1.7배 넓은 공간 범위 | §3.19 §3.20 |
| G3 는 터빈 간격이 좁아 후류가 심함 | **[폐기]** G3 기당 512 m = **3.8 D**, G1 240 m = **1.9 D**. 내부 후류는 G3 가 오히려 적음 | §3.20 |
| 가용률 저하가 최악 오차의 주범임 | **[정정]** 최악 꼬리에는 몰려 있으나(lift 3.8배) 전체 손실로는 **19%**. 그리고 1위도 못 고치는 **공통 손실**이라 갭 분해에 넣으면 안 됨 | §3.19 |
| 격자 산포 피처가 균등평균이 버리는 정보를 준다 | **[측정 후 폐기]** raw OOF +0.0023 (3/3) 인데 **LB −0.0010**. 선택 예산과 대체 관계(상호작용 −0.0027). 컬럼 54개의 비용이 이득을 넘었음. 1~2컬럼 버전만 남음 | §3.21 |
| 통합 학습의 이득은 라벨 양에서 나온다 | **[정정]** G1+G2 **+0.0126** vs G1+G3 **+0.0034**. 라벨 비율은 1.49배뿐. **동질성이 양을 이김.** G3 의 세 번째 핸디캡 = 동질 파트너 부재 | §3.22 |
| ASOS 관측 피처가 게이트를 못 넘었으니 관측은 무용함 | **[무효]** 관측 컬럼이 gain top-200 에서 전량 탈락(운영 경로 joint 0/30). 게이트가 잡은 +0.0012 는 obs 가 **0개인** G1(+0.0013)·G3(+0.0011)에서 obs 가 있는 G2(+0.0007)보다 **더 컸음** — 선택 재추첨이었음. 판정 자체가 성립하지 않음 | §3.32 |
| 관측은 NWP 에서 유도 불가능한 새 정보이므로 이득이 있음 | **[폐기]** 강제 포함해 재측정하니 **−0.0057 (3/3 음수, 비율 4.07, 그룹 0/3)**. gain 비중 1.3~1.9% 로 트리가 **쓰기는 함** — 쓰는데 해로움. `[가설]` 블록 상수 10개가 날짜 식별자로 작동. '§3.8 따름정리를 안 걸린다' 는 필요조건이었지 충분조건이 아니었음 | §3.32 |
| 새 정보원이면 §3.5 정보형 전이율(79%)이 적용된다 | **[정정]** 정보형은 '새 정보' 가 아니라 **'모델을 실제로 개선하는 새 정보'** 를 뜻함. v20b 는 원천이 다른 정보인데 raw OOF 부터 음수였음. 전이율은 raw OOF 가 양수인 뒤에 쓰는 값임 | §3.32 |
| 균등평균이 그룹 간 차이 13%를 버리므로 데이터 기반 가중이 이득이다 (A-4) | **[폐기]** 폴드별·그룹별 NNLS 가중 3컬럼을 예산 0으로 넣었더니 Δ **−0.0002**(부호 갈림, 그룹 0/3). NNLS 가 2~3격자로 붕괴 — 격자 간 상관 0.99 라 '두 컬럼 가중평균' 이었고 트리가 이미 근사하고 있었음 | §3.33 |
| 모델이 많이 쓰는 피처(gain 상위)는 가치가 있다 | **[폐기]** `wgrid_ws` 가 **203개 중 gain 1위, 3컬럼 합 19%** 인데 총점 Δ −0.0002. gain 은 **분할의 편의성**이지 정보량이 아님. 피처 가치의 증거는 연도짝비교 Δ 뿐 | §3.33 |
| 압축형 격자 피처는 선택 예산만 아끼는 게 아니라 정보도 준다 | **[폐기]** §3.21 의 정정을 직접 시험함 — 예산을 **전혀 쓰지 않고**(3컬럼 강제, 나머지 200 고정) 압축 컬럼만 주니 남는 정보 성분이 **0**. §3.21 이 옳았음 | §3.33 §3.21 |

### 9.3 산식·전략

| 가설 | 결과 | 근거 |
|---|---|---|
| FICR 후처리가 최우선 레버다 | **[정정]** v1→v6 에서는 맞았음. 이후 국면이 바뀌었음 | §2.4 |
| 밴드 프리미엄이 이미 1등과 동급 → 여력 소진 (1차) | **[폐기]** piecewise 가 +0.0549→+0.0665 로 올려 당시 1등(+0.0523) 추월 | §2.3 |
| 밴드는 포화됐음, 남은 갭은 전부 정확도다 (2차) | **[폐기]** 새 1위 프리미엄 +0.0857. 밴드에 총점 0.0095 가 남아 있음 | §2.2, §2.3 |
| 정제 타깃이 NMAE 를 올림 | **[정정]** OOF 에선 +0.0048 였으나 LB NMAE 는 +0.0010 뿐. 실제 이득은 FICR | §2.4 |
| Tweedie 가 이 타깃에 맞음 | **[정정]** v6 시점엔 옳았다. 정제 타깃 도입 + 0점 질량이 채점 밖이라는 점으로 근거가 약해졌음 | §3.10 |
| MAE 목적함수가 NMAE 를 개선함 | **[정정]** 개선의 96%는 FICR 이었음. MAE 의 최적 예측이 조건부 중앙값이고 FICR 이 최빈 정확도를 보상하기 때문 | §3.10 |
| v14 의 LB 기대치는 +0.005 ~ +0.010 | **[정정]** 실제 −0.0120. **부호까지 틀렸음.** 누락한 항은 "후처리 여지는 총량이 정해져 있고 모델 개선이 그것을 잠식함" | §3.10 |
| 폴드 평균이 MAE 모델의 밴드 집중을 푼다 | **[폐기]** 정반대. D +0.0049(6/6). MAE 잎 값이 중앙값이라 고분산이고 평균이 그 분산을 깎음 | §3.11 |
| 혼합(Tweedie×MAE)이 v13 을 이김 | **[보류]** 판정 지표가 정답을 아는 두 끝점의 순서를 반대로 매긴다. 전이 모델 확보 후 재판정 | §3.12 |
| 행별 불확실성으로 후처리 강도를 변조하면 이득 (B-1) | **[폐기]** 수준을 층화하면 산포 기울기 부호가 갈림(L1 −0.090 / L3 +0.040). 교란이었음 | §3.14 |
| 후처리 후 평균 부호편향이 잔여 프리미엄을 가리킨다 | **[폐기]** 실제값으로 구간을 나눈 평균편향은 오차 있는 어떤 예측기에서도 저출력 +, 고출력 − 가 나옴. floor 흔적이 겹침 | §3.16 |
| 후처리 격자 해상도에 여지가 있음 | **[폐기]** 증설(11)·축소(5/4/3) 전부 총점 하락. 현행 6매듭이 국소 최적 | §3.15 |
| 이득이 L1 에 몰렸으니 L1 에 자유도를 줘야 함 | **[정정]** L1 은 상수가 최적이라 해상도 무관. 격자의 한계 감도는 행의 95%가 있는 L2·L3 에 있음 | §3.15 |
| 밴드는 후처리로 더 캘 수 있음 | **[폐기]** 후처리는 p 의 함수인데 이득의 52%가 나오는 저역에서 p 가 무정보다. **원리적 한계**다. 남은 길은 다른 타깃(§8.2 B-2)을 학습하는 것뿐 | §3.14 |
| 분산 축소(시드 배깅)가 밴드 프리미엄 레버임 | **[폐기]** N=8−N=1 평균 −0.0003. 시드 산포 1%cap vs 밴드 반폭 6%cap | §3.17 |